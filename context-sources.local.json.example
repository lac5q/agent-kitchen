{
  "_comment": "Copy to ~/.memroos/context-sources.local.json (never commit). Deep-merged over context-sources.config.json per deployment. Set CONTEXT_SOURCES_LOCAL_CONFIG env var to override the default path for testing.",
  "sources": [
    {
      "id": "meet-recordings",
      "enabled": true,
      "_comment": "Fathom dual-account provider (luis@epiloguecapital.com + luis.calderon@gmail.com). Prefer: bash scripts/integrations/fathom/install-local.sh. Sets MEETINGS_INGEST_COMMAND to scripts/integrations/fathom/fathom-ingest.sh and reads API keys from 1Password via ~/.memroos/integrations/fathom-accounts.json. Circleback remains a private alternative."
    }
  ]
}
