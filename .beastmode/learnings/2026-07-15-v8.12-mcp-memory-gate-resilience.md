# Beastmode Learning: v8.12 MCP Memory Gate Resilience

**Created:** 2026-07-15T20:55:00Z  
**Updated:** 2026-07-15T20:55:00Z  
**Version:** 2026-07-15.1  
**Task:** MemRoOS v8.12 Phases 154–156 (Codex strict memory gate resilience)

## Role Routing

| Role | Model | Effort | Notes |
|------|-------|--------|-------|
| Director / Orchestrator | Grok 4.5 | high | This session |
| Worker / Executor | MiniMax-M3 | high/medium | Invoked via Anthropic-compatible `api.minimax.io`; smoke `MINIMAX M3 OK` |
| Watcher / Adversarial Validator | Grok 4.5 | xhigh posture | Separate BinEval per phase; rejected rubber-stamps |

**Watcher degraded flag:** Partially YES — Phase 156 Worker returned a tool-call stub (no usable diffs); Director implemented that slice. Phases 154–155 Worker usable (154 accepted with Director test add; 155 raw diffs rejected for hallucinated structure, Director rewrite accepted).

**Not used:** Qwen (locked out). GitNexus / MemRoOS MCP main brain unavailable in this cloud run (cursor-cloud only).

## What Worked

- MiniMax M3 smoke + Phase 154 bounded contract produced a clean, style-matched helper.
- Adversarial Watcher catching MiniMax hallucinations (Phase 155 invented symbols) prevented bad merges.
- Keeping `MEMROOS_REQUIRE_SERVER_MEMORY=1` and fixing probe honesty (15s) addresses the Codex RCA without allow-degraded.

## What Failed

- MiniMax Phase 155 invented a parallel API (`_PROBE_INTERVAL_S`, locks) not present in `mem0-server.py`.
- MiniMax Phase 156 returned an internal tool-call stub instead of diffs — treat as Worker degraded for that slice.
- `check_disk_space` lambdas in existing tests took 0 args; path-scoped calls broke them until `*args` fixed.

## Routing Rule To Change

Yes. For MemRoOS Beastmode with MiniMax-M3 via raw Anthropic API: require unified diffs against **quoted current file excerpts**; if output invents symbols or emits tool stubs, Director implements and marks Worker degraded. Do not claim MiniMax applied a patch unless the diff applies cleanly to HEAD.

## Verification Pattern

- Phase 154: vitest timeout honesty (default 15s, env override, Abort→down).
- Phase 155: pytest livez + cached Qdrant + source has no self-HTTP client calls; bash -n healthcheck.
- Phase 156: home advisory critical → vector up; disconnected → down; path disk critical → degraded; MCP diagnostics include tier detail without secrets.

## Promotion

- Candidate: document `MEM0_HEALTH_TIMEOUT_MS` in operator MCP docs on Mac follow-up.
- Do not promote MiniMax hallucinated rewrites without Director apply+test.
