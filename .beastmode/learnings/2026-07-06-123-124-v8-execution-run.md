## BM-20260706-1110 Phase 123 + 124 (v8.0/v8.1 execution run)
- Director/Lead: GLM-5.2 @ xhigh (ZCode session)
- Watcher/Adversarial Validator: GLM-5.2 @ xhigh (tier-2 inline; Claude tier-1 session-limited)
- Executor: MiniMax-M3 / medium (Hermes `hermes -z ... -m MiniMax-M3`)
- Harness: manual git branch isolation + Hermes subprocess dispatch
- Acceptance checks: outbound-policy 12/12; belief-eval 5/5 cases; operator-load-slo 12/12; typecheck clean; full belief+provenance suite 29/29
- Result: pass (all three phases merged)
- Watcher fallback chain: tier 1 (claude-opus) → session-limit → tier 2 (glm-5.2 xhigh inline)
- Effective Watcher: tier 2 (glm-5.2) @ xhigh
- Watcher degraded: yes (Claude session cap hit both runs)

What worked:
- Pre-grounding each PLAN.md in exact file:line surfaces before dispatch let MiniMax produce correct large modules (1300-line gate, 600-line eval harness) with zero architectural mistakes.
- Watcher BinEval grep/read probes (gold-stage assignments, content leakage, exec, FK targets) were fast and decisive without a Claude subprocess.
- Verifying worker self-reports independently caught a real measurement artifact (shell pipe masking node exit code) and confirmed gate correctness — never merge on self-report.

What failed / drifted:
- Worker self-reported "corrupted test file" (Phase 122) that was actually transient DB state — re-run cleared it. Confirms the Phase 122 learning.
- Worker ran out of turn mid-finishing (Phase 123): implementation complete, tests green, but missed mechanical wiring (package.json script, ci.yml job) + commit. Required Director to finish ~5 min of config glue.
- MiniMax HTTP 529 overload + 10m hang on one retry (Phase 122 fix attempt) — worker tier is volatile under load; the actual fix was unnecessary.
- Claude CLI session limit forced tier-2 Watcher for the entire run.

Routing rule to change: yes — (1) when a worker reports a syntax/corruption blocker, Director re-runs verification before re-dispatching (confirmed twice now). (2) Worker "ran out of turn before finishing wiring" is a known MiniMax failure mode at ~45-60 min; for phases with mechanical config-glue finish steps, dispatch those as a SHORT follow-up prompt rather than expecting the worker to complete a 9-step checklist in one turn.
Skill/config update needed: yes
Promoted to: this learning + the two routing rules above (pending user approval to add to beastmode SKILL.md "Worker failure modes" subsection)
