# TASK: Phase 229 — Agent Issue Reporting (AGENTREPORT-01..05)

Read the full plan first: .planning/phases/229-agent-issue-reporting/229-01-PLAN.md
RCA context: .planning/STATE.md "Latest Position (2026-08-04 goal run)".

Binding design constraint: the reporting path must ride the WEAKEST surviving credential.
In the RCA, the agent had a working MCP OAuth session (knowledge_write worked) while every
MEMROOS_AGENT_API_KEY surface returned missing_agent_key. report_issue must never be gated
on MEMROOS_AGENT_API_KEY.

1. AGENTREPORT-01 — schema + API:
   - Table `agent_issue_reports` (see plan for columns) added via the governed schema path:
     find how recent tables were added (grep db-schema.ts for the newest ensure/migration
     pattern, e.g. tool_connections from Phase 227) and follow it exactly, bumping the schema
     version the same way. Data access goes through the store chokepoint pattern
     (apps/memroos/src/lib/store/ — mirror how the newest store module is structured, e.g.
     store/audit.ts) — do NOT add raw better-sqlite3 usage outside the allowlist
     (scripts/check-sqlite-allowlist or check:sqlite-allowlist will fail).
   - `POST /api/agent-report` (apps/memroos/src/app/api/agent-report/route.ts):
     auth = authenticateAgentHeaders (bearer agent key) OR the MCP OAuth session mechanism —
     find how MCP OAuth bearer tokens are validated app-side (grep lib/auth/mcp-oauth.ts for
     an introspection/verify helper used by API routes) OR an operator session
     (authenticateUser). Rate limit via lib/auth/rate-limit.ts (5/hour per principal id).
     Caps: title <= 200 chars, body <= 8000 chars. Secret rejection: reject bodies/titles
     matching /ak_[A-Za-z0-9_-]{20,}/, /\bSG\.[A-Za-z0-9._-]{10,}/,
     /Bearer\s+[A-Za-z0-9._~+/=-]{20,}/i, or /\b[a-f0-9]{64}\b/ with HTTP 422 and a coach-back
     error: "report contains credential-shaped content — strip secrets and resend".
   - `GET /api/agent-report` — operator session only (requireRole/capability per the newest
     route conventions); filters: status, severity, component; newest first; limit 100.
   - Special lane for AGENTREPORT-04: when body.component === "onboarding" and the payload
     carries tokenKid (8 hex chars) and no auth header is present, accept it UNAUTHENTICATED
     but rate-limit by IP (3/hour) and force severity/high fields server-side. reporter_kind
     = "onboarding-script".
2. AGENTREPORT-02 — MCP tool: in services/knowledge-mcp/knowledge_system/mcp_server.py add
   `report_issue(title, body, severity="medium", component="general", agent_id=None)` tool;
   register in CORE_TOOLS (capabilities.py). It POSTs to the app /api/agent-report using
   whatever credential the server has, in order: MEMROOS_AGENT_API_KEY bearer, else the same
   app-reachability lane memory_save/mem0 already uses if it carries auth, else the
   unauthenticated onboarding lane is NOT applicable — instead include header
   X-Memroos-Reporter: knowledge-mcp and send to the operator-key-free path: if no credential
   exists, still POST and let the API decide (the API accepts OAuth bearer if the transport
   passed one through). Look at how existing tools obtain MEMROOS app base URL
   (MEMROOS_APP_URL / MEMROOS_PUBLIC_BASE_URL envs already read in this service) and reuse.
   Tool description: "Report a problem with MemRoOS itself (broken tool, auth failure,
   timeout). Works even when other agent surfaces are down." Update the orientation prompt
   string (~line 1639) to mention report_issue.
   Python tests: services/knowledge-mcp has an existing test layout — add coverage for the
   tool's payload shape + credential fallback order using mocks (no network).
3. AGENTREPORT-03 — NOC + Team surfacing: follow the existing NOC Attention contract
   (apps/memroos/src/lib/tool-attention.ts + wherever NOC attention items are produced —
   grep for "attention" producers and the NOC API route). Add open agent_issue_reports as
   attention items (critical/high individually, medium/low as one aggregated item). Add
   ack/resolve: PATCH /api/agent-report/:id with {action:"ack"|"resolve", note?} — operator
   role; each transition writes an audit entry via the existing audit helper
   (event types "agent_issue.acknowledged"/"agent_issue.resolved"). Team page badge: find the
   Team page component (apps/memroos/src/app/team/) and add an "Issues" count chip linking to
   a simple list — keep UI minimal and consistent with existing patterns.
4. AGENTREPORT-04 — onboarding script self-report: the onboarding script is generated by
   apps/memroos/src/app/api/onboarding/script/route.ts (bash template). Wrap its failing
   steps: on any curl 4xx/5xx from the registration/bootstrap calls, POST a secret-free JSON
   report {component:"onboarding", severity:"high", title, body: step+status+truncated error,
   tokenKid} to $MEMROOS_URL/api/agent-report before exiting. Never include the token itself.
   Test: the script template contains the self-report block (string assertions in the route
   test), and the API accepts the unauthenticated onboarding lane (route test).
5. AGENTREPORT-05 — contract propagation: add a short section to agents/AGENTS_TEMPLATE.md
   ("Reporting problems to MemRoOS") naming report_issue as the standing channel and
   .health/bug-reports/ as last resort; update the memroos-troubleshooter skill if present in
   this repo (search .agents/skills/); one line in the orientation prompt (done in step 2).

TESTS: auth matrix (agent key OK / operator session OK / anon rejected except onboarding
lane / onboarding lane rate-limited), secret rejection 422, caps, ack/resolve audit rows,
attention producer output, GET filters. Run targeted vitest + `npm run typecheck`. For
Python: run the service's test command for just your new test file (find how existing tests
run, e.g. python -m pytest services/knowledge-mcp/... -k report).
