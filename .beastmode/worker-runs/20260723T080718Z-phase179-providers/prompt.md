You are the bounded MiniMax-M3 worker for a director-led Beastmode run on memroos.

TASK: Produce a TypeScript provider-registry module at /home/<user>/github/memroos/apps/memroos/src/lib/tool-auth/providers.ts. This is the single source of truth for third-party tool connection metadata that memroos will use with Nango (the OAuth + API-key auth plane chosen for v8.23 / Phase 179).

CONSTRAINTS:
- Pure data + tiny helper functions. No side effects, no I/O, no env reads.
- Use only TypeScript builtins + the `lucide-react` icon imports. Do NOT import anything else.
- 12 providers, organized by 5 categories. Categories are: productivity, developer, crm, finance, other.
- Each provider declares whether auth is OAuth or API-key (mixed-auth providers are not in this slice — those are a follow-on).
- For OAuth providers: include `providerConfigKey` (the Nango provider id slug), `scopes` (the OAuth scopes), and `label` for the connect CTA.
- For API-key providers: include `apiKeyField` (description for the input), `docsUrl` (link to where the user gets the key), and `label`.
- Use Lucide icons available in lucide-react@1.7.0: `Slack`, `Notion`, `Github`, `Cloud`, `MessageSquare`, `Users`, `Banknote`, `Receipt`, `Calendar`, `Database`, `Box`, `LayoutGrid`, `Layers`, `Code2`. Verify each name exists in that version. If uncertain, use a generic icon (`Box` or `Layers`).
- Categories must include the 12 providers split sensibly. Minimum: 2 in each of productivity, developer, crm, finance. Other category is a catch-all.
- Every provider must have a unique `key` (lowercase, hyphen-safe, used as the Nango integration id).
- Order providers within each category by display priority (most-used first).
- Export: `PROVIDER_CATEGORIES` (ordered list of category ids), `PROVIDERS_BY_KEY` (Record<key, Provider>), `getProvidersByCategory()` (returns ordered array), `getProvider(key)` (returns one or undefined).

ALLOWED FILES (write only the output file):
- /home/<user>/github/memroos/apps/memroos/src/lib/tool-auth/providers.ts
- Do not modify any other file.

ALLOWED COMMANDS:
- None. Pure code generation.

FORBIDDEN:
- Any file outside the allowed list
- Any shell command
- Any web call
- Commit, push, install

OUTPUT: Write the full file content to stdout. Do not call any tools. Do not emit `<tool_call>` blocks. Just print the TypeScript source. Begin with `// AUTO-GENERATED by Phase 179 worker — see .beastmode/worker-runs/<ts>/prompt.md` and end with the final closing brace.

Begin.