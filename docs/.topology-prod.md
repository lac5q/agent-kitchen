# Runtime topology — local-dev profile

Source: `apps/memroos/src/lib/runtime-topology.json` (rendered by `scripts/render-topology-docs.mjs`).

| Service | Required | Display | Health | Port (default) | Supervision |
|---------|----------|---------|--------|----------------|-------------|
| `memroos-app` | ✓ | MemRoOS Next.js app | http /api/health | docker-public-http=3000, local-next-http=3002, launchd-next-http=3002 | docker-compose, launchd, manual-script |
| `mem0-memory` | ✓ | Local mem0 memory service | http /health | mem0-http=3201 | docker-compose, external |
| `orchestration-service` |  | Agent orchestration service | http /health | orchestration-http=3210 | docker-compose, external |
| `connmem` |  | Connected Work Memory runtime (Phase 185 / v8.27) | http /health | connmem-http=3290 | docker-compose, manual-script |
| `voice-server` |  | Pipecat voice server | http /health | pipecat-http=7860, pipecat-health=7861 | manual-script |
| `agentmemory-engine` |  | AgentMemory iii-engine backend | http /health | agentmemory-http=3111 | manual-script |
| `knowledge-mcp` |  | Knowledge MCP service (added in v8.27 / Phase 186) | http /health | knowledge-mcp-http=3291 | docker-compose |
| `healthcheck` |  | Operator healthcheck (systemd timer, oracle-1 or local) | script /usr/local/bin/memroos-healthcheck.py | — | systemd |

Required services MUST be healthy for the operator to admit traffic. Health paths above are the canonical probe; the runtime-topology gate asserts each `required: true` service's health endpoint is configured.
