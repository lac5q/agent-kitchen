---
phase: 109-parallel-domain-audit
plan: 06
type: execute
wave: 3
depends_on: ["109-02", "109-03", "109-04", "109-05"]
files_modified:
  - .planning/audit/FINDINGS-INDEX.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04]
user_setup: []

must_haves:
  truths:
    - "All findings from the four domain reports appear in one filterable index table"
    - "Index is ranked by severity (critical → high → medium → low)"
    - "Every finding routes to the correct downstream fix requirement (SEC-01..06 / ARCH-01..05)"
    - "Aggregate severity counts and per-domain counts are reported"
  artifacts:
    - path: ".planning/audit/FINDINGS-INDEX.md"
      provides: "Consolidated severity-ranked, fix-requirement-routed findings index"
      contains: "| Finding ID | Domain |"
  key_links:
    - from: ".planning/audit/FINDINGS-INDEX.md"
      to: "Phase 110/111/112 planners"
      via: "filter by severity + mapped-fix-requirement to build work queues"
      pattern: "SEC-0[1-6]|ARCH-0[1-5]"
---

<objective>
Merge the four domain findings reports (Auth/Secrets, API Surface, Data/Memory, Architecture) into a single consolidated `.planning/audit/FINDINGS-INDEX.md`, ranked by severity and routed to the correct downstream fix requirement. This is the work queue that phases 110 (SEC-01/02/05), 111 (SEC-03/04/06), and 112 (ARCH-01..05) filter directly.

Purpose: Without one filterable index, each downstream planner must re-read four reports. This plan produces the single source of truth keyed by severity + fix requirement.
Output: `.planning/audit/FINDINGS-INDEX.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-parallel-domain-audit/109-RESEARCH.md
@.planning/audit/domain-auth-secrets.md
@.planning/audit/domain-api-surface.md
@.planning/audit/domain-data-memory.md
@.planning/audit/domain-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate all domain findings into a ranked index</name>
  <read_first>
    - .planning/audit/domain-auth-secrets.md
    - .planning/audit/domain-api-surface.md
    - .planning/audit/domain-data-memory.md
    - .planning/audit/domain-architecture.md
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Findings Index Schema, Fix-requirement routing)
  </read_first>
  <files>.planning/audit/FINDINGS-INDEX.md</files>
  <action>
    Read every finding from the four domain reports. Build `.planning/audit/FINDINGS-INDEX.md` with a single table: `| Finding ID | Domain | Title | Severity | Location | Fix Requirement |`, with rows sorted critical → high → medium → low (and within a severity tier, group by domain). Carry each finding's existing Fix Requirement from its domain report; do NOT re-derive routing — the domain agents already applied type-based routing (CVE→SEC-04, hardcoded secret→SEC-05, CI bypass→SEC-06, arch→ARCH-01..05, else severity→SEC-01/02/03). If any finding lacks a Fix Requirement, flag it inline as `UNROUTED — needs disposition` rather than guessing.

    Verify completeness: the count of index rows must equal the sum of finding rows across the four domain reports. Record a `## Coverage Cross-Check` note stating the per-domain finding count pulled from each report and confirming the total matches the index row count.
  </action>
  <acceptance_criteria>
    - `.planning/audit/FINDINGS-INDEX.md` contains a table with header `| Finding ID | Domain | Title | Severity | Location | Fix Requirement |`.
    - Rows are ordered with all `critical` before `high` before `medium` before `low`.
    - Every row's Fix Requirement matches `SEC-0[1-6]`, `ARCH-0[1-5]`, `none`, or `UNROUTED`.
    - `## Coverage Cross-Check` confirms index row count equals the summed per-domain finding count.
    - No finding ID from any domain report is missing from the index (spot-check each domain's `*01-NNN` prefix appears).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "| Finding ID | Domain |" .planning/audit/FINDINGS-INDEX.md &amp;&amp; grep -q "## Coverage Cross-Check" .planning/audit/FINDINGS-INDEX.md</automated>
  </verify>

  <done>All four domain reports merged into one severity-ranked, fix-routed index with a verified completeness cross-check.</done>
</task>

<task type="auto">
  <name>Task 2: Add aggregate severity + routing summary stats</name>
  <read_first>
    - .planning/audit/FINDINGS-INDEX.md
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (Findings Index Schema)
  </read_first>
  <files>.planning/audit/FINDINGS-INDEX.md</files>
  <action>
    Append two summary blocks to `.planning/audit/FINDINGS-INDEX.md`. `## Severity Summary`: total counts of critical / high / medium / low across all domains, plus a per-domain breakdown (A/B/C/D × severity). `## Fix-Requirement Routing Summary`: count of findings routed to each of SEC-01..06 and ARCH-01..05 (and any `none` / `UNROUTED`), so phase 110/111/112 planners can size their work queues at a glance. Add a one-paragraph `## Headline` at the top calling out the highest-severity findings (any critical, else the top high findings) by Finding ID and Location.
  </action>
  <acceptance_criteria>
    - `grep -q "## Severity Summary" .planning/audit/FINDINGS-INDEX.md` exits 0.
    - `grep -q "## Fix-Requirement Routing Summary" .planning/audit/FINDINGS-INDEX.md` exits 0.
    - Severity Summary reports four total counts plus a per-domain (A/B/C/D) breakdown.
    - Routing Summary lists a count for each SEC-01..06 and ARCH-01..05 bucket.
    - `## Headline` names the top findings by ID + Location.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "## Severity Summary" .planning/audit/FINDINGS-INDEX.md &amp;&amp; grep -q "## Fix-Requirement Routing Summary" .planning/audit/FINDINGS-INDEX.md</automated>
  </verify>

  <done>Index carries aggregate severity counts, per-requirement routing counts, and a headline of top findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain reports → consolidated index | All input is planner-controlled audit output; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-AGG-01 | Repudiation | findings dropped during merge | mitigate | Coverage Cross-Check forces index row count == summed per-domain finding count; mismatch blocks done. |
| T-109-AGG-02 | Tampering | mis-routed fix requirement | mitigate | Routing carried verbatim from domain reports; missing routes flagged UNROUTED, not guessed. |
</threat_model>

<verification>
- `.planning/audit/FINDINGS-INDEX.md` exists with the consolidated table, `## Severity Summary`, `## Fix-Requirement Routing Summary`, and `## Headline`.
- Index row count equals summed per-domain finding count (Coverage Cross-Check).
- Every finding routed (or explicitly UNROUTED).
</verification>

<success_criteria>
All four AUDIT requirements are demonstrably aggregated: a downstream planner can open FINDINGS-INDEX.md, filter by severity or fix requirement, and obtain a complete remediation work queue for phases 110–112.
</success_criteria>

<output>
Create `.planning/phases/109-parallel-domain-audit/109-06-SUMMARY.md` when done.
</output>
