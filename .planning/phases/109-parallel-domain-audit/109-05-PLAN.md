---
phase: 109-parallel-domain-audit
plan: 05
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - .planning/audit/domain-architecture.md
autonomous: true
requirements: [AUDIT-04]
user_setup: []

must_haves:
  truths:
    - "madge circular-dependency scan and knip dead-code scan were run across the TS codebase"
    - "Each `as any` / `: any` / @ts-ignore occurrence is assessed individually (security-path ones flagged higher)"
    - "Error-handling consistency, leaky abstractions, redundant patterns, and execFile/spawn arg-safety each attested"
    - "Each finding has file:line, severity, and a mapped fix requirement (ARCH-01..05)"
  artifacts:
    - path: ".planning/audit/domain-architecture.md"
      provides: "Domain D findings report with coverage attestation"
      contains: "## Coverage Attestation"
  key_links:
    - from: ".planning/audit/domain-architecture.md"
      to: ".planning/audit/FINDINGS-INDEX.md"
      via: "findings rows aggregated in Wave 3"
      pattern: "D01-[0-9]"
---

<objective>
Audit the Architecture & Code Quality domain (AUDIT-04): dead code, circular dependencies, leaky abstractions, redundant patterns, unsafe TypeScript casts, and inconsistent error handling. Run madge + knip (installed in 109-01), plus TS strict / `as any` analysis and Python ruff/vulture, then work the manual architecture checklist.

Purpose: Phase 112 consumes Domain D findings as the ARCH-01..05 work queue. Apply Pitfall 3 — severity reflects exploitability/impact (`as any` in a JWT path is critical; in a UI helper is low), not a blanket template rating.
Output: `.planning/audit/domain-architecture.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/109-parallel-domain-audit/109-RESEARCH.md
@.planning/audit/toolchain-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run madge/knip/ruff + TS unsafe-cast scan (Domain D)</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Domain D Agent, Pitfall 3, Pitfall 5, ### Domain D — Architecture Hotspots)
    - .planning/audit/toolchain-baseline.md (confirms madge/knip/ruff/vulture availability)
    - apps/memroos/tsconfig.json
  </read_first>
  <files>.planning/audit/domain-architecture.md (scratch findings collected here)</files>
  <action>
    Run the Domain D automated tools (installed in 109-01): madge for circular deps `madge --circular --ts-config apps/memroos/tsconfig.json apps/memroos/src/lib/`; knip for dead code `cd apps/memroos && knip --reporter json > /tmp/knip-domain-d.json`; TS strict check `cd apps/memroos && npx tsc --noEmit 2>&1 | tail -20`; unsafe-cast scan `grep -rn "as any\|: any\b\|// @ts-ignore\|// @ts-expect-error" apps/memroos/src/ --include="*.ts" --include="*.tsx" | grep -v "test\|spec\|\.d\.ts"`; Python `ruff check services/` and `vulture services/` (or fallback per toolchain-baseline.md).

    Before trusting GitNexus arch queries, run `gitnexus_detect_changes()` and regenerate if stale (Pitfall 5). Use gitnexus_query for boundary/circular/unused analysis to complement madge/knip. Record candidate findings in `.planning/audit/domain-architecture.md` using schema prefix `D01-NNN`; architecture findings map to ARCH-01..05 by type, not severity.
  </action>
  <acceptance_criteria>
    - madge circular-dep output recorded (cycle list or "none found").
    - `/tmp/knip-domain-d.json` exists and is valid JSON (or fallback ts-prune/manual documented).
    - The `as any` / `@ts-ignore` grep result recorded with a count and per-occurrence file:line list.
    - ruff + vulture results recorded (or fallback noted).
    - GitNexus freshness checked (`gitnexus_detect_changes()` run) before any graph-based finding.
  </acceptance_criteria>
  <done>All Domain D automated tools run, output captured; candidate findings drafted in schema mapped to ARCH-01..05.</done>
</task>

<task type="auto">
  <name>Task 2: Work Domain D manual checklist + write attested report</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Domain D Agent manual checklist, Project Constraints table, ## Findings Report Schema severity rubric)
    - apps/memroos/src/app/api/chat/route.ts
    - apps/memroos/src/app/api/health/route.ts
  </read_first>
  <files>.planning/audit/domain-architecture.md</files>
  <action>
    Work every Domain D manual checklist item: map madge cycles to specific module pairs; flag knip dead exports in security-sensitive paths (auth, policy-gate, content-scanner) higher; assess each `as any` occurrence individually — is any in an auth/validation path? (Pitfall 3 severity calibration); error-handling consistency — do all route handlers return a consistent `{ ok: false, error }` shape? (ARCH-04); redundant patterns — multiple auth-check or DB-connection implementations? (ARCH-03); cross-layer leakage — does UI code import from lib/db directly? (ARCH-02); Python cross-service-boundary imports; execFile/spawn/execFileSync usage (chat/route.ts, health/route.ts, local-agent-runtime.ts, memory-recall-evals.ts, parsers.ts) — verify no user-controlled arguments reach these calls (execSync is banned per STATE.md; execFile is allowed but args must be safe).

    Map each finding to its ARCH requirement: dead code→ARCH-01, circular dep/cross-layer→ARCH-02, redundant pattern→ARCH-03, inconsistent error handling→ARCH-04, unsafe TS cast→ARCH-05. Finalize `.planning/audit/domain-architecture.md`: top severity-ranked findings table, full findings in schema, `## Coverage Attestation` table with every checklist item tagged (no blank rows), `## Summary Stats`. Do not edit source code.
  </action>
  <acceptance_criteria>
    - `grep -q "## Coverage Attestation" .planning/audit/domain-architecture.md` exits 0.
    - Every Domain D checklist item (8 manual items) is a row with one of the four permitted statuses; no blank cells (`grep -cE "CLEAN —|FINDING:|N/A —|NOT CHECKED —" .planning/audit/domain-architecture.md` ≥ 8).
    - Each `as any` finding's severity is justified in Evidence (no blanket rating — Pitfall 3).
    - Each finding maps to an ARCH-01..05 requirement.
    - `## Summary Stats` present; every finding has file:line.
  </acceptance_criteria>
  <done>Domain D report complete: tool + manual findings + full attestation + summary stats; each mapped to ARCH-01..05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → execFile/spawn arguments | Command-arg injection surface if args are user-controlled |
| UI layer → lib/db | Cross-layer leakage if UI imports DB directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-D-01 | Tampering | execFile/spawn/execFileSync arg construction | mitigate | Verify no user-controlled args reach these calls; finding if reachable. |
| T-109-D-02 | Spoofing/Tampering | `as any` in auth/validation code paths | mitigate | Per-occurrence assessment; security-path casts rated high/critical (Pitfall 3). |
| T-109-D-03 | Tampering | dead exports in security modules (policy-gate, content-scanner) | accept/mitigate | knip flags; dead security code → ARCH-01 finding, prioritized. |
| T-109-D-SC | Tampering | madge/knip/ruff/vulture invocation | accept | Read-only scans; tools verified in 109-01. |
</threat_model>

<verification>
- `.planning/audit/domain-architecture.md` exists with `## Coverage Attestation`, ranked findings, `## Summary Stats`.
- All 8 Domain D checklist items attested; no blank rows.
- madge + knip run; each `as any` individually assessed; every finding mapped to ARCH-01..05.
</verification>

<success_criteria>
AUDIT-04 satisfied: an engineering reviewer can confirm dead code / circular deps / leaky abstractions / redundant patterns / unsafe casts / error-handling consistency were all audited, with context-calibrated severity and ARCH-mapped routing.
</success_criteria>

<output>
Create `.planning/phases/109-parallel-domain-audit/109-05-SUMMARY.md` when done.
</output>
