---
phase: 109-parallel-domain-audit
plan: 04
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - .planning/audit/domain-data-memory.md
autonomous: true
requirements: [AUDIT-03]
user_setup: []

must_haves:
  truths:
    - "Python service trust boundaries (memory, orchestration, knowledge-mcp, voice-server) documented before assigning severity"
    - "pip-audit + bandit run across all Python services; unsafe deserialization (pickle/yaml.load) scanned"
    - "Memory access controls (mem0/Qdrant/SQLite), data leakage paths, and PII-in-logs each attested"
    - "Each finding has file:line, severity, and a mapped fix requirement"
  artifacts:
    - path: ".planning/audit/domain-data-memory.md"
      provides: "Domain C findings report with coverage attestation"
      contains: "## Coverage Attestation"
  key_links:
    - from: ".planning/audit/domain-data-memory.md"
      to: ".planning/audit/FINDINGS-INDEX.md"
      via: "findings rows aggregated in Wave 3"
      pattern: "C01-[0-9]"
---

<objective>
Audit the Data & Memory domain (AUDIT-03): unsafe deserialization, data leakage paths, privacy exposure in logs, unsafe file operations, and memory service access controls across the four Python services. The critical methodological rule (RESEARCH.md Pitfall 2): do NOT flag "missing auth" on FastAPI services without first documenting the intended trust boundary (loopback / Docker-internal / public-reachable).

Purpose: Phases 110–111 consume Domain C findings (SEC-01/02/03/04). Trust-boundary documentation prevents false-high severities on intentionally loopback-only services.
Output: `.planning/audit/domain-data-memory.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/109-parallel-domain-audit/109-RESEARCH.md
@.planning/audit/toolchain-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pip-audit/bandit + document Python service trust boundaries</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Domain C Agent, Pitfall 2, Open Question 2, ### Domain C — Data/Memory Hotspots)
    - .planning/audit/toolchain-baseline.md (confirms pip-audit/bandit availability + CVE baseline)
  </read_first>
  <files>.planning/audit/domain-data-memory.md (scratch findings collected here)</files>
  <action>
    Run the Domain C automated tools (installed in 109-01): `pip-audit -r services/knowledge-mcp/requirements.txt` and the same for services/memory, services/orchestration, services/voice-server (reuse toolchain-baseline.md results if already captured). Run `bandit -r services/ --format json -o /tmp/bandit-domain-c.json`. Scan for unsafe deserialization: `grep -rn "pickle\|yaml\.load\|marshal\|shelve" services/ --include="*.py"` — any `yaml.load()` without `Loader=yaml.SafeLoader` is an RCE-class finding.

    Document the trust boundary for each Python service BEFORE assigning severity (Pitfall 2 / Open Question 2): inspect docker-compose.yml port bindings and any LaunchAgent service configs to determine if memory/orchestration FastAPI servers are loopback-only, Docker-internal, or public-reachable. Record candidate findings in `.planning/audit/domain-data-memory.md` using schema prefix `C01-NNN`. Triage against Prior Security Baseline (Phases 74–78 memory-security fixes — do not re-file).
  </action>
  <acceptance_criteria>
    - pip-audit results recorded for all four service requirements files (count or `N/A — file not found`).
    - `/tmp/bandit-domain-c.json` exists and is valid JSON (or fallback to semgrep Python rules documented if bandit unavailable per toolchain-baseline.md).
    - The pickle/yaml grep result is recorded; any unsafe `yaml.load` is filed as a finding.
    - A `## Trust Boundaries` table lists each of the four services with boundary = loopback | docker-internal | public-reachable | unknown, with evidence (port binding / config file).
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json;json.load(open('/tmp/bandit-domain-c.json'))" &amp;&amp; grep -q "## Trust Boundaries" .planning/audit/domain-data-memory.md</automated>
  </verify>

  <done>Python CVE + SAST scans run, deserialization scanned, per-service trust boundaries documented with evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Work Domain C manual checklist + write attested report</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Domain C Agent manual checklist, Project Constraints table)
    - services/knowledge-mcp/ (bearer token auth path)
    - services/voice-server/meeting_bot.py
    - apps/memroos/src/lib (policy-gate.ts — memory authorization)
  </read_first>
  <files>.planning/audit/domain-data-memory.md</files>
  <action>
    Work every Domain C manual checklist item: knowledge-mcp bearer token (constant-time `secrets.compare_digest`, non-empty-string check, env required at startup); memory + orchestration FastAPI service-level auth vs documented trust boundary; voice-server meeting tokens never logged (no room_url / join-token logging paths); unsafe YAML across services; data leakage — memory API responses redact/filter per policy-gate labels; user-supplied file paths in file operations (path traversal); SQLite DB paths are hardcoded constants not user-supplied; ChromaDB/qdrant-client CVEs at installed versions; mem0ai 0.1.x vs 2.0.2 version-lag security implications.

    Also verify the STATE.md/CLAUDE.md data constraints hold: no direct Qdrant writes from TS code (mem0 writes only via POST http://localhost:3201/memory/add), no local Qdrant in docker compose. Finalize `.planning/audit/domain-data-memory.md`: top severity-ranked findings table, full findings in schema, `## Coverage Attestation` table with every checklist item tagged (no blank rows), `## Summary Stats`. Do not edit source code.
  </action>
  <acceptance_criteria>
    - `grep -q "## Coverage Attestation" .planning/audit/domain-data-memory.md` exits 0.
    - The `## Coverage Attestation` table contains a distinct row for each of the 10 Domain C manual checklist items (knowledge-mcp bearer token, memory/orchestration service auth, voice-server token logging, unsafe YAML, data-leakage redaction, file-path traversal, SQLite path constants, ChromaDB/qdrant CVEs, mem0ai version lag, no-direct-Qdrant-write constraint). Every such row's status cell is non-empty and one of CLEAN/FINDING/N-A/NOT-CHECKED. Legend/prose lines do not count toward coverage — the check is against table body rows.
    - Each "missing service auth" finding cites the documented trust boundary in its Evidence/severity justification (no severity assigned without boundary).
    - `## Summary Stats` present; every finding has file:line + fix-requirement route.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "## Coverage Attestation" .planning/audit/domain-data-memory.md &amp;&amp; grep -q "## Summary Stats" .planning/audit/domain-data-memory.md</automated>
  </verify>

  <done>Domain C report complete: trust-boundary-grounded findings + full attestation + summary stats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS code → Python memory services | mem0 via POST localhost:3201 only; FastAPI services may rely on network isolation |
| service → external data stores | Qdrant Cloud, SQLite, ChromaDB — access-control + path-construction surface |
| agent output → logs | PII / token leakage risk in voice-server, orchestration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-C-01 | Tampering/RCE | yaml.load / pickle in Python services | mitigate | grep + bandit; any unsafe deserialization → finding routed by severity. |
| T-109-C-02 | Information Disclosure | FastAPI services with no service-level auth | mitigate | Document trust boundary first (Pitfall 2); severity reflects actual exposure. |
| T-109-C-03 | Information Disclosure | token/PII logging (voice-server, orchestration) | mitigate | Verify no room_url/join-token logging paths. |
| T-109-C-04 | Elevation of Privilege | dependency CVEs (mem0ai 0.x, chromadb) | mitigate | pip-audit; CVE findings → SEC-04 regardless of severity label. |
| T-109-C-SC | Tampering | pip-audit/bandit invocation | accept | Read-only scans; tools verified in 109-01. |
</threat_model>

<verification>
- `.planning/audit/domain-data-memory.md` exists with `## Coverage Attestation`, `## Trust Boundaries`, ranked findings, `## Summary Stats`.
- All 10 Domain C checklist items attested; no blank rows.
- pip-audit + bandit run (or documented fallback); no missing-auth severity assigned without a boundary.
</verification>

<success_criteria>
AUDIT-03 satisfied: a reviewer can confirm data/memory handling was audited with documented trust boundaries, and each deserialization/leakage/access-control sub-area is attested with file:line precision.
</success_criteria>

<output>
Create `.planning/phases/109-parallel-domain-audit/109-04-SUMMARY.md` when done.
</output>
