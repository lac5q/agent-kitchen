---
phase: 109-parallel-domain-audit
plan: 03
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - .planning/audit/domain-api-surface.md
autonomous: true
requirements: [AUDIT-02]
user_setup: []

must_haves:
  truths:
    - "Proxy matcher coverage was analyzed (not inferred from per-route grep) — proxy.ts config.matcher + ROUTE_LOCAL_AUTH_API_ROUTES audited for bypass gaps"
    - "Injection vectors (SQLi via better-sqlite3, XSS/CSP, SSTI), input validation gaps, rate limiting, and CORS each attested"
    - "Next.js v16.2.4 middleware-bypass CVEs assessed against this app's proxy auth pattern"
    - "Each finding has file:line, severity, and a mapped fix requirement"
  artifacts:
    - path: ".planning/audit/domain-api-surface.md"
      provides: "Domain B findings report with coverage attestation"
      contains: "## Coverage Attestation"
  key_links:
    - from: ".planning/audit/domain-api-surface.md"
      to: ".planning/audit/FINDINGS-INDEX.md"
      via: "findings rows aggregated in Wave 3"
      pattern: "B01-[0-9]"
---

<objective>
Audit the API Surface domain (AUDIT-02): missing auth guards, input validation gaps, injection risks (SQLi/XSS/SSTI), rate limiting, CORS, and Next.js CVE exploitability. The central methodological rule (RESEARCH.md Pitfall 1): auth is enforced in `proxy.ts` middleware, not per-route — coverage must be derived from proxy matcher + bypass-list analysis, NOT from grepping route files.

Purpose: Phases 110–111 consume Domain B findings (SEC-01/02/03/04). A wrong "90 unauthenticated routes" finding from naive grep is invalid — this plan forces real matcher analysis.
Output: `.planning/audit/domain-api-surface.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/109-parallel-domain-audit/109-RESEARCH.md
@.planning/audit/toolchain-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze proxy matcher coverage + run injection scans (Domain B)</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Domain B Agent, Pitfall 1, ### Domain B — API Surface Hotspots)
    - apps/memroos/src/proxy.ts
  </read_first>
  <files>.planning/audit/domain-api-surface.md (scratch findings collected here)</files>
  <action>
    First, the matcher analysis (the load-bearing step): read proxy.ts and document the exact `config.matcher` pattern, the `withSecurityHeaders` CSP, and every entry of `ROUTE_LOCAL_AUTH_API_ROUTES` (and OPERATOR_ROUTES / ADMIN_ROUTES if present). For each bypass-list entry, confirm the route actually implements route-local auth (open the route file) — flag any orphan bypass (listed but no local auth) as a finding. Enumerate API routes: `find apps/memroos/src/app/api -name "route.ts" | wc -l`. Do NOT conclude "N unauthenticated routes" from grep alone — exposure is determined by matcher + bypass list.

    Then run semgrep injection/security rulesets: `semgrep --config=p/owasp-top-ten --config=p/nodejs --config=p/nextjs apps/memroos/src/app/api/ --json -o /tmp/semgrep-domain-b.json`. Triage against the Prior Security Baseline (do not re-file Phase 68/69 closed fixes). Record candidate findings in `.planning/audit/domain-api-surface.md` using the Findings Report Schema with prefix `B01-NNN`.
  </action>
  <acceptance_criteria>
    - The report records the literal `config.matcher` string and a per-entry table of `ROUTE_LOCAL_AUTH_API_ROUTES` with a `local-auth: yes/no` column.
    - `/tmp/semgrep-domain-b.json` exists and is valid JSON.
    - The route count from `find ... -name route.ts | wc -l` is recorded.
    - Any "unauthenticated route" claim is backed by matcher/bypass analysis, not grep count alone.
  </acceptance_criteria>
  <done>Proxy matcher + bypass list fully analyzed; injection scan run and triaged; candidate findings drafted in schema.</done>
</task>

<task type="auto">
  <name>Task 2: Work Domain B manual checklist + write attested report</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Domain B Agent manual checklist, Open Questions 1 + 3, ## Security Domain threat patterns)
    - apps/memroos/src/proxy.ts
    - apps/memroos/src/app/api/time-series/route.ts
  </read_first>
  <files>.planning/audit/domain-api-surface.md</files>
  <action>
    Work every Domain B manual checklist item: proxy matcher blind spots; ROUTE_LOCAL_AUTH_API_ROUTES orphan bypasses; SQL injection — review all `db.prepare()` calls for string interpolation of user input (specifically verify time-series/route.ts "safe per T-25-02" SQL claim); CSP `unsafe-eval` in script-src — check Next.js v16 docs in node_modules/next/dist/docs/ to determine if RSC requires it before assigning severity (Open Question 1); rate limiting — list unprotected endpoints and risk-rank them; CORS — confirm no route sends `Access-Control-Allow-Origin: *`; Next.js v16.2.4 CVEs — assess which middleware/proxy-bypass CVEs are exploitable given this proxy.ts auth pattern (route as SEC-04, type=dependency-CVE, regardless of severity label — Open Question 3); input validation — identify routes accepting untrusted body/query without validation (no zod framework present); Iris/content-scanner ingress coverage beyond dispatch.

    Finalize `.planning/audit/domain-api-surface.md`: top severity-ranked findings table, full findings in schema, a `## Coverage Attestation` table with every checklist item tagged CLEAN/FINDING/N-A/NOT-CHECKED (no blank rows), and `## Summary Stats` with severity counts. Next.js CVE findings map to SEC-04 by type. Do not edit source code.
  </action>
  <acceptance_criteria>
    - `grep -q "## Coverage Attestation" .planning/audit/domain-api-surface.md` exits 0.
    - Every Domain B checklist item (9 manual items) is a row with one of the four permitted statuses; no blank cells (`grep -cE "CLEAN —|FINDING:|N/A —|NOT CHECKED —" .planning/audit/domain-api-surface.md` ≥ 9).
    - The Next.js v16.2.4 CVE assessment row exists and maps to SEC-04.
    - The time-series SQL "safe per T-25-02" claim is explicitly verified or refuted.
    - `## Summary Stats` present; every finding has file:line + fix-requirement route.
  </acceptance_criteria>
  <done>Domain B report complete: matcher-grounded findings + full attestation + summary stats; CVE + CSP + SQL hotspots resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Next.js middleware (proxy.ts) | Auth/RBAC + CSP enforced centrally; single point of failure |
| agent → route-local-auth endpoints | ROUTE_LOCAL_AUTH_API_ROUTES bypass proxy JWT, rely on local auth |
| untrusted body/query → route handlers | No zod framework; ad-hoc validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-B-01 | Elevation of Privilege | proxy.ts matcher / Next.js v16.2.4 CVEs | mitigate | Analyze matcher gaps + assess middleware-bypass CVEs; route CVE findings → SEC-04. |
| T-109-B-02 | Tampering | better-sqlite3 db.prepare() queries | mitigate | Review for string interpolation; verify time-series SQL claim. |
| T-109-B-03 | XSS escalation | CSP unsafe-eval in script-src | mitigate | Check Next.js v16 docs for RSC requirement before severity; finding if removable. |
| T-109-B-04 | Denial of Service | unrate-limited endpoints | accept/mitigate | Inventory unprotected endpoints; risk-rank, file medium findings. |
| T-109-B-SC | Tampering | semgrep invocation | accept | Read-only scan; tool verified in 109-01. |
</threat_model>

<verification>
- `.planning/audit/domain-api-surface.md` exists with `## Coverage Attestation`, ranked findings, `## Summary Stats`.
- Proxy matcher + every bypass-list entry analyzed and tabulated.
- Next.js CVE exploitability assessed and routed to SEC-04.
</verification>

<success_criteria>
AUDIT-02 satisfied: a reviewer can confirm API exposure was determined by proxy-matcher analysis (not grep), and every injection/validation/rate-limit/CORS/CVE sub-area is attested with file:line precision.
</success_criteria>

<output>
Create `.planning/phases/109-parallel-domain-audit/109-03-SUMMARY.md` when done.
</output>
