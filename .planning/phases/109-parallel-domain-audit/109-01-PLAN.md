---
phase: 109-parallel-domain-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/toolchain-baseline.md
autonomous: false
requirements: [AUDIT-03, AUDIT-04]
user_setup: []

must_haves:
  truths:
    - "pip-audit, bandit, madge, knip are installed and runnable"
    - "semgrep v1.132.0 confirmed runnable"
    - "npm audit and pip-audit baseline output captured to a file"
    - ".planning/audit/ directory exists for domain agents to write into"
  artifacts:
    - path: ".planning/audit/toolchain-baseline.md"
      provides: "Tool version manifest + npm/pip audit baseline output"
      contains: "## Tool Versions"
  key_links:
    - from: ".planning/audit/toolchain-baseline.md"
      to: "Wave 2 domain audit plans (109-03, 109-04, 109-05)"
      via: "installed tools consumed by domain agents"
      pattern: "pip-audit|bandit|madge|knip"
---

<objective>
Bootstrap the audit toolchain so the four parallel domain agents in Wave 2 can run their prescribed automated tools. Install the missing tools (pip-audit, bandit, madge, knip — all flagged [ASSUMED] in RESEARCH.md, so installs are gated behind a human-verify checkpoint), confirm semgrep works, capture an npm/pip CVE baseline, and create the shared output directory.

Purpose: Without pip-audit + bandit, Domain C (Data/Memory) is blind to Python CVEs; without madge + knip, Domain D (Architecture) cannot detect circular deps or dead code across 647 TS files. This plan removes those blockers.
Output: `.planning/audit/toolchain-baseline.md` (tool versions + baseline CVE output) and a ready `.planning/audit/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-parallel-domain-audit/109-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm audit tool installs (package legitimacy gate)</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (## Package Legitimacy Audit, ## Standard Stack)
  </read_first>
  <action>Pause for operator approval of the audit tool package list before any install runs. This is a package-legitimacy gate (T-109-SC): RESEARCH.md slopcheck was unavailable, so all seven audit tools are tagged [ASSUMED]. Present the package list and wait for "approved" or an exclusion list before proceeding to Task 2. This task installs nothing and changes no source.</action>
  <what-built>Nothing yet — this checkpoint gates the install step. RESEARCH.md slopcheck was unavailable, so all seven audit tools (semgrep, pip-audit, bandit, ruff, gitleaks, madge, knip) are tagged [ASSUMED]. No code change occurs in this task.</what-built>
  <how-to-verify>
    These are the packages to be installed in Task 2. Each is a well-known, long-standing security/quality tool. Verify legitimacy if desired:
    1. pip-audit — PyPI, maintained by PyPA / Trail of Bits: https://pypi.org/project/pip-audit/
    2. bandit — PyPI, PyCQA organization: https://pypi.org/project/bandit/
    3. ruff — PyPI, Astral: https://pypi.org/project/ruff/
    4. vulture — PyPI, dead-code detection: https://pypi.org/project/vulture/
    5. madge — npm, long-standing TS/JS circular-dep tool: https://www.npmjs.com/package/madge
    6. knip — npm, modern TS dead-code tool: https://www.npmjs.com/package/knip
    No new production dependencies are added — these are dev/audit tools only.
  </how-to-verify>
  <acceptance_criteria>
    - Operator has reviewed the package list and approved the install, OR named any package to exclude.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install all, or name packages to exclude.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install audit toolchain and capture version manifest</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Wave 0 Gaps, ## Environment Availability)
  </read_first>
  <files>.planning/audit/toolchain-baseline.md</files>
  <action>
    Install the Python audit tools. Python 3.14 on Homebrew requires the --break-system-packages flag (or a venv): run `pip install pip-audit bandit ruff vulture --break-system-packages` and if that fails, retry without the flag. Install the Node tools globally: `npm install -g madge knip`. Then confirm semgrep is already present (RESEARCH.md verified v1.132.0 at /opt/homebrew) via `semgrep --version`.

    Write `.planning/audit/toolchain-baseline.md` with a `## Tool Versions` section listing the resolved version of each of: semgrep, pip-audit, bandit, ruff, vulture, madge, knip, npm (node), python3. For any tool that fails to install, record it as `UNAVAILABLE — {error}` and note the documented fallback from RESEARCH.md ## Environment Availability (e.g. bandit → semgrep Python rules; vulture → manual review). Do NOT edit any source code — this plan only installs tools and writes the baseline file.
  </action>
  <acceptance_criteria>
    - `command -v pip-audit && command -v bandit && command -v madge && command -v knip` each resolve to a path (or the file records the specific install error + fallback for any that did not).
    - `semgrep --version` prints a version string.
    - `.planning/audit/toolchain-baseline.md` exists and contains a `## Tool Versions` heading with one row per tool.
    - `grep -q "## Tool Versions" .planning/audit/toolchain-baseline.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>command -v semgrep &amp;&amp; grep -q "## Tool Versions" .planning/audit/toolchain-baseline.md</automated>
  </verify>

  <done>All four missing tools installed (or fallback documented), semgrep confirmed, version manifest written.</done>
</task>

<task type="auto">
  <name>Task 3: Capture npm + pip dependency CVE baseline</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Automated Tools, Domain C automated commands)
  </read_first>
  <files>.planning/audit/toolchain-baseline.md</files>
  <action>
    Append a `## Dependency CVE Baseline` section to `.planning/audit/toolchain-baseline.md`. Run `cd apps/memroos && npm audit --json` and capture the summary counts (critical/high/moderate/low) plus the named advisories (RESEARCH.md expects 1 high on next, 3 moderate). Then run pip-audit against each Python service requirements file that exists: `pip-audit -r services/knowledge-mcp/requirements.txt`, `services/memory/requirements.txt`, `services/orchestration/requirements.txt`, `services/voice-server/requirements.txt`. Record each service's CVE count and any flagged packages. If a requirements file is absent, record `N/A — file not found`. This is a baseline snapshot consumed by Domain B (npm CVEs → SEC-04) and Domain C (pip CVEs → SEC-04); do not attempt to fix anything here.
  </action>
  <acceptance_criteria>
    - `.planning/audit/toolchain-baseline.md` contains a `## Dependency CVE Baseline` heading.
    - The npm audit summary (counts + at least the `next` advisory) is recorded.
    - Each of the four Python service requirements files has a pip-audit result row (count or `N/A — file not found`).
    - `grep -q "## Dependency CVE Baseline" .planning/audit/toolchain-baseline.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "## Dependency CVE Baseline" .planning/audit/toolchain-baseline.md</automated>
  </verify>

  <done>Baseline CVE snapshot captured for both ecosystems; domain agents can reference it instead of re-running from scratch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → package registries (PyPI, npm) | Audit tools are downloaded from public registries during install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-SC | Tampering | pip/npm audit tool installs | mitigate | All seven tools [ASSUMED] (slopcheck unavailable); blocking-human checkpoint (Task 1) before install verifies each against pypi.org/npmjs.com. Never auto-approvable. |
| T-109-01 | Tampering | toolchain-baseline.md output | accept | File is planner-controlled audit metadata; no untrusted input written into it. |
</threat_model>

<verification>
- `.planning/audit/` directory exists.
- `.planning/audit/toolchain-baseline.md` has both `## Tool Versions` and `## Dependency CVE Baseline` sections.
- pip-audit, bandit, madge, knip resolve on PATH (or fallback documented).
</verification>

<success_criteria>
Wave 2 domain agents (109-03, 109-04, 109-05) can invoke pip-audit, bandit, madge, knip, and semgrep without install failures, and can reference a captured CVE baseline.
</success_criteria>

<output>
Create `.planning/phases/109-parallel-domain-audit/109-01-SUMMARY.md` when done.
</output>
