---
phase: 109-parallel-domain-audit
plan: 02
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - .planning/audit/domain-auth-secrets.md
autonomous: true
requirements: [AUDIT-01]
user_setup: []

must_haves:
  truths:
    - "Security team can confirm every Domain A checklist item was checked (CLEAN / FINDING / N/A / NOT CHECKED tagged)"
    - "Hardcoded-secret scan (semgrep + git ls-files) was run and results recorded"
    - "JWT mechanics (alg, expiry, secret strength) and cookie flags (HttpOnly/Secure/SameSite) are explicitly attested"
    - "Each finding has file:line, severity, and a mapped fix requirement (SEC-01..06)"
  artifacts:
    - path: ".planning/audit/domain-auth-secrets.md"
      provides: "Domain A findings report with coverage attestation"
      contains: "## Coverage Attestation"
  key_links:
    - from: ".planning/audit/domain-auth-secrets.md"
      to: ".planning/audit/FINDINGS-INDEX.md"
      via: "findings rows aggregated in Wave 3"
      pattern: "A01-[0-9]"
---

<objective>
Audit the Auth & Secrets domain (AUDIT-01): hardcoded secrets, token handling, JWT security, API key exposure, session management, cookie flags. Run the prescribed automated scans, work the Domain A manual checklist, and write a severity-ranked findings report with a complete Coverage Attestation table.

Purpose: Phases 110–111 consume Domain A findings as a remediation work queue (SEC-01/02/03/05). Coverage attestation removes the "checked-clean vs never-checked" ambiguity.
Output: `.planning/audit/domain-auth-secrets.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/109-parallel-domain-audit/109-RESEARCH.md
@.planning/audit/toolchain-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated secret + JWT scans (Domain A)</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Domain A Agent, ### Domain A — Auth/Secrets Hotspots, ## Findings Report Schema)
    - apps/memroos/src/app/api/auth/login/route.ts
    - apps/memroos/src/app/api/auth/refresh/route.ts
    - apps/memroos/src/lib/auth/jwt.ts
    - apps/memroos/src/lib/operator-auth.ts
  </read_first>
  <files>.planning/audit/domain-auth-secrets.md (scratch findings collected here)</files>
  <action>
    Run the Domain A automated commands and collect raw output. Run semgrep with secrets and jwt rulesets: `semgrep --config=p/secrets --config=p/jwt apps/memroos/src/ services/ --json -o /tmp/semgrep-domain-a.json`. If gitleaks installed (per toolchain-baseline.md), run `gitleaks detect --source=. --no-banner --report-path=/tmp/gitleaks-domain-a.json`; if not installed, record reliance on TruffleHog CI (secret-guard.yml) as the git-history fallback per RESEARCH.md. Check for committed env files: `git ls-files | grep -E "\.env[^.]"`.

    Triage each automated hit against the Prior Security Baseline (RESEARCH.md "Do Not Re-Litigate" — Phase 68/69/74-78 fixes); do NOT re-file closed findings. Begin building `.planning/audit/domain-auth-secrets.md` by recording raw tool output references and candidate findings using the exact Findings Report Schema (Finding ID prefix `A01-NNN`, Domain A, severity per the rubric, Location file:line, Evidence, Impact, Remediation, Mapped Fix Requirement, Confidence).
  </action>
  <acceptance_criteria>
    - `/tmp/semgrep-domain-a.json` exists and is valid JSON (`python3 -c "import json;json.load(open('/tmp/semgrep-domain-a.json'))"` exits 0).
    - The `git ls-files | grep -E "\.env[^.]"` result is recorded (empty or listed) in the report.
    - Gitleaks output captured OR TruffleHog-CI fallback explicitly noted.
    - Each automated finding uses the `A01-NNN` schema with a Mapped Fix Requirement.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json;json.load(open('/tmp/semgrep-domain-a.json'))"</automated>
  </verify>

  <done>Automated Domain A scans run, output captured, candidate findings drafted in schema; no closed-baseline findings re-filed.</done>
</task>

<task type="auto">
  <name>Task 2: Work Domain A manual checklist + write attested report</name>
  <read_first>
    - .planning/phases/109-parallel-domain-audit/109-RESEARCH.md (### Domain A Agent manual checklist, ## Validation Architecture coverage-attestation format)
    - apps/memroos/src/lib/auth/jwt.ts
    - apps/memroos/src/lib/operator-auth.ts
    - apps/memroos/src/app/api/auth/login/route.ts
    - apps/memroos/src/app/api/auth/refresh/route.ts
  </read_first>
  <files>.planning/audit/domain-auth-secrets.md</files>
  <action>
    Work every Domain A manual checklist item from RESEARCH.md: MEMROOS_JWT_SECRET entropy (≥256-bit, not in source); JWT alg (HS256 min / RS256 preferred) verified in lib/auth/jwt.ts; access + refresh token TTLs; cookie flags HttpOnly/SameSite=Lax-intentional/Secure-isProd-logic (login/route.ts:85, refresh/route.ts:82); refresh-token revocation (hashed-before-storage, revocation path); `authorizeRegistryWrite` loopback bypass not reachable via spoofed headers (note x-forwarded-host fix already applied); per-agent API key strength/storage/rotation; complement TruffleHog with manual hardcoded-key scan; `.env.example` covers all secrets with no real values.

    For severity, apply the rubric and Pitfall 3 (an `as any` in a UI helper is low; a JWT-path issue is critical — justify in Evidence). Finalize `.planning/audit/domain-auth-secrets.md` with: a top severity-ranked findings table, full findings in schema below it, a `## Coverage Attestation` table where EVERY checklist item has a status of `CLEAN — verified by {tool/file}` | `FINDING: {ID}` | `N/A — {reason}` | `NOT CHECKED — {blocker}` (no blank rows), and a `## Summary Stats` block with critical/high/medium/low counts. Do not edit source code.
  </action>
  <acceptance_criteria>
    - `grep -q "## Coverage Attestation" .planning/audit/domain-auth-secrets.md` exits 0.
    - The `## Coverage Attestation` table contains a distinct row for each of the 9 Domain A manual checklist items (JWT secret entropy, JWT algorithm, token TTLs, cookie flags, refresh-token revocation, authorizeRegistryWrite loopback, per-agent API keys, hardcoded-key scan, .env.example). Every such row's status cell is non-empty and is one of CLEAN/FINDING/N-A/NOT-CHECKED. No legend/prose line is counted as coverage — the check is against the table body rows.
    - `## Summary Stats` block present with four severity counts.
    - Every finding row references a file:line and a mapped fix requirement in `SEC-01|SEC-02|SEC-03|SEC-05|SEC-06|none`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "## Coverage Attestation" .planning/audit/domain-auth-secrets.md &amp;&amp; grep -q "## Summary Stats" .planning/audit/domain-auth-secrets.md</automated>
  </verify>

  <done>Domain A report complete: ranked findings + full attestation table covering all checklist items + summary stats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Next.js proxy/middleware | JWT cookie + role checks enforced here (proxy.ts) |
| agent → API key auth | x-memroos-operator-key / Bearer validated in operator-auth.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-A-01 | Spoofing | JWT verification (lib/auth/jwt.ts) | mitigate | Audit confirms alg is pinned (no alg=none/confusion); finding filed if not. |
| T-109-A-02 | Information Disclosure | Hardcoded secrets in source/git | mitigate | semgrep p/secrets + git ls-files + TruffleHog-CI; findings → SEC-05. |
| T-109-A-03 | Spoofing | authorizeRegistryWrite loopback bypass | mitigate | Verify spoofed x-forwarded-host cannot reach loopback path (fix already applied; confirm no regression). |
| T-109-A-SC | Tampering | semgrep/gitleaks invocation | accept | Tools installed/verified in 109-01; read-only scans, no writes to source. |
</threat_model>

<verification>
- `.planning/audit/domain-auth-secrets.md` exists with `## Coverage Attestation`, ranked findings table, and `## Summary Stats`.
- All 9 Domain A manual checklist items attested; no blank rows.
- semgrep JSON produced and triaged against the closed-baseline list.
</verification>

<success_criteria>
AUDIT-01 satisfied: a security reviewer can read `domain-auth-secrets.md` and confirm every auth/secrets sub-area was checked, with each open issue carrying file:line, severity, and a fix-requirement route.
</success_criteria>

<output>
Create `.planning/phases/109-parallel-domain-audit/109-02-SUMMARY.md` when done.
</output>
