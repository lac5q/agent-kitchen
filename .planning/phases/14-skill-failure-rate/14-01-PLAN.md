---
phase: 14-skill-failure-rate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - ~/github/knowledge/scripts/skill-sync.py
  - ~/github/knowledge/scripts/failures_parser.py
  - ~/github/knowledge/scripts/tests/test_skill_sync.py
  - ~/github/knowledge/scripts/tests/test_failures_parser.py
autonomous: true
requirements:
  - SKILL-06
must_haves:
  truths:
    - "When skill-sync.py encounters a skill operation error, a new JSONL entry with event=failed appears in skill-contributions.jsonl"
    - "The failures_parser module produces aggregate counts grouped by agent_id and error_type from failures.log"
    - "Multi-line JSON objects spanning several lines in failures.log are parsed as single entries (not truncated to first line)"
    - "Entries with error_type=='disk_critical' are excluded from all aggregate counts"
    - "Parser returns empty aggregates ({}) when failures.log is missing or empty — never raises"
  artifacts:
    - path: "~/github/knowledge/scripts/failures_parser.py"
      provides: "Stateful multi-line JSON parser for failures.log"
      exports: ["parse_failures_log", "aggregate_failures"]
    - path: "~/github/knowledge/scripts/skill-sync.py"
      provides: "failed event emission in skill-contributions.jsonl"
      contains: "event.*failed"
    - path: "~/github/knowledge/scripts/tests/test_failures_parser.py"
      provides: "Pytest coverage for multi-line parsing, disk_critical filter, empty file handling"
    - path: "~/github/knowledge/scripts/tests/test_skill_sync.py"
      provides: "Pytest coverage for failed event emission on skill operation errors"
  key_links:
    - from: "skill-sync.py error handler"
      to: "skill-contributions.jsonl"
      via: "JSONL append with event=failed, agent_id, error_type, timestamp"
      pattern: "event.*failed"
    - from: "failures_parser.py"
      to: "failures.log"
      via: "Stateful line-buffered JSON object reader"
      pattern: "parse_failures_log"
---

<objective>
Instrument `skill-sync.py` to emit `failed` events to `skill-contributions.jsonl` when skill operations error, and create a stateful parser (`failures_parser.py`) that correctly reads multi-line JSON tracebacks from `failures.log`, filters `disk_critical` noise, and produces aggregate counts by `agent_id` and `error_type`.

Purpose: Establishes the data path for SKILL-06 (failure rate visibility). Honors the STATE.md decision: "two-commit sequence — stateful parser first, then API + UI; naive line-by-line parser breaks on multi-line tracebacks." This plan MUST complete and land data before Plan 14-02 wires it to the API.

Output:
- New `failures_parser.py` module with `parse_failures_log()` and `aggregate_failures()` exported.
- `skill-sync.py` modified to emit `{event: "failed", agent_id, error_type, timestamp}` JSONL entries on skill operation errors.
- Pytest coverage proving multi-line parsing, disk_critical exclusion, and graceful empty-file handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Understand existing skill-sync.py behavior and JSONL schema before modifying
@~/github/knowledge/scripts/skill-sync.py
@~/github/knowledge/scripts/tests/test_skill_sync.py

<interfaces>
<!-- skill-contributions.jsonl existing entry shape (from Phase 9). New 'failed' event must follow this pattern. -->

Existing JSONL entry shape:
```json
{"timestamp":"2026-04-12T10:15:22Z","agent_id":"hermes","event":"synced","skill":"<name>","path":"<path>"}
```

New 'failed' event shape (this plan adds):
```json
{"timestamp":"2026-04-13T19:45:10Z","agent_id":"hermes","event":"failed","error_type":"<category>","skill":"<name-if-known-else-null>"}
```

failures.log entry shape (multi-line JSON objects):
```
{
  "timestamp": "2026-04-13T...",
  "agent_id": "hermes",
  "error_type": "permission_denied",
  "traceback": "Traceback (most recent call last):\n  File ...\n  ...\nOSError: ..."
}
{
  "timestamp": "2026-04-13T...",
  "agent_id": "gwen",
  "error_type": "disk_critical",   <-- MUST be excluded from aggregates
  "traceback": "..."
}
```

Aggregator output shape (to be consumed by Plan 14-02):
```python
{
  "failuresByAgent": {"hermes": 12, "gwen": 3},
  "failuresByErrorType": {"permission_denied": 8, "timeout": 7}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create stateful failures.log parser with multi-line JSON support</name>
  <files>~/github/knowledge/scripts/failures_parser.py, ~/github/knowledge/scripts/tests/test_failures_parser.py</files>
  <behavior>
    - Test 1 (happy path, single-line entries): Given failures.log with 3 newline-separated single-line JSON objects, returns a list of 3 dicts with correct agent_id/error_type.
    - Test 2 (multi-line tracebacks — CRITICAL): Given failures.log with one JSON object spanning 8 lines (pretty-printed with indented keys and embedded `\n` in traceback string), returns a single dict with the full traceback intact. Line-by-line parsing would fail this.
    - Test 3 (mixed single-line + multi-line): Log containing both styles returns exactly the expected count with all entries intact.
    - Test 4 (disk_critical exclusion in aggregate): `aggregate_failures()` called on a list with 2 `permission_denied` + 3 `disk_critical` + 1 `timeout` entries returns `failuresByErrorType={"permission_denied": 2, "timeout": 1}` — disk_critical NEVER appears.
    - Test 5 (agent attribution): `aggregate_failures()` groups by `agent_id` correctly; unknown/missing agent_id defaults to "unknown" bucket.
    - Test 6 (empty file): `parse_failures_log()` on an empty file returns `[]`; `aggregate_failures([])` returns `{"failuresByAgent": {}, "failuresByErrorType": {}}`.
    - Test 7 (missing file): `parse_failures_log("/nonexistent/path")` returns `[]` — does NOT raise FileNotFoundError.
    - Test 8 (malformed object): A partially-written object at the end of the file (unterminated JSON) is skipped; earlier complete entries are still returned.
    - Test 9 (disk_critical variant spellings): Case-insensitive match — "disk_critical", "DISK_CRITICAL", "Disk_Critical" all excluded. (Decision: be strict lowercase per STATE.md `disk_critical` literal; document in module docstring.)
  </behavior>
  <action>
    Create `~/github/knowledge/scripts/failures_parser.py`:

    1. Implement `parse_failures_log(path: str) -> list[dict]`:
       - If path does not exist → return `[]`
       - Open file, read line-by-line into a buffer
       - Track brace depth: increment on `{`, decrement on `}` (ignoring braces inside string literals — use `json.JSONDecoder.raw_decode` on the accumulated buffer for robust parsing)
       - Preferred implementation: accumulate ALL file content into a string, then use `json.JSONDecoder().raw_decode()` in a loop, advancing the index past whitespace between objects. This natively handles both single-line and multi-line pretty-printed JSON objects, and cleanly stops on malformed trailing content.
       - Return list of successfully decoded dicts (skip objects that fail to decode — do not abort).
       - Wrap the outer file-open in try/except for FileNotFoundError → return `[]`.

    2. Implement `aggregate_failures(entries: list[dict]) -> dict`:
       - Filter out entries where `entry.get("error_type") == "disk_critical"` (strict lowercase per STATE.md).
       - Build `failuresByAgent` dict: count entries per `agent_id` (default bucket "unknown" if missing/None/empty).
       - Build `failuresByErrorType` dict: count entries per `error_type` (skip entries missing error_type — they were already filtered if disk_critical, otherwise bucket as "unknown" is acceptable).
       - Return `{"failuresByAgent": {...}, "failuresByErrorType": {...}}`.

    3. Module docstring must state: "disk_critical exclusion is strict lowercase match per SKILL-06 / STATE.md."

    Create `~/github/knowledge/scripts/tests/test_failures_parser.py`:
    - Use pytest tmp_path fixture to write test failures.log files.
    - Cover all 9 test cases above.
    - No network, no real filesystem outside tmp_path.

    NOTE: `skill-sync.py` lives in a separate repo (`~/github/knowledge/`) — work in that worktree. Ensure the file is tracked; commit will happen in the knowledge repo.
  </action>
  <verify>
    <automated>cd ~/github/knowledge && python -m pytest scripts/tests/test_failures_parser.py -v</automated>
  </verify>
  <done>All 9 pytest test cases pass. Module exports `parse_failures_log` and `aggregate_failures`. Multi-line JSON test (Test 2) explicitly demonstrates a case that would break naive `.splitlines()` parsing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument skill-sync.py to emit 'failed' events on skill operation errors</name>
  <files>~/github/knowledge/scripts/skill-sync.py, ~/github/knowledge/scripts/tests/test_skill_sync.py</files>
  <behavior>
    - Test 1 (failure path emits JSONL): When a skill operation raises inside the skill-sync loop, a new line is appended to `skill-contributions.jsonl` containing `event=failed`, correct `agent_id`, a classified `error_type`, and a valid ISO8601 `timestamp`.
    - Test 2 (success path unchanged): Happy-path operations still emit their existing `synced`/`contributed`/`pruned` events with no regression — existing test suite still passes.
    - Test 3 (error_type classification): Different exception classes map to stable error_type strings (e.g., `PermissionError` → `"permission_denied"`, `FileNotFoundError` → `"file_not_found"`, `OSError` with ENOSPC → `"disk_full"`, generic `Exception` → `"unknown"`). Exact mapping documented in code.
    - Test 4 (atomic append): JSONL append uses the same append-safe pattern existing events use (no truncation, newline-terminated). File integrity check via re-reading and parsing each line.
    - Test 5 (does NOT log disk_critical to JSONL): If the classification yields `disk_critical` (e.g., emergency abort path), the event is still written to failures.log as today but the JSONL `failed` event is SKIPPED — since Plan 14-02 will filter disk_critical downstream anyway, keeping JSONL clean is preferred. (Decision: document this explicitly in the module — don't double-count the user's noisiest error type in the contributions feed.)
  </behavior>
  <action>
    Modify `~/github/knowledge/scripts/skill-sync.py`:

    1. Locate the existing error handling path(s) in the sync loop (grep for `except`, `failures.log`, and existing JSONL emit helpers).

    2. Add a helper `_classify_error(exc: Exception) -> str`:
       - `PermissionError` → `"permission_denied"`
       - `FileNotFoundError` → `"file_not_found"`
       - `OSError` with `errno == errno.ENOSPC` → `"disk_full"`
       - `TimeoutError` → `"timeout"`
       - else → `"unknown"`
       - Emergency/abort paths that currently write `disk_critical` to failures.log → return `"disk_critical"` (caller will skip JSONL emit).

    3. Add a helper `_emit_failed_event(agent_id: str, error_type: str, skill: str | None = None)`:
       - If `error_type == "disk_critical"`: return early (do NOT write to JSONL — log-only).
       - Build dict: `{"timestamp": <iso8601 UTC Z>, "agent_id": agent_id, "event": "failed", "error_type": error_type, "skill": skill}`.
       - Append to `skill-contributions.jsonl` using the same write pattern existing events use (open with mode="a", write JSON + "\n", no flush tricks that existing code doesn't already use).

    4. In the existing `except` blocks that currently write to `failures.log`, ALSO call `_emit_failed_event(agent_id, _classify_error(exc), skill=<if-known>)` immediately after the failures.log write. Do NOT change failures.log format — Plan 14-02's parser depends on it.

    5. The `skill` field may be `None` when the error occurs before a specific skill is identified — that's expected and documented.

    Extend `~/github/knowledge/scripts/tests/test_skill_sync.py`:
    - Add tests 1-5 above using pytest monkeypatch for the sync path and tmp_path for the JSONL file.
    - Mock exceptions at the right point in the sync loop.
    - Assert JSONL contains exactly the new `failed` entry with all expected fields.

    Do NOT modify the Vitest/TypeScript side — Plan 14-02 handles the consumer.
  </action>
  <verify>
    <automated>cd ~/github/knowledge && python -m pytest scripts/tests/test_skill_sync.py -v</automated>
  </verify>
  <done>Failure events emit to `skill-contributions.jsonl` with event=failed on all classified exceptions except `disk_critical`. All new tests pass. Existing test suite still passes (no regression on success paths). `_classify_error` and `_emit_failed_event` helpers are exported at module scope so they can be unit-tested directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → parser | `failures.log` content is machine-generated, not user-supplied, but malformed entries must not crash the parser. |
| skill-sync error path → JSONL | Error metadata (exception message, paths) could contain sensitive info — MUST NOT leak raw tracebacks into the JSONL feed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `failures.log` parser | mitigate | Parser treats content as untrusted JSON; uses `json.JSONDecoder.raw_decode` with per-object try/except so a poisoned entry cannot break aggregation. |
| T-14-02 | Information Disclosure | JSONL `failed` event | mitigate | `_emit_failed_event` writes ONLY `agent_id`, `error_type` (classified enum — not raw message), `timestamp`, `skill`. No traceback, no exception args. |
| T-14-03 | Denial of Service | Parser on giant log | accept | Current deployment log size is bounded by rotation; document in module docstring that full-file read is used and note rotation assumption. |
| T-14-04 | Repudiation | JSONL append | accept | Existing Phase 9 JSONL has no signing — out of scope; matches existing threat profile. |
</threat_model>

<verification>
Phase-level checks for this plan:
- `python -m pytest scripts/tests/test_failures_parser.py scripts/tests/test_skill_sync.py -v` passes with all new cases green.
- Manually trigger a failure (e.g., chmod -r a target dir, run skill-sync.py) → observe a new `event=failed` line appended to `skill-contributions.jsonl` within one run.
- `python -c "from scripts.failures_parser import parse_failures_log, aggregate_failures; print(aggregate_failures(parse_failures_log('/tmp/does-not-exist')))"` prints `{'failuresByAgent': {}, 'failuresByErrorType': {}}` — no exception.
</verification>

<success_criteria>
1. SKILL-06 Success Criterion #1 met: a triggered skill failure produces a new `event=failed` JSONL entry.
2. `failures_parser.py` parses multi-line JSON correctly (SKILL-06 SC #2 data source ready).
3. `disk_critical` is strictly excluded from aggregate_failures output (SKILL-06 SC #3 at the parser layer).
4. Parser returns empty aggregates gracefully when failures.log is absent (SKILL-06 SC #4 data-layer prep).
5. All pytest cases (9 parser + 5 skill-sync) pass. No regression in existing skill-sync tests.
</success_criteria>

<output>
After completion, create `.planning/phases/14-skill-failure-rate/14-01-SUMMARY.md` documenting:
- Exact path to `failures_parser.py` and the two exported functions.
- The `_classify_error` exception → error_type mapping table (for Plan 14-02 consumers).
- Confirmation that `disk_critical` is filtered at BOTH the emit layer (skill-sync) and aggregate layer (parser).
- Pointer to the knowledge-repo commit SHA where these changes land (`~/github/knowledge` is a separate git repo).
</output>
