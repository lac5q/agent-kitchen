---
phase: 174-noc-operator-layout
plan: 01
type: execute
wave: 2
files_modified:
  - apps/memroos/src/components/operations/index.tsx
  - apps/memroos/src/components/operations/noc-header.tsx
  - apps/memroos/src/components/operations/pulse-strip.tsx
  - apps/memroos/src/components/operations/memory-consumption.tsx
  - apps/memroos/src/components/operations/memory-not-digested.tsx
  - apps/memroos/src/components/operations/agent-workload.tsx
  - apps/memroos/src/components/operations/activity-heatmap.tsx
  - apps/memroos/src/components/operations/savings-waste.tsx
  - apps/memroos/src/components/operations/behavior-signals.tsx
  - apps/memroos/src/components/operations/__tests__/operations-noc.test.tsx
  - apps/memroos/src/components/operations/__tests__/efficiency-signals.test.tsx
autonomous: true
requirements:
  - NOCUX-01
  - NOCUX-02
  - NOCUX-03
  - NOCUX-04
  - NOCUX-05
must_haves:
  truths:
    - "The default NOC order is Pulse, Attention, Memory, Agent Activity/Models/Activity, Cost, Governance/Skills."
    - "Every default panel has a semantic state: window-empty, no-history, stale/error, or live; none says only 'No events'."
    - "Useful but unwired panels are hidden behind a remembered Show advanced toggle, off by default."
    - "24h, 7d, 30d, and workspace filters apply consistently to every default panel."
---

<objective>
Turn the approved K3 v1.1 design into the smallest operator-first NOC layout. Reuse existing panels where their source is truthful; merge, hide, or source-swap the rest. Do not add new analytics concepts.
</objective>

<files>
- `index.tsx` and `noc-header.tsx` — reorder panels and add the remembered Show advanced control.
- Existing panel files above — merge tier health into Memory, make Agent Activity message-backed, make heatmap source truthful, make Cost spend-primary, move Behavior/Efficiency to Advanced.
- Existing tests — update the layout and semantic empty-state assertions.
</files>

<steps>
1. Read Phase 173's response contract and K3 design at `.planning/design/noc-rethink-v1.md`. Do not start until the Phase 173 tests are green.
2. Reorder `OperationsNoc`: Header → Pulse → Attention → Memory with tier health → Agent Activity/Models/Activity Heatmap → Cost → Governance/Skills → Advanced.
3. Update Pulse KPIs to use only reliable day-1 signals: messages, memory writes, cron health, skills enabled, active models, and last activity. Remove hive-dependent KPIs from the default strip.
4. Merge `MemoryNotDigested` tier health into `MemoryConsumption`; remove the standalone default block.
5. Convert AgentWorkload to Agent Activity using Phase 173 message data. Show delegation detail only if present.
6. Convert ActivityHeatmap to messages plus memory-write timing. Make Cost show spend and per-model usage first; render waste/savings only when the existing ledger proves them.
7. Move EfficiencySignals and BehaviorSignals to Advanced. Add a header Show advanced toggle with remembered preference; default false. Preserve accessible labels and keyboard operation.
8. Apply the four-state empty model from the K3 design to every default panel. Use a shared local renderer only if it removes duplicated empty-state code; do not create a framework.
9. Add/update focused tests for default ordering, hidden advanced panels, toggle behavior, 24h/7d/30d + workspace propagation, semantic empty states, and mobile-safe grid behavior.
</steps>

<verify>
- `cd apps/memroos && npm test -- --run src/components/operations/__tests__/operations-noc.test.tsx src/components/operations/__tests__/efficiency-signals.test.tsx src/components/operations/__tests__/noc-actions.test.tsx`
- `git grep -nE 'No events|empty by design' -- apps/memroos/src/components/operations` returns no user-facing default-panel copy.
- `npm run typecheck` and `npm test -- --run` pass before PR.
</verify>

<out_of_scope>
- Do not add new charts, a new design system, new panel drill-downs, or a new persisted dashboard configuration service.
- Do not expose advanced EFFTEL metrics as live while producers remain unwired.
- Do not change the existing workspace model; hide the picker when one workspace is available.
</out_of_scope>
