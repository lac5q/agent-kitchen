---
phase: 115-prod-hygiene-2026-07-18
plan: 02
type: execute
wave: 2
depends_on:
  - "115-01"
files_modified:
  - scripts/disk-prune.sh
autonomous: true
must_haves:
  truths:
    - "`scripts/disk-prune.sh` exists, is executable, and accepts `--dry-run` / `--apply` modes"
    - "Default target list = the 4 items cleared on 2026-07-18: `/tmp/pip-unpack-*`, `~/.npm/_logs`, 4-day-old `/home/<user>/.factory/logs/droid-log-single.log.2026-07-*` rotated, plus `node_modules` directories older than 60 days no longer referenced by any checked-out repo"
    - "`disk-prune --dry-run` prints a top-10 list and exits non-zero on disk >75% used without `--apply`"
---

<objective>
Convert the ad-hoc `Disk-prune on /home/opc: cleared /tmp/pip-unpack-*` note in `reports/m2-m3/prod-findings-2026-07-18.md` (item 1) into a reproducible script + a follow-up cadence check. No more "will surface top-10 items before any delete" — write the script.

</objective>
