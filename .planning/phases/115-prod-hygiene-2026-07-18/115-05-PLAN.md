---
phase: 115-prod-hygiene-2026-07-18
plan: 05
type: execute
wave: 2
depends_on:
  - "115-01"
files_modified:
  - scripts/cursor-agent-wrapper.sh
  - scripts/integrations/cursor-agent/** (caller sites)
autonomous: true
must_haves:
  truths:
    - "`ps aux | grep cursor-agent` shows no api-key argument in any line"
    - "All `cursor-agent` invocations read the key from `CURSOR_API_KEY` env var, never from argv"
---

<objective>
Fix item 4: `cursor-agent` exposes API key in `ps aux`. Wrap the call so the key is read from env (not argv). Single wrapper script + retrofit any caller that uses `--api-key`.
