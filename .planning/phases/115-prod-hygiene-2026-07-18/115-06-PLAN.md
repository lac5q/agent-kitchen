---
phase: 115-prod-hygiene-2026-07-18
plan: 06
type: execute
wave: 2
depends_on:
  - "115-01"
files_modified:
  - docs/cloud-operator-oracle1-runbook.md
  - infrastructure/tailscale-acl.json
autonomous: true
must_haves:
  truths:
    - "`docs/cloud-operator-oracle1-runbook.md` §`security-considerations` calls out that `ssh-user=root` on `oracle-1` means tailnet membership == root, and points at `infrastructure/tailscale-acl.json` for the fix"
    - "`infrastructure/tailscale-acl.json` (if present) has a comment noting the `ssh-user` should be `opc`, not `root`"
---

<objective>
Fix item 5: Tailscale `--ssh-user=root` on `oracle-1` makes tailnet membership equivalent to root. Document the risk in the operator runbook and add an ACL note narrowing it. Hygiene, not a fire.
