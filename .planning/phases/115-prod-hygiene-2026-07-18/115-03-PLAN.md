---
phase: 115-prod-hygiene-2026-07-18
plan: 03
type: execute
wave: 2
depends_on:
  - "115-01"
files_modified:
  - services/knowledge-mcp/catchup.py
  - services/knowledge-mcp/tests/test_catchup_paging.py
autonomous: true
must_haves:
  truths:
    - "Reproducible test: when graph-catchup queries a queue of N entries and the page-size cursor equals the projected count, the second-page result is empty (no skip-loop)"
    - "Paging in `services/knowledge-mcp/catchup.py` uses cursor comparison, not equality"
---

<objective>
Fix item 2 from prod findings: `graph-catchup projected=50, skipped=50` forever. Likely a paging bug where `cursor == projected` is treated as `skipped == projected`. Add a failing test that reproduces, then fix.
