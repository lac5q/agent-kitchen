---
phase: 115-prod-hygiene-2026-07-18
plan: 01
type: execute
wave: 1
files_modified:
  - reports/m2-m3/prod-findings-2026-07-18.md
  - services/knowledge-mcp/** (disk-prune owners)
  - services/orchestration/** (graph-catchup paging)
autonomous: false
must_haves:
  truths:
    - "Each of the 5 prod findings in `reports/m2-m3/prod-findings-2026-07-18.md` has either: a follow-up child phase, or an explicit 'acknowledged-no-action' annotation"
    - "The disk-prune section in that file has an owner + next-run timestamp"
    - "`graph-catchup projected=50, skipped=50` is reproduced in a test or recorded as a known issue with paging heuristic to apply"
---

<objective>
Convert the 5 prod-hygiene items in `reports/m2-m3/prod-findings-2026-07-18.md` from prose findings into actionable tickets. Each item gets either a child phase or a deliberate `acknowledged-no-action` annotation so the report stops drifting.

Source finding list (verbatim from the report — 5 items):

1. **Disk at 79% used on prod `/home/opc`** (~700 `node_modules` dirs). Phase 115-02 = deterministic, runbook'd prune.
2. **`graph-catchup projected=50 skipped=50` forever.** Phase 115-03 = paging-heuristic unit test.
3. **`cloudflared` boot-time `Permission denied` on the binary.** Recovered. Phase 115-04 = binary-permission pre-flight in operator runbook.
4. **`cursor-agent` exposes API key as CLI arg in `ps aux`.** Phase 115-05 = tighten invocation to read from env.
5. **Tailscale `--ssh-user=root` on `oracle-1`** means tailnet membership == root. Phase 115-06 = operator runbook warning.

</objective>

<files>
- `.planning/phases/115-prod-hygiene-2026-07-18/115-{02,03,04,05,06}-PLAN.md`: 5 child phases (~30 lines each).
- `reports/m2-m3/prod-findings-2026-07-18.md` is NOT modified in this plan; it remains the source document.
</files>

<steps>
1. Create the 5 child plan files 115-02 through 115-06.
2. Open one PR containing just the new PLAN.md files (no source changes).
</steps>

<verify>
- `ls .planning/phases/115-prod-hygiene-2026-07-18/*.md` shows 6 files (one parent + 5 children).
- Each child PLAN.md has at least 2 of `must_haves.truths` populated.
- `git diff --stat` shows only files under `.planning/phases/115-prod-hygiene-2026-07-18/`.
</verify>

<out_of_scope>
- Actually running the disk prune, fixing cursor-agent, or touching cloudflared permissions. Those are the work of the child phases — this plan just creates them.
- Editing the `reports/m2-m3/prod-findings-2026-07-18.md` triage document; that is a human-maintained file.
</out_of_scope>
