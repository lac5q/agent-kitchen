---
phase: 114-tsc-test-cleanup
plan: 01
type: execute
wave: 1
files_modified: []
autonomous: true
must_haves:
  truths:
    - "A grouped inventory of the 728 `tsc --noEmit` errors by folder exists at `.planning/phases/114-tsc-test-cleanup/INVENTORY.md`"
    - "Each error group is classified as one of: (a) deliberate loose-vitest typing, (b) test-fixture drift, (c) real type bug masked by vitest"
    - "For each group, a follow-up plan path is recorded (114-02 onward, or skipped with rationale)"
---

<objective>
`npm run typecheck` returns 0 because the project wrapper excludes `__tests__/**`. Raw `tsc --noEmit --project apps/memroos/tsconfig.json` produces 728 errors across 140+ test files. Goal of this plan: classify them so the next 2-4 plans only have to fix the ones that matter.

</objective>

<files>
Only `.planning/phases/114-tsc-test-cleanup/INVENTORY.md` is added. No source files modified in this plan.
</files>

<steps>
1. From repo root: `npx tsc --noEmit --project apps/memroos/tsconfig.json 2>&1 | tee /tmp/tsc-full.log`
2. Bucket by `*.tsx`/`*.ts` and group by error code (TS2322, TS2345, TS2741, etc.):
   `grep -oE 'TS[0-9]+' /tmp/tsc-full.log | sort | uniq -c | sort -rn`
3. Bucket by directory (already known from prior audit):
   ```
    79  components/flow/__tests__
    75  components/operations/__tests__
    62  lib/__tests__
    55  components/engagement/__tests__
    ...
   ```
4. Write `.planning/phases/114-tsc-test-cleanup/INVENTORY.md` with three columns: directory, error code count, classification (loose-vitest / fixture-drift / real-bug).
5. Open a PR that adds only that one inventory file.
</steps>

<verify>
- `ls .planning/phases/114-tsc-test-cleanup/INVENTORY.md` exists.
- File has one row per affected directory.
- `npx tsc --noEmit --project apps/memroos/tsconfig.json 2>&1 | wc -l` still 728 (no source changes yet — this plan is inventory only).
- `npm test -- --run` stays at 3327 passed / 19 skipped / 0 failed.
</verify>

<out_of_scope>
- Fixing the errors — handled by plan 02 onward.
- Touching `apps/memroos/tsconfig.json` to re-include tests (that would change CI behavior; not in scope here).
</out_of_scope>
