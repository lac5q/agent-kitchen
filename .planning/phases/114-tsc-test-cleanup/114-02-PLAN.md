---
phase: 114-tsc-test-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "114-01"
files_modified: []
autonomous: true
must_haves:
  truths:
    - "Top 5 directories by error count (per plan 01 inventory) have a one-line fix recipe per error code"
    - "Recipes cover the three largest error codes (likely TS2322 + TS2345 + TS2741) with concrete `as` / generic-param / type-narrowing patches"
    - "Each recipe includes a 1-test verification command (`npx vitest run <file>`)"
---

<objective>
Produce concrete fix recipes for the top-5 directories from plan 01's inventory. Recipes are copy-paste-able into the next session by an agent that hasn't read 114-01.

</objective>

<files>
Just `.planning/phases/114-tsc-test-cleanup/FIX-RECIPES.md`.
</files>

<steps>
1. For each of the top 5 directories, run `npx tsc --noEmit --project apps/memroos/tsconfig.json 2>&1 | grep '^<dir>/'` and read the first ~10 errors.
2. Group by `(error-code, target-symbol)` so the same fix can be repeated.
3. Write a recipe per pattern:
   - Example: `TS2322: Type 'string' is not assignable to type 'Role'` → replace mock cast `as unknown as Role` with explicit builder.
   - Example: `TS2345: Argument of type ... is not assignable to parameter` → add explicit `as const` or extend the fixture type.
4. Each recipe: title, root cause, diff (before/after), verification (`npx vitest run <one-file>`).
</steps>

<verify>
- `npx vitest run` after applying any single recipe stays at the same pass/fail count (3327/0/19).
- No recipe edits source files outside `__tests__/`.
</verify>

<out_of_scope>
- Re-running typecheck to verify zero errors — that's plan 03.
- Touching the wrapper `npm run typecheck` to re-include tests.
</out_of_scope>
