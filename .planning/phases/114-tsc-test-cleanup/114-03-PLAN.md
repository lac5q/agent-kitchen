---
phase: 114-tsc-test-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - "114-02"
files_modified:
  - apps/memroos/src/components/flow/__tests__/**
  - apps/memroos/src/components/operations/__tests__/**
  - apps/memroos/src/lib/__tests__/**
  - apps/memroos/src/components/engagement/__tests__/**
  - apps/memroos/src/lib/msiq/__tests__/**
autonomous: true
must_haves:
  truths:
    - "`npx tsc --noEmit --project apps/memroos/tsconfig.json 2>&1 | wc -l` decreases by ~280 (from 728 to ~448)"
    - "`npm test -- --run` stays at 3327 passed / 19 skipped / 0 failed"
    - "No source files outside `__tests__/` are modified"
---

<objective>
Apply the recipes from plan 02 to the top-5 directories by error count. Goal: clear ~280 errors (~40% of the 728 baseline) in one batch.

</objective>

<files>
5 directories; ~50 test files total. Each file gets the recipe that matches its largest error code.
</files>

<steps>
1. Open a branch from `origin/main`: `git checkout -b chore/tsc-cleanup-batch-1 origin/main`
2. For each top-5 directory, run the recipe from 114-02 against every file in the directory.
3. After each directory: `npx tsc --noEmit --project apps/memroos/tsconfig.json 2>&1 | grep '^<dir>/' | wc -l` to confirm 0 or near-0 remaining in that dir.
4. After all five: `npm test -- --run` to confirm vitest pass count unchanged.
5. Open one PR (or 5 narrow PRs if the diff exceeds ~2000 lines).
</steps>

<verify>
- `git diff --stat` shows only files under the 5 target dirs.
- `wc -l <(npx tsc --noEmit 2>&1)` ≤ 448.
- `npm test -- --run` → 3327 passed.
</verify>
