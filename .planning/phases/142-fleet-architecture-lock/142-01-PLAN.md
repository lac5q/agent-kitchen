# Phase 142 Plan 01 — Fleet Architecture Lock + Validation Gate

**Phase:** 142 — Fleet Architecture Lock + Validation Gate  
**Milestone:** v8.5 Agent Fleet Plane  
**Status:** potential plan (not started)  
**Requirements:** FLEET-01, FLEET-02, FLEET-03, FLEET-04  
**Wave:** 1  

## Goal

Make the MemroOS-top / LangGraph-peer / Paperclip-tenant decision binding in GSD and knowledge, with an independent model validation gate and a scenario that the fleet plane must survive.

## Context

Architecture decision already drafted at `content/architecture/memroos-as-agent-fleet-plane-2026-07-08.md`. Paperclip audit and OSS control-plane research are filed. Independent validation failed (self-validation via MiniMax-M3). This plan closes the documentation + validation loop so later phases do not re-open topology.

## Tasks

### Task 1 — Promote architecture decision to GSD-bound status
- [ ] Ensure `content/architecture/memroos-as-agent-fleet-plane-2026-07-08.md` is linked from `docs/architecture.md` (module map / placement rules)
- [ ] Add one-paragraph "Fleet plane" subsection to `docs/architecture.md` pointing at the decision doc
- [ ] Keep standing gates: MIT-OSS, fail-closed, no Archestra default

### Task 2 — Independent second-opinion validation
- [ ] Run architecture brief through a model that is **not** MiniMax-M3 (prefer GLM-5.2 Max if key available; else GPT-5.5 / Claude)
- [ ] File verdict under `content/architecture/memroos-fleet-plane-validation-<model>-2026-07-08.md` with provenance frontmatter
- [ ] If verdict is reject / major changes, open amend loop before Phase 143

### Task 3 — Fleet scenario S12
- [ ] Add scenario **S12 — Operator runs agents on Mac + remote OpenClaw/Hermes gateways** to ROADMAP backlog scenarios
- [ ] Success condition: MemroOS registry + NOC show all agents; Paperclip company tasks still budget-enforced; LangGraph multi-step HIL works without Paperclip being the top layer

### Task 4 — Research index in planning tree
- [ ] Symlink or short index note in `.planning/research/agent-fleet-plane-2026-07-08.md` pointing at content/ research + audit + architecture

## Success criteria

1. Architecture decision is discoverable from `docs/architecture.md`
2. Independent validation artifact exists with non-MiniMax provenance
3. S12 scenario is in ROADMAP backlog and referenced by FLEET requirements
4. No code path changes required for this plan (docs + validation only)

## Verification

- [ ] `rg -n "Fleet plane|Agent Fleet" docs/architecture.md`
- [ ] Validation file frontmatter has `model:` ≠ MiniMax-M3
- [ ] ROADMAP contains S12
- [ ] MEMSEC-08 unchanged (no product code)

## Risks

- GLM key still missing → use GPT-5.5 / Claude; do not fake GLM provenance again
- Validator proposes Archestra top → re-assert AGPL + adapter rewiring cost; document as rejected alternative

## Out of scope

- Implementing adapters, OPA, Paperclip bridge, HA
