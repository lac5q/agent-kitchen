---
phase: 05-knowledge-ingestion-pipeline
plan: 05
type: execute
wave: 3
depends_on: [05-02, 05-03, 05-04]
files_modified:
  - ~/github/knowledge/knowledge-curator.sh
autonomous: false
requirements: [D-16, D-17]

must_haves:
  truths:
    - "Email ingestion cron runs every 6 hours"
    - "Transcript ingestion is called from knowledge-curator.sh nightly"
    - "Live test of email ingestion produces markdown files in ~/github/knowledge/emails/"
    - "Live test of transcript ingestion produces files in gdrive/meet-recordings/ or spark-recordings/"
    - "ingestion-state.json is updated after successful runs"
  artifacts:
    - path: "~/github/knowledge/knowledge-curator.sh"
      provides: "Updated orchestrator calling transcript ingestion"
      contains: "personal-ingestion-transcripts.sh"
  key_links:
    - from: "~/github/knowledge/knowledge-curator.sh"
      to: "~/github/knowledge/personal-ingestion-transcripts.sh"
      via: "Step 5 call in nightly orchestrator"
      pattern: "personal-ingestion-transcripts"
---

<objective>
Wire cron schedules for both ingestion scripts and run live end-to-end integration tests to verify the full pipeline works.

Purpose: All scripts exist but are not yet scheduled or tested with real data. This plan registers crons and validates the pipeline produces real output.
Output: Updated knowledge-curator.sh, registered crontab entries, verified real data output
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-knowledge-ingestion-pipeline/05-CONTEXT.md
@.planning/phases/05-knowledge-ingestion-pipeline/05-02-SUMMARY.md
@.planning/phases/05-knowledge-ingestion-pipeline/05-03-SUMMARY.md
@.planning/phases/05-knowledge-ingestion-pipeline/05-04-SUMMARY.md
@~/github/knowledge/knowledge-curator.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register cron entries and update knowledge-curator.sh</name>
  <files>~/github/knowledge/knowledge-curator.sh</files>
  <read_first>
    - ~/github/knowledge/knowledge-curator.sh (file being modified)
    - ~/github/knowledge/personal-ingestion-email.sh (verify exists and is executable)
    - ~/github/knowledge/personal-ingestion-transcripts.sh (verify exists and is executable)
  </read_first>
  <action>
1. Add email ingestion to crontab (every 6 hours):
   Run: crontab -l to get current crontab
   Append this line (if not already present):
   0 */6 * * * /Users/yourname/github/knowledge/personal-ingestion-email.sh >> /tmp/personal-ingestion-email.log 2>&1
   Write back with: (crontab -l; echo "NEW_LINE") | crontab -

2. Update ~/github/knowledge/knowledge-curator.sh to add transcript ingestion as Step 5 (before the final "complete" log):

   Add after Step 4 (the QMD update + Qdrant indexer section):
   ```
   # Step 5 -- Personal transcript ingestion (calendar + Drive Meet + Spark)
   log "[5/5] Personal transcript ingestion..."
   ~/github/knowledge/personal-ingestion-transcripts.sh || log "  Warning: transcript ingestion failed (non-fatal)"
   ```

   Also update Step labels from [1/4], [2/4], [3/4], [4/4] to [1/5], [2/5], [3/5], [4/5] to reflect new total.

3. Verify crontab has both entries:
   - The existing nightly curator: 0 2 * * * .../knowledge-curator.sh
   - The new email ingestion: 0 */6 * * * .../personal-ingestion-email.sh
  </action>
  <verify>
    <automated>crontab -l 2>&1 | grep -c "personal-ingestion" && grep "5/5" ~/github/knowledge/knowledge-curator.sh</automated>
  </verify>
  <acceptance_criteria>
    - crontab -l | grep "personal-ingestion-email.sh" exits 0
    - grep "personal-ingestion-transcripts.sh" ~/github/knowledge/knowledge-curator.sh exits 0
    - grep "5/5" ~/github/knowledge/knowledge-curator.sh exits 0
    - grep "Step 5" ~/github/knowledge/knowledge-curator.sh exits 0 (or equivalent comment)
    - bash -n ~/github/knowledge/knowledge-curator.sh exits 0 (still valid syntax)
  </acceptance_criteria>
  <done>Cron registered for email (every 6h). Transcript ingestion wired into nightly knowledge-curator.sh as Step 5.</done>
</task>

<task type="auto">
  <name>Task 2: Run live integration test</name>
  <files></files>
  <read_first>
    - ~/github/knowledge/ingestion-state.json (check initial state before test)
    - ~/github/knowledge/personal-ingestion-email.sh (script to test)
    - ~/github/knowledge/personal-ingestion-transcripts.sh (script to test)
  </read_first>
  <action>
Run both ingestion scripts live and verify output:

1. Run email ingestion:
   bash ~/github/knowledge/personal-ingestion-email.sh
   Verify output: ls ~/github/knowledge/emails/ should show at least 1 .md file
   Verify state: jq .gmail.last_run ~/github/knowledge/ingestion-state.json should show recent timestamp
   Verify daily note: cat ~/github/knowledge/journals/$(date '+%Y-%m-%d').md should contain "## Email Digest"

2. Run transcript ingestion:
   bash ~/github/knowledge/personal-ingestion-transcripts.sh
   Verify output: ls ~/github/knowledge/gdrive/meet-recordings/ OR ls ~/github/knowledge/spark-recordings/ should show files
   Verify state: jq .calendar.last_run ~/github/knowledge/ingestion-state.json should show recent timestamp
   Verify daily note: cat ~/github/knowledge/journals/$(date '+%Y-%m-%d').md should contain "## Meetings"

3. Run smoke test to confirm everything still passes:
   bash ~/github/knowledge/personal-ingestion-smoke.sh

If any script fails: examine the error output, fix the issue in the relevant Python worker, and re-run. Common issues:
- gws JSON parse error: check gws command output format
- mem0 connection refused: verify mem0 server is running (curl localhost:3201/health)
- Permission denied on Spark DB: verify file path and read permissions
  </action>
  <verify>
    <automated>ls ~/github/knowledge/emails/*.md 2>/dev/null | wc -l | xargs test 0 -lt && echo "EMAILS OK" && jq -e '.gmail.last_run != "2026-01-01T00:00:00Z"' ~/github/knowledge/ingestion-state.json > /dev/null && echo "STATE OK"</automated>
  </verify>
  <acceptance_criteria>
    - ls ~/github/knowledge/emails/*.md | wc -l is greater than 0
    - jq .gmail.last_run ~/github/knowledge/ingestion-state.json does NOT output "2026-01-01T00:00:00Z" (was updated)
    - cat ~/github/knowledge/journals/$(date '+%Y-%m-%d').md contains "Email Digest" or "Meetings"
    - bash ~/github/knowledge/personal-ingestion-smoke.sh exits 0
  </acceptance_criteria>
  <done>Live test confirms email and transcript ingestion produce real output. State file updated. Daily notes created. Smoke test passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of pipeline output</name>
  <action>Human verifies the complete pipeline output is correct and well-formatted.</action>
  <what-built>Complete personal knowledge ingestion pipeline: email (6h cron) + calendar/Drive transcripts/Spark (nightly via knowledge-curator.sh). Produces markdown files, mem0 memories, and Obsidian daily notes.</what-built>
  <how-to-verify>
1. Check email output: ls ~/github/knowledge/emails/ (should show .md files with thread content)
2. Check transcript output: ls ~/github/knowledge/gdrive/meet-recordings/ (Drive transcripts) and ls ~/github/knowledge/spark-recordings/ (Spark transcripts)
3. Check daily note: cat ~/github/knowledge/journals/$(date '+%Y-%m-%d').md (should have Email Digest and/or Meetings sections)
4. Check cron: crontab -l | grep personal (should show email every 6h)
5. Verify multi-event handling (if applicable): check if any index notes exist in ~/github/knowledge/projects/
6. Open Obsidian and visually confirm daily note formatting looks correct
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron daemon to scripts | System scheduler invokes bash scripts |
| Live test to external APIs | Real Gmail/Calendar/Drive API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | D (DoS) | Cron running every 6h | accept | Email ingestion is capped at 50 threads/run; rate limited; non-fatal wrapper prevents cascade |
| T-05-18 | R (Repudiation) | No audit trail for ingestion | mitigate | All scripts log to /tmp/*.log with timestamps; ingestion-state.json records last_run times |
</threat_model>

<verification>
1. crontab -l shows email ingestion entry
2. knowledge-curator.sh has Step 5 for transcripts
3. Live run produces real markdown files
4. State file shows updated timestamps (not default 2026-01-01)
5. Smoke test passes after live run
</verification>

<success_criteria>
- Email cron registered (every 6 hours)
- Transcript ingestion wired into nightly knowledge-curator.sh
- Live test produces at least 1 email markdown file
- State file updated with recent timestamps
- Daily note created with Email Digest and/or Meetings section
- Human approves output format and completeness
</success_criteria>

<output>
After completion, create .planning/phases/05-knowledge-ingestion-pipeline/05-05-SUMMARY.md
</output>
