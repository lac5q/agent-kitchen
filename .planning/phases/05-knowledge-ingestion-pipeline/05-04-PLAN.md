---
phase: 05-knowledge-ingestion-pipeline
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - ~/github/knowledge/personal-ingestion-transcripts.py
  - ~/github/knowledge/qdrant-indexer.py
autonomous: true
requirements: [D-07, D-20]

must_haves:
  truths:
    - "Spark SQLite meetTranscriptEvent rows are read and saved as markdown"
    - "Spark transcripts are added to mem0 as meeting memories"
    - "qdrant-indexer.py PATHS list includes emails, meet-recordings, and spark-recordings dirs"
    - "Spark data accessed read-only via sqlite3 URI mode=ro"
  artifacts:
    - path: "~/github/knowledge/personal-ingestion-transcripts.py"
      provides: "Spark ingestion function added to existing transcript worker"
      contains: "def ingest_spark"
    - path: "~/github/knowledge/qdrant-indexer.py"
      provides: "Updated PATHS list with 3 new directories"
      contains: "spark-recordings"
  key_links:
    - from: "~/github/knowledge/personal-ingestion-transcripts.py"
      to: "/Users/yourname/Library/Application Support/Spark Desktop/core-data/messages.sqlite"
      via: "sqlite3.connect read-only"
      pattern: "mode=ro"
    - from: "~/github/knowledge/qdrant-indexer.py"
      to: "~/github/knowledge/emails"
      via: "PATHS list entry"
      pattern: "emails"
---

<objective>
Add Spark SQLite transcript ingestion to the transcripts worker and update qdrant-indexer.py to index the 3 new content directories (emails, meet-recordings, spark-recordings).

Purpose: Spark captures meetings not recorded by Google Meet. Adding its transcripts ensures comprehensive meeting coverage. Updating qdrant-indexer.py enables automatic vector indexing of all new content on nightly runs.
Output: Spark ingestion function in personal-ingestion-transcripts.py, updated qdrant-indexer.py PATHS
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-knowledge-ingestion-pipeline/05-CONTEXT.md
@.planning/phases/05-knowledge-ingestion-pipeline/05-RESEARCH.md
@.planning/phases/05-knowledge-ingestion-pipeline/05-01-SUMMARY.md
@~/github/knowledge/ingestion_utils.py
@~/github/knowledge/qdrant-indexer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Spark SQLite ingestion to personal-ingestion-transcripts.py</name>
  <files>~/github/knowledge/personal-ingestion-transcripts.py</files>
  <read_first>
    - ~/github/knowledge/personal-ingestion-transcripts.py (file being modified -- from Plan 03)
    - ~/github/knowledge/ingestion_utils.py (shared utilities)
    - ~/github/knowledge/ingestion-state.json (state structure -- spark section)
  </read_first>
  <action>
Add the following to ~/github/knowledge/personal-ingestion-transcripts.py:

New imports (add at top if not already present): import sqlite3

New constant:
SPARK_DB = Path("/Users/yourname/Library/Application Support/Spark Desktop/core-data/messages.sqlite")
SPARK_DIR = KNOWLEDGE_ROOT / "spark-recordings"

New function: ingest_spark(state: dict) -> dict
1. Verify SPARK_DB exists: if not SPARK_DB.exists(): print("Warning: Spark DB not found"); return state
2. Connect read-only: conn = sqlite3.connect(f"file:{SPARK_DB}?mode=ro", uri=True)
3. Set conn.row_factory = sqlite3.Row
4. Query:
   SELECT mte.rowid, mte.summary, mte.startDate,
          m.subject, m.messageFrom, m.messageTo
   FROM meetTranscriptEvent mte
   JOIN messages m ON m.pk = mte.messagePk
   WHERE mte.rowid > ?
   ORDER BY mte.startDate ASC
   Pass state["spark"]["last_message_rowid"] as parameter.
5. For each row:
   a. Convert startDate (unix seconds) to datetime: dt = datetime.datetime.fromtimestamp(row["startDate"])
   b. date_str = dt.strftime("%Y-%m-%d")
   c. Title: use row["subject"] if available, else row["summary"]
   d. Attendees: parse row["messageTo"] (comma-separated emails)
   e. Content: row["summary"] is short (26-39 chars -- this is a title, not full transcript per research). Use as meeting title/topic.
   f. Write markdown to SPARK_DIR / "{date_str}-{rowid}.md" with frontmatter:
      ---
      rowid: {rowid}
      title: {title}
      date: {date_str}
      attendees: [{attendees}]
      source: spark_desktop
      ---
      # {title}
      **Date:** {date_str} {dt.strftime("%H:%M")}
      **Attendees:** {attendees}
      **Summary:** {row["summary"]}
   g. Add mem0 memory: add_mem0("Spark meeting: {title} on {date_str} with {attendees}. Topic: {summary}", "luis", {"type": "spark_transcript", "rowid": rowid, "date": date_str})
   h. Track max rowid for state update
6. Close connection
7. Update state["spark"]["last_message_rowid"] to max rowid seen
8. Print: "Ingested {N} Spark transcript events"
9. Return updated state

Update ingest_transcripts() main function -- add after Drive ingestion:
  state = ingest_spark(state)

Also update state["spark"]["last_run"] to now before save_state(state).
  </action>
  <verify>
    <automated>cd ~/github/knowledge && .venv/bin/python3 -c "from personal_ingestion_transcripts import ingest_spark; print('SPARK IMPORT OK')" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "def ingest_spark" ~/github/knowledge/personal-ingestion-transcripts.py exits 0
    - grep "SPARK_DB" ~/github/knowledge/personal-ingestion-transcripts.py exits 0
    - grep "mode=ro" ~/github/knowledge/personal-ingestion-transcripts.py exits 0 (read-only access)
    - grep "last_message_rowid" ~/github/knowledge/personal-ingestion-transcripts.py exits 0
    - grep "spark-recordings" ~/github/knowledge/personal-ingestion-transcripts.py exits 0
    - grep "import sqlite3" ~/github/knowledge/personal-ingestion-transcripts.py exits 0
    - grep "spark_desktop" ~/github/knowledge/personal-ingestion-transcripts.py exits 0 (source marker in frontmatter)
    - Python import of ingest_spark succeeds
  </acceptance_criteria>
  <done>Spark ingestion reads meetTranscriptEvent joined with messages, writes markdown files with metadata, adds mem0 memories, updates watermark by rowid. Read-only access enforced via URI mode.</done>
</task>

<task type="auto">
  <name>Task 2: Update qdrant-indexer.py PATHS with new content directories</name>
  <files>~/github/knowledge/qdrant-indexer.py</files>
  <read_first>
    - ~/github/knowledge/qdrant-indexer.py (file being modified -- read full PATHS section)
  </read_first>
  <action>
Edit ~/github/knowledge/qdrant-indexer.py -- add 3 new entries to the PATHS list (after the existing 5 entries):

PATHS = [
    Path.home() / "github/agent-lightning",
    Path.home() / "github/agent-lightning/docs",
    Path.home() / "github/knowledge/llm-wiki/wiki",
    Path.home() / "github/knowledge",
    Path.home() / "github/knowledge/mem0-exports",
    # Personal ingestion content (added Phase 5)
    Path.home() / "github/knowledge/emails",
    Path.home() / "github/knowledge/gdrive/meet-recordings",
    Path.home() / "github/knowledge/spark-recordings",
]

No other changes to qdrant-indexer.py. The existing rglob("*.md") logic will automatically pick up any .md files in these new directories.
  </action>
  <verify>
    <automated>cd ~/github/knowledge && grep -c "emails\|meet-recordings\|spark-recordings" qdrant-indexer.py</automated>
  </verify>
  <acceptance_criteria>
    - grep "emails" ~/github/knowledge/qdrant-indexer.py exits 0
    - grep "meet-recordings" ~/github/knowledge/qdrant-indexer.py exits 0
    - grep "spark-recordings" ~/github/knowledge/qdrant-indexer.py exits 0
    - grep -c "Path.home" ~/github/knowledge/qdrant-indexer.py outputs 8 (5 original + 3 new)
  </acceptance_criteria>
  <done>qdrant-indexer.py PATHS list has 8 entries (5 original + 3 new). Next nightly run will automatically index all email and transcript markdown files into knowledge_docs collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script to Spark SQLite | Read-only access to local application database |
| Spark content to mem0 | Meeting metadata stored as agent memory |
| qdrant-indexer.py to Qdrant Cloud | HTTPS with API key; new content dirs indexed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | T (Tampering) | Spark SQLite writes | mitigate | mode=ro in sqlite3 URI connection string prevents any writes to Spark database |
| T-05-15 | I (Info Disclosure) | Spark meeting data in markdown | accept | Same trust level as the Spark app itself; local files in gitignored knowledge repo |
| T-05-16 | D (DoS) | Large PATHS list in qdrant-indexer | accept | 3 additional small directories (tens of files each); negligible impact on runtime |
</threat_model>

<verification>
1. Python import of ingest_spark function succeeds
2. grep confirms all 3 new PATHS in qdrant-indexer.py
3. After live test: ls ~/github/knowledge/spark-recordings/ shows markdown files
4. qdrant-indexer.py still runs without error (import test)
</verification>

<success_criteria>
- Spark ingestion reads from correct SQLite path in read-only mode
- Markdown files created with proper frontmatter (rowid, title, date, attendees, source)
- mem0 memories added for each Spark transcript event
- State watermark updated to highest rowid processed
- qdrant-indexer.py has all 8 PATHS and remains syntactically valid
</success_criteria>

<output>
After completion, create .planning/phases/05-knowledge-ingestion-pipeline/05-04-SUMMARY.md
</output>
