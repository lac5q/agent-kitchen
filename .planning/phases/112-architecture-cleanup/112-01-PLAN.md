---
phase: 112-architecture-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memroos/src/lib/auth/jwt.ts
  - apps/memroos/src/lib/auth/types.ts
  - apps/memroos/src/lib/seal/behavioral-runner.ts
  - apps/memroos/src/lib/seal/apply.ts
  - apps/memroos/src/lib/seal/reflection.ts
  - apps/memroos/src/lib/seal/behavioral-jobs.ts
  - apps/memroos/src/lib/seal/behavioral-sandbox.ts
  - apps/memroos/src/lib/seal/types.ts
  - apps/memroos/src/lib/memory/policy-gate.ts
  - apps/memroos/src/lib/classification/types.ts
  - apps/memroos/src/lib/l3/adapters/netsuite.ts
  - apps/memroos/src/lib/l3/adapters/salesforce.ts
  - apps/memroos/src/lib/l3/adapters/zendesk.ts
  - services/knowledge-mcp/__init__.py
  - services/memory/__init__.py
  - apps/memroos/src/components/flow/demo-mode.tsx
  - apps/memroos/src/components/flow/flow-canvas.tsx
  - apps/memroos/src/components/flow/flow-edge.tsx
  - apps/memroos/src/components/flow/flow-node.tsx
  - apps/memroos/src/components/layout/health-dot.tsx
  - apps/memroos/src/components/ui/tabs.tsx
  - apps/memroos/src/components/voice/useVoiceTranscript.ts
  - apps/memroos/src/scripts/generate-demo-video.mjs
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "knip reports zero unused exports in auth/seal/policy-gate/classification paths"
    - "L3 CRM adapter files are preserved with an intentional-future stub comment"
    - "generateRefreshToken export is removed (no callers exist)"
    - "ruff check services/ reports zero errors (or only pre-existing non-fixable warnings)"
    - "Python vulture whitelist covers MCP/FastAPI decorator handlers"
    - "11 confirmed-dead non-L3 files are removed from the repo"
  artifacts:
    - path: "apps/memroos/src/lib/auth/jwt.ts"
      provides: "jwt utilities without the dead generateRefreshToken export"
    - path: "services/knowledge-mcp/vulture_whitelist.py"
      provides: "vulture whitelist for decorator-registered MCP tool handlers"
    - path: "services/memory/vulture_whitelist.py"
      provides: "vulture whitelist for decorator-registered FastAPI/mem0 handlers"
  key_links:
    - from: "apps/memroos/src/lib/l3/adapters/salesforce.ts"
      to: "ROADMAP backlog P3"
      via: "intentional-stub comment block"
      pattern: "INTENTIONAL_STUB"
---

<objective>
Remove confirmed-dead code (D01-005, D01-006, D01-007, D01-008) from the auth, seal, policy-gate, classification, flow, and voice paths — while preserving intentional-future stubs and adding Python vulture whitelists for decorator-registered handlers.

Purpose: Reduce attack surface from dead security exports, improve bundle size, and establish a clean knip/ruff baseline for Phase 113 test validation.

Output: Clean knip/ruff baseline; dead security exports removed; L3 CRM adapters retained with stub comment; vulture whitelists created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lcalderon/github/memroos/.planning/PROJECT.md
@/Users/lcalderon/github/memroos/.planning/ROADMAP.md
@/Users/lcalderon/github/memroos/.planning/audit/domain-architecture.md
@/Users/lcalderon/github/memroos/.planning/audit/FINDINGS-INDEX.md

<interfaces>
From apps/memroos/src/lib/auth/jwt.ts (current exports):
  export function getSecret(): Uint8Array
  export async function signAccessToken(userId: string, role: UserRole): Promise<string>
  export async function verifyAccessToken(token: string): Promise<JwtPayload | null>
  export function generateRefreshToken(): string  ← TARGET: remove (no callers)

From apps/memroos/src/lib/seal/types.ts (targeted dead exports):
  export type SealAuditEvent  ← knip: unused export

From apps/memroos/src/lib/seal/behavioral-jobs.ts:
  export type PromotionMetadata  ← knip: unused export

From apps/memroos/src/lib/seal/behavioral-sandbox.ts:
  export type SandboxToolCallResult  ← knip: unused export

From apps/memroos/src/lib/seal/behavioral-runner.ts:
  export function listEvalJobs  ← knip: unused export

From apps/memroos/src/lib/seal/apply.ts:
  export function applyProposal  ← knip: unused export (verify dynamic refs first)

From apps/memroos/src/lib/seal/reflection.ts:
  export function reflectOnTrace  ← knip: unused export
  export function buildProposalDraftsForRun  ← knip: unused export

From apps/memroos/src/lib/auth/types.ts:
  export type AuthUser  ← knip: unused type

From apps/memroos/src/lib/memory/policy-gate.ts:6:
  export type MemoryUseDecision  ← knip: unused type

From apps/memroos/src/lib/classification/types.ts:34:
  export type ClassificationReviewStatus  ← knip: unused type
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove dead security-path exports (D01-005) and verify L3 stubs</name>
  <files>
    apps/memroos/src/lib/auth/jwt.ts,
    apps/memroos/src/lib/auth/types.ts,
    apps/memroos/src/lib/seal/behavioral-runner.ts,
    apps/memroos/src/lib/seal/apply.ts,
    apps/memroos/src/lib/seal/reflection.ts,
    apps/memroos/src/lib/seal/behavioral-jobs.ts,
    apps/memroos/src/lib/seal/behavioral-sandbox.ts,
    apps/memroos/src/lib/seal/types.ts,
    apps/memroos/src/lib/memory/policy-gate.ts,
    apps/memroos/src/lib/classification/types.ts,
    apps/memroos/src/lib/l3/adapters/netsuite.ts,
    apps/memroos/src/lib/l3/adapters/salesforce.ts,
    apps/memroos/src/lib/l3/adapters/zendesk.ts
  </files>
  <read_first>
    Before touching ANY symbol, run:
      gitnexus_impact({target: "generateRefreshToken", direction: "upstream"})
      gitnexus_impact({target: "applyProposal", direction: "upstream"})
      gitnexus_impact({target: "listEvalJobs", direction: "upstream"})
      gitnexus_impact({target: "reflectOnTrace", direction: "upstream"})
      gitnexus_impact({target: "buildProposalDraftsForRun", direction: "upstream"})
    MCP fallback (if GitNexus unavailable):
      grep -rn "generateRefreshToken" apps/memroos/src/ --include="*.ts" --include="*.tsx" | grep -v "/__tests__/\|\.test\.\|\.spec\.\|\.d\.ts$"
      grep -rn "applyProposal" apps/memroos/src/ --include="*.ts" --include="*.tsx" | grep -v "/__tests__/\|\.test\.\|\.spec\.\|\.d\.ts$"
      (repeat for each export being removed)
    Read audit detail D01-005 in domain-architecture.md before any deletion.
  </read_first>
  <action>
    **Step 1 — Verify no callers (mandatory before deletion):**
    Run gitnexus_impact or grep fallback for each of the 11 D01-005 exports listed in read_first.
    If ANY export has a production caller (non-test), do NOT remove it — add a TODO comment and skip that symbol.

    **Step 2 — Remove dead exports from security paths:**
    - jwt.ts: Remove the `export function generateRefreshToken()` declaration entirely (line 53). Audit finding notes this may indicate an incomplete refresh-token flow — since Phase 82 auth hardening is complete and the function has zero callers, remove it with a git commit message that references the audit finding D01-005.
    - auth/types.ts: Remove `export type AuthUser` if 0 callers confirmed.
    - seal/types.ts: Remove `export type SealAuditEvent` if 0 callers confirmed.
    - seal/behavioral-jobs.ts: Remove `export type PromotionMetadata` if 0 callers confirmed.
    - seal/behavioral-sandbox.ts: Remove `export type SandboxToolCallResult` if 0 callers confirmed.
    - seal/behavioral-runner.ts: Remove `export` keyword from `listEvalJobs` (make it module-private) if 0 callers confirmed.
    - seal/apply.ts: Remove `export` keyword from `applyProposal` if 0 callers confirmed.
    - seal/reflection.ts: Remove `export` keywords from `reflectOnTrace` and `buildProposalDraftsForRun` if 0 callers confirmed.
    - memory/policy-gate.ts: Remove `export type MemoryUseDecision` if 0 callers confirmed.
    - classification/types.ts: Remove `export type ClassificationReviewStatus` if 0 callers confirmed.

    **Step 3 — Preserve L3 CRM adapters as intentional-future stubs:**
    Do NOT delete netsuite.ts, salesforce.ts, zendesk.ts. These are planned for ROADMAP backlog P3 (live Salesforce/Zendesk/NetSuite adapters). Add the following comment block at the top of each file (after existing imports):
    ```
    // INTENTIONAL_STUB: This adapter is pre-built for ROADMAP backlog P3 (live CRM vertical).
    // See .planning/ROADMAP.md "Backlog > P3 — Plan commercial/product/backend expansion".
    // Do not remove. It will be wired in a future phase.
    ```

    **Step 4 — TypeScript typecheck:**
    Run: cd apps/memroos && npx tsc --noEmit
    Fix any type errors introduced by export removal (if a type was used internally, keep the type but drop the `export`).
  </action>
  <verify>
    <automated>
      cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit 2>&1 | grep -v "/__tests__/\|\.test\.\|\.spec\." | grep -c "error TS" || true
      # Should output 0 (production path errors only)
    </automated>
  </verify>
  <done>
    All 11 D01-005 dead exports removed (or kept with documented reason if callers found).
    L3 CRM adapters contain INTENTIONAL_STUB comment.
    npx tsc --noEmit reports 0 errors outside __tests__ paths.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove 11 confirmed-dead non-L3 files (D01-006) and fix Python dead code (D01-007, D01-008)</name>
  <files>
    apps/memroos/src/components/flow/demo-mode.tsx,
    apps/memroos/src/components/flow/flow-canvas.tsx,
    apps/memroos/src/components/flow/flow-edge.tsx,
    apps/memroos/src/components/flow/flow-node.tsx,
    apps/memroos/src/components/layout/health-dot.tsx,
    apps/memroos/src/components/ui/tabs.tsx,
    apps/memroos/src/components/voice/useVoiceTranscript.ts,
    apps/memroos/src/scripts/generate-demo-video.mjs,
    services/knowledge-mcp/vulture_whitelist.py,
    services/memory/vulture_whitelist.py
  </files>
  <read_first>
    Before deleting UI component files, run gitnexus_impact on each:
      gitnexus_impact({target: "DemoMode", direction: "upstream"})
      gitnexus_impact({target: "FlowCanvas", direction: "upstream"})
      gitnexus_impact({target: "FlowEdge", direction: "upstream"})
      gitnexus_impact({target: "FlowNode", direction: "upstream"})
      gitnexus_impact({target: "HealthDot", direction: "upstream"})
      gitnexus_impact({target: "useVoiceTranscript", direction: "upstream"})
    MCP fallback:
      grep -rn "demo-mode\|flow-canvas\|flow-edge\|flow-node\|health-dot\|useVoiceTranscript" apps/memroos/src/ --include="*.ts" --include="*.tsx" | grep -v "/__tests__/\|\.test\.\|\.spec\.\|\.d\.ts$"
    Also run vulture on Python services to confirm genuine unused items:
      cd /Users/lcalderon/github/memroos && python3 -m vulture services/ --min-confidence 80 2>/dev/null | head -30
  </read_first>
  <action>
    **Step 1 — Verify and delete dead TypeScript/TSX files:**
    Run gitnexus_impact or grep fallback for each component. If zero inbound references confirmed:
    - Delete: components/flow/demo-mode.tsx, flow-canvas.tsx, flow-edge.tsx, flow-node.tsx
    - Delete: components/layout/health-dot.tsx
    - Delete: components/ui/tabs.tsx (knip-flagged; confirm shadcn/ui has a canonical replacement before removing — check if any other component imports this)
    - Delete: components/voice/useVoiceTranscript.ts
    - Delete: scripts/generate-demo-video.mjs
    If ANY file has a live caller, skip deletion and add a TODO comment instead.

    **Step 2 — Create Python vulture whitelists:**
    Create services/knowledge-mcp/vulture_whitelist.py with entries for all @mcp.tool(), @app.get(), @app.post(), @app.on_event decorator-registered handlers found in the knowledge-mcp service. Format:
      # vulture whitelist — MCP/FastAPI decorator-registered handlers are not dead code
      knowledge_search = knowledge_search  # noqa: F841 — @mcp.tool() registered
      (one entry per flagged handler)

    Create services/memory/vulture_whitelist.py similarly for mem0-server.py / mem0-queue.py handlers.
    For the 2-3 genuine unused vars (store.py:115 preview, mem0-queue.py:19 MAX_RETRIES, mem0-queue.py:94 row_factory): remove them if they are truly unused. Verify by reading each before deleting.

    **Step 3 — Run ruff auto-fix:**
    cd /Users/lcalderon/github/memroos && python3 -m ruff check services/ --fix
    This addresses D01-008 (50 errors, 29 auto-fixable unused imports in test files).

    **Step 4 — TypeScript build check:**
    cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit
    Fix any import errors from deleted files.
  </action>
  <verify>
    <automated>
      cd /Users/lcalderon/github/memroos && python3 -m ruff check services/ 2>&1 | grep -c "error" || true
      # Should be 0 or near-0 (only non-auto-fixable warnings remain)
      cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit 2>&1 | grep -v "/__tests__/\|\.test\.\|\.spec\." | grep -c "error TS" || true
      # Should be 0
    </automated>
  </verify>
  <done>
    11 confirmed-dead files removed (or documented with reason if callers found).
    vulture_whitelist.py created in knowledge-mcp and memory services.
    ruff check services/ reports 0 errors (down from 50).
    tsc --noEmit passes clean on production paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export surface → consumers | Removing exports could break dynamic imports not caught by static analysis |
| file deletion → build | Removing TSX files could break barrel exports or dynamic requires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01-01 | Tampering | Dead security exports (generateRefreshToken, SealAuditEvent) | mitigate | Run gitnexus_impact + grep fallback on each symbol before deletion; keep if any production caller found |
| T-112-01-02 | Tampering | L3 CRM adapter files | accept | Retained with INTENTIONAL_STUB comment per ROADMAP backlog P3 — not deleted |
| T-112-01-SC | Tampering | npm/pip/cargo installs | accept | This plan adds no new packages; vulture_whitelist.py uses standard Python — no install needed |
</threat_model>

<verification>
After both tasks complete:
1. `cd apps/memroos && npx knip --reporter json 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); files=[i for i in d.get('files',[]) if not any(x in i for x in ['l3/adapters/netsuite','l3/adapters/salesforce','l3/adapters/zendesk'])]; print('Unexpected unused files:',files)"` — should be empty list
2. `cd /Users/lcalderon/github/memroos && python3 -m ruff check services/ | grep -c "E\|F\|W"` — should be 0
3. `cd apps/memroos && npx tsc --noEmit 2>&1 | grep -v "/__tests__/\|\.test\.\|\.spec\." | grep "error TS"` — no output
4. `grep -r "INTENTIONAL_STUB" apps/memroos/src/lib/l3/adapters/` — should show 3 matches (netsuite, salesforce, zendesk)
</verification>

<success_criteria>
- D01-005: All 11 dead exports in security-sensitive paths removed (or explicitly retained with caller evidence)
- D01-006: 11 confirmed-dead TypeScript files removed (L3 CRM adapters preserved with INTENTIONAL_STUB comment)
- D01-007: Python vulture whitelists created; genuine unused vars removed
- D01-008: ruff auto-fix applied; 0 ruff errors remain
- TypeScript production build clean
</success_criteria>

<output>
Create `.planning/phases/112-architecture-cleanup/112-01-SUMMARY.md` when done.
</output>
