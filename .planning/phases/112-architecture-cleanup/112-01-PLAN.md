---
phase: 112-architecture-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memroos/src/lib/auth/jwt.ts
  - apps/memroos/src/lib/auth/types.ts
  - apps/memroos/src/lib/seal/behavioral-runner.ts
  - apps/memroos/src/lib/seal/apply.ts
  - apps/memroos/src/lib/seal/reflection.ts
  - apps/memroos/src/lib/seal/behavioral-jobs.ts
  - apps/memroos/src/lib/seal/behavioral-sandbox.ts
  - apps/memroos/src/lib/memory/policy-gate.ts
  - apps/memroos/src/lib/classification/types.ts
  - apps/memroos/src/lib/l3/adapters/netsuite.ts
  - apps/memroos/src/lib/l3/adapters/salesforce.ts
  - apps/memroos/src/lib/l3/adapters/zendesk.ts
  - services/knowledge-mcp/vulture_whitelist.py
  - services/memory/vulture_whitelist.py
  - apps/memroos/src/components/flow/demo-mode.tsx
  - apps/memroos/src/components/flow/flow-canvas.tsx
  - apps/memroos/src/components/flow/flow-edge.tsx
  - apps/memroos/src/components/flow/flow-node.tsx
  - apps/memroos/src/components/layout/health-dot.tsx
  - apps/memroos/src/components/ui/tabs.tsx
  - apps/memroos/src/components/voice/useVoiceTranscript.ts
  - apps/memroos/src/scripts/generate-demo-video.mjs
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "knip reports zero unused exports in auth/seal/policy-gate/classification paths (excluding seal/types.ts SealAuditEvent — handled in 112-02)"
    - "L3 CRM adapter files are preserved with an intentional-future stub comment"
    - "generateRefreshToken export is removed from jwt.ts (no callers exist)"
    - "ruff auto-fix applied; remaining non-auto-fixable errors documented, not silently ignored"
    - "Python vulture whitelist covers MCP/FastAPI decorator handlers; whitelist files excluded from ruff F821"
    - "10 confirmed-dead non-L3 TypeScript files are removed from the repo (seal/types.ts NOT in scope here)"
  artifacts:
    - path: "apps/memroos/src/lib/auth/jwt.ts"
      provides: "jwt utilities without the dead generateRefreshToken export"
    - path: "services/knowledge-mcp/vulture_whitelist.py"
      provides: "vulture whitelist for decorator-registered MCP tool handlers"
    - path: "services/memory/vulture_whitelist.py"
      provides: "vulture whitelist for decorator-registered FastAPI/mem0 handlers"
  key_links:
    - from: "apps/memroos/src/lib/l3/adapters/salesforce.ts"
      to: "ROADMAP backlog P3"
      via: "intentional-stub comment block"
      pattern: "INTENTIONAL_STUB"
---

<objective>
Remove confirmed-dead code (D01-005, D01-006, D01-007, D01-008) from auth, seal, policy-gate, classification, flow, and voice paths — while preserving intentional-future stubs and adding Python vulture whitelists for decorator-registered handlers.

NOTE: seal/types.ts is NOT modified in this plan. The SealAuditEvent removal is handled in 112-02 (which already edits seal/types.ts for the circular dep fix) to prevent Wave 1 file-ownership conflict.

Purpose: Reduce attack surface from dead security exports, improve bundle size, establish a clean knip/ruff baseline for Phase 113 test validation.

Output: Dead security exports removed; L3 CRM adapters retained with stub comment; ruff auto-fix applied; vulture whitelists created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lcalderon/github/memroos/.planning/PROJECT.md
@/Users/lcalderon/github/memroos/.planning/ROADMAP.md
@/Users/lcalderon/github/memroos/.planning/audit/domain-architecture.md
@/Users/lcalderon/github/memroos/.planning/audit/FINDINGS-INDEX.md

<interfaces>
From apps/memroos/src/lib/auth/jwt.ts (current exports):
  export function getSecret(): Uint8Array
  export async function signAccessToken(userId: string, role: UserRole): Promise<string>
  export async function verifyAccessToken(token: string): Promise<JwtPayload | null>
  export function generateRefreshToken(): string  ← TARGET: remove (no callers)

EXCLUDED from this plan (handled in 112-02):
  seal/types.ts: export type SealAuditEvent  ← removed in 112-02 Task 2

From apps/memroos/src/lib/seal/behavioral-jobs.ts:
  export type PromotionMetadata  ← knip: unused export

From apps/memroos/src/lib/seal/behavioral-sandbox.ts:
  export type SandboxToolCallResult  ← knip: unused export

From apps/memroos/src/lib/seal/behavioral-runner.ts:
  export function listEvalJobs  ← knip: unused export

From apps/memroos/src/lib/seal/apply.ts:
  export function applyProposal  ← knip: unused export (verify dynamic refs first)

From apps/memroos/src/lib/seal/reflection.ts:
  export function reflectOnTrace  ← knip: unused export
  export function buildProposalDraftsForRun  ← knip: unused export

From apps/memroos/src/lib/auth/types.ts:
  export type AuthUser  ← knip: unused type

From apps/memroos/src/lib/memory/policy-gate.ts:6:
  export type MemoryUseDecision  ← knip: unused type

From apps/memroos/src/lib/classification/types.ts:34:
  export type ClassificationReviewStatus  ← knip: unused type
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove dead security-path exports (D01-005) and preserve L3 stubs</name>
  <files>
    apps/memroos/src/lib/auth/jwt.ts,
    apps/memroos/src/lib/auth/types.ts,
    apps/memroos/src/lib/seal/behavioral-runner.ts,
    apps/memroos/src/lib/seal/apply.ts,
    apps/memroos/src/lib/seal/reflection.ts,
    apps/memroos/src/lib/seal/behavioral-jobs.ts,
    apps/memroos/src/lib/seal/behavioral-sandbox.ts,
    apps/memroos/src/lib/memory/policy-gate.ts,
    apps/memroos/src/lib/classification/types.ts,
    apps/memroos/src/lib/l3/adapters/netsuite.ts,
    apps/memroos/src/lib/l3/adapters/salesforce.ts,
    apps/memroos/src/lib/l3/adapters/zendesk.ts
  </files>
  <read_first>
    Before touching ANY symbol, run gitnexus_impact for each target export:
      gitnexus_impact({target: "generateRefreshToken", direction: "upstream"})
      gitnexus_impact({target: "applyProposal", direction: "upstream"})
      gitnexus_impact({target: "listEvalJobs", direction: "upstream"})
      gitnexus_impact({target: "reflectOnTrace", direction: "upstream"})
      gitnexus_impact({target: "buildProposalDraftsForRun", direction: "upstream"})
      gitnexus_impact({target: "PromotionMetadata", direction: "upstream"})
      gitnexus_impact({target: "SandboxToolCallResult", direction: "upstream"})
      gitnexus_impact({target: "AuthUser", direction: "upstream"})
      gitnexus_impact({target: "MemoryUseDecision", direction: "upstream"})
      gitnexus_impact({target: "ClassificationReviewStatus", direction: "upstream"})
    MCP fallback (if GitNexus unavailable — use path-only filter to avoid false negatives):
      grep -rn "generateRefreshToken" apps/memroos/src/ --include="*.ts" --include="*.tsx" | grep -v "/__tests__/\|\.test\.\|\.spec\.\|\.d\.ts$"
      (repeat pattern for each export, substituting the symbol name)
    Read audit detail D01-005 in domain-architecture.md before any deletion.
  </read_first>
  <action>
    **Step 1 — Verify no callers (mandatory before any change):**
    Run gitnexus_impact or grep fallback for each of the D01-005 exports listed above.
    If ANY export has a production caller (non-test, non-declaration file), do NOT remove it — add a TODO comment and skip that symbol.

    **Step 2 — Remove/reduce dead exports by symbol type:**

    For TYPES (if 0 callers confirmed): delete the entire type declaration.
      - auth/types.ts: Remove `export type AuthUser`
      - seal/behavioral-jobs.ts: Remove `export type PromotionMetadata`
      - seal/behavioral-sandbox.ts: Remove `export type SandboxToolCallResult`
      - memory/policy-gate.ts: Remove `export type MemoryUseDecision`
      - classification/types.ts: Remove `export type ClassificationReviewStatus`

    For FUNCTIONS (if 0 callers confirmed):
      - jwt.ts: Remove the entire `export function generateRefreshToken()` declaration (line 53). Zero callers verified; Phase 82 auth hardening is complete.
      - seal/behavioral-runner.ts: If `listEvalJobs` has zero callers ANYWHERE (including within its own module), delete the function. If it is used within the module but not exported, drop the `export` keyword only.
      - seal/apply.ts: Same rule for `applyProposal`.
      - seal/reflection.ts: Same rule for `reflectOnTrace` and `buildProposalDraftsForRun`.

    Decision rule: zero references anywhere → delete declaration; references within own module only → drop `export` keyword; references in other production modules → keep, add TODO.
    This avoids the noUnusedLocals TypeScript error that would arise from leaving a dead private function.

    **Step 3 — Preserve L3 CRM adapters as intentional-future stubs:**
    Do NOT delete netsuite.ts, salesforce.ts, zendesk.ts. These are planned for ROADMAP backlog P3.
    Add this comment block at the top of each file (after existing license/imports):
      // INTENTIONAL_STUB: This adapter is pre-built for ROADMAP backlog P3 (live CRM vertical).
      // See .planning/ROADMAP.md "Backlog > P3 — Plan commercial/product/backend expansion".
      // Do not remove. It will be wired in a future phase.

    **Step 4 — TypeScript typecheck:**
    Run: cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit
    Fix any type errors introduced by export/function removal.
  </action>
  <verify>
    <automated>
      cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit 2>&1 | grep -v "/__tests__/\|\.test\.\|\.spec\." | grep "error TS" || echo "clean"
      grep -rn "INTENTIONAL_STUB" /Users/lcalderon/github/memroos/apps/memroos/src/lib/l3/adapters/ | wc -l
      # Should be >= 3 (one per adapter file)
    </automated>
  </verify>
  <done>
    All D01-005 dead exports in auth/seal/policy-gate/classification removed or de-exported (excluding seal/types.ts SealAuditEvent — deferred to 112-02).
    L3 CRM adapters contain INTENTIONAL_STUB comment.
    tsc --noEmit passes clean on production paths.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove 10 confirmed-dead TypeScript files (D01-006) and fix Python dead code (D01-007, D01-008)</name>
  <files>
    apps/memroos/src/components/flow/demo-mode.tsx,
    apps/memroos/src/components/flow/flow-canvas.tsx,
    apps/memroos/src/components/flow/flow-edge.tsx,
    apps/memroos/src/components/flow/flow-node.tsx,
    apps/memroos/src/components/layout/health-dot.tsx,
    apps/memroos/src/components/ui/tabs.tsx,
    apps/memroos/src/components/voice/useVoiceTranscript.ts,
    apps/memroos/src/scripts/generate-demo-video.mjs,
    services/knowledge-mcp/vulture_whitelist.py,
    services/memory/vulture_whitelist.py
  </files>
  <read_first>
    Before deleting UI component files, run gitnexus_impact on each:
      gitnexus_impact({target: "DemoMode", direction: "upstream"})
      gitnexus_impact({target: "FlowCanvas", direction: "upstream"})
      gitnexus_impact({target: "FlowEdge", direction: "upstream"})
      gitnexus_impact({target: "FlowNode", direction: "upstream"})
      gitnexus_impact({target: "HealthDot", direction: "upstream"})
      gitnexus_impact({target: "useVoiceTranscript", direction: "upstream"})
    MCP fallback (path-only filter):
      grep -rn "demo-mode\|DemoMode\|flow-canvas\|FlowCanvas\|flow-edge\|FlowEdge\|flow-node\|FlowNode\|health-dot\|HealthDot\|useVoiceTranscript" apps/memroos/src/ --include="*.ts" --include="*.tsx" | grep -v "/__tests__/\|\.test\.\|\.spec\.\|\.d\.ts$" | grep -v "demo-mode\.tsx\|flow-canvas\.tsx\|flow-edge\.tsx\|flow-node\.tsx\|health-dot\.tsx\|useVoiceTranscript\.ts"
    Also run vulture to identify genuine unused Python vars (to separate from decorator false positives):
      cd /Users/lcalderon/github/memroos && python3 -m vulture services/ --min-confidence 80 2>/dev/null | head -40
  </read_first>
  <action>
    **Step 1 — Verify and delete dead TypeScript/TSX files:**
    Run gitnexus_impact or grep fallback for each component. If zero inbound references from other production files confirmed, delete:
    - components/flow/demo-mode.tsx
    - components/flow/flow-canvas.tsx
    - components/flow/flow-edge.tsx
    - components/flow/flow-node.tsx
    - components/layout/health-dot.tsx
    - components/ui/tabs.tsx (before deleting, verify that shadcn/ui provides an alternative Tabs component via @/components/ui/tabs from the shadcn registry — if tabs.tsx IS the shadcn canonical, do NOT delete)
    - components/voice/useVoiceTranscript.ts
    - scripts/generate-demo-video.mjs
    If ANY file has a live production caller, skip deletion and add a code comment instead.
    NOTE: seal/types.ts is NOT in scope for this task (handled in 112-02).

    **Step 2 — Create Python vulture whitelists:**
    Read the Python source files to identify decorator-registered functions that vulture flags as unused:
      cat services/knowledge-mcp/*.py | grep "@mcp.tool\|@app.get\|@app.post\|@app.on_event\|@router." -A1 | grep "def " | awk '{print $2}' | tr -d ':'

    Create services/knowledge-mcp/vulture_whitelist.py:
      # vulture whitelist — MCP/FastAPI decorator-registered handlers are not dead code.
      # Do NOT add this file to ruff's linting scope (add to ruff exclude below).
      # Format: assign function name to itself so vulture's static analysis marks it used.
      knowledge_search = knowledge_search  # @mcp.tool() registered
      (add one entry per flagged decorator-registered handler)

    Create services/memory/vulture_whitelist.py similarly for mem0-server.py / mem0-queue.py handlers.

    CRITICAL — prevent ruff F821 errors from whitelist files:
    Add a per-file ruff ignore to each whitelist file's first lines:
      # ruff: noqa: F821
    OR add the whitelist files to ruff's exclude in pyproject.toml or ruff.toml:
      exclude = ["**/vulture_whitelist.py"]

    For genuine unused vars (store.py:115 preview, mem0-queue.py:19 MAX_RETRIES, mem0-queue.py:94 row_factory):
    Read each file/line to confirm zero references, then delete if genuinely unused.

    **Step 3 — Run ruff auto-fix:**
    cd /Users/lcalderon/github/memroos && python3 -m ruff check services/ --fix
    This resolves the 29 auto-fixable errors (D01-008: unused imports in test files).

    Check remaining non-auto-fixable errors:
    cd /Users/lcalderon/github/memroos && python3 -m ruff check services/ 2>&1 | grep -v "^$" | head -30
    Document each non-auto-fixable error in the plan summary. Do NOT claim the gate passes
    if errors remain — instead, note them as accepted-residual with the error code and location.
    The accept threshold: only E501 (line-too-long) or D-series doc errors count as acceptable residual.
    Any F-series (undefined, unused, import) errors must be fixed.

    **Step 4 — TypeScript build check:**
    cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit
    Fix any import errors from deleted files.
  </action>
  <verify>
    <automated>
      cd /Users/lcalderon/github/memroos && python3 -m ruff check services/ --exclude "**/vulture_whitelist.py" 2>&1 | grep "^services" | grep -c "F\|E[0-9]" || echo "0"
      # Should be 0 (F-series and E-series errors resolved; D/W-series acceptable)
      cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit 2>&1 | grep -v "/__tests__/\|\.test\.\|\.spec\." | grep "error TS" || echo "clean"
    </automated>
  </verify>
  <done>
    Confirmed-dead TypeScript files removed (seal/types.ts excluded — handled in 112-02).
    vulture_whitelist.py created in knowledge-mcp and memory services; excluded from ruff F821.
    ruff auto-fix applied; remaining non-auto-fixable errors documented in summary (not silently passed).
    tsc --noEmit passes clean on production paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export surface → consumers | Removing exports could break dynamic imports not caught by static analysis |
| file deletion → build | Removing TSX files could break barrel exports or dynamic requires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01-01 | Tampering | Dead security exports (generateRefreshToken, etc.) | mitigate | Run gitnexus_impact + grep fallback on each symbol before deletion; keep if any production caller found; use path-only filter to avoid false negatives |
| T-112-01-02 | Tampering | L3 CRM adapter files | accept | Retained with INTENTIONAL_STUB comment per ROADMAP backlog P3 — not deleted |
| T-112-01-03 | Tampering | vulture_whitelist.py ruff F821 | mitigate | Add ruff: noqa: F821 header or exclude in ruff.toml to prevent whitelist from generating new ruff errors |
| T-112-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; vulture_whitelist.py uses standard Python assignments — no install needed |
</threat_model>

<verification>
After both tasks complete:
1. `grep -rn "INTENTIONAL_STUB" /Users/lcalderon/github/memroos/apps/memroos/src/lib/l3/adapters/` — shows 3 matches
2. `cd /Users/lcalderon/github/memroos && python3 -m ruff check services/ --exclude "**/vulture_whitelist.py" 2>&1 | grep "^services" | grep "F"` — no F-series errors
3. `cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit 2>&1 | grep -v "/__tests__/\|\.test\.\|\.spec\." | grep "error TS"` — no output
4. `ls /Users/lcalderon/github/memroos/services/knowledge-mcp/vulture_whitelist.py /Users/lcalderon/github/memroos/services/memory/vulture_whitelist.py` — both exist
5. `grep -n "generateRefreshToken" /Users/lcalderon/github/memroos/apps/memroos/src/lib/auth/jwt.ts | grep -v "/__tests__/"` — no output (removed)
</verification>

<success_criteria>
- D01-005: Dead exports in auth/seal/policy-gate/classification paths removed (SealAuditEvent delegated to 112-02)
- D01-006: Confirmed-dead TypeScript files removed; L3 CRM adapters preserved with INTENTIONAL_STUB comment
- D01-007: Python vulture whitelists created; genuine unused vars removed; whitelists excluded from ruff F821
- D01-008: ruff auto-fix applied; remaining non-auto-fixable errors documented (not silently passed)
- TypeScript production build clean
</success_criteria>

<output>
Create `.planning/phases/112-architecture-cleanup/112-01-SUMMARY.md` when done.
</output>
