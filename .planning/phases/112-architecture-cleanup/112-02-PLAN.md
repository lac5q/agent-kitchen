---
phase: 112-architecture-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memroos/src/lib/memory/backends.ts
  - apps/memroos/src/lib/memory/registry.ts
  - apps/memroos/src/lib/memory/adapter.ts
  - apps/memroos/src/lib/memory/registry-contract.ts
  - apps/memroos/src/lib/seal/proposal-types.ts
  - apps/memroos/src/lib/seal/types.ts
  - apps/memroos/src/lib/seal/proposal-registry.ts
  - apps/memroos/src/lib/context-sources.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "madge reports zero circular imports in apps/memroos/src/lib/memory/"
    - "madge reports zero circular imports in apps/memroos/src/lib/seal/"
    - "context-sources.ts defaultHasTool() no longer uses shell:true interpolation"
    - "Memory subsystem types still compile and tests pass"
    - "context-sources execFileSync fix is documented with accepted-risk rationale"
    - "SealAuditEvent export removed from seal/types.ts (ARCH-01 coverage for this file)"
  artifacts:
    - path: "apps/memroos/src/lib/memory/registry-contract.ts"
      provides: "Shared MemoryTierHealth type to break the value-import cycle in memory subsystem"
      exports: ["MemoryTierHealth"]
    - path: "apps/memroos/src/lib/seal/proposal-types.ts"
      provides: "Leaf type module for ProposalType (breaks seal circular)"
      exports: ["ProposalType"]
    - path: "apps/memroos/src/lib/context-sources.ts"
      provides: "Shell-free defaultHasTool using positional argv"
  key_links:
    - from: "apps/memroos/src/lib/memory/backends.ts"
      to: "apps/memroos/src/lib/memory/registry-contract.ts"
      via: "import type MemoryTierHealth (replaces import from adapter.ts closure)"
      pattern: "registry-contract"
    - from: "apps/memroos/src/lib/seal/types.ts"
      to: "apps/memroos/src/lib/seal/proposal-types.ts"
      via: "import type ProposalType from leaf module"
      pattern: "proposal-types"
---

<objective>
Break the runtime circular import in the memory subsystem (D01-002) by extracting a registry-contract type module; fix the type-only seal circular (D01-004) by hoisting ProposalType to a leaf module; while editing seal/types.ts, also remove the dead SealAuditEvent export (D01-005 — ARCH-01, assigned here to avoid Wave 1 file collision with 112-01); harden context-sources.ts defaultHasTool() against shell arg injection (D01-009).

Purpose: Eliminate init-order fragility in the memory subsystem, clean the seal type graph, document/fix the sole shell-mode exec pattern. SealAuditEvent removal is folded in because this plan already edits seal/types.ts — prevents parallel write conflicts.

Output: Zero madge cycles in memory/ and seal/; shell-free PATH probe in context-sources.ts; SealAuditEvent dead export removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lcalderon/github/memroos/.planning/PROJECT.md
@/Users/lcalderon/github/memroos/.planning/ROADMAP.md
@/Users/lcalderon/github/memroos/.planning/audit/domain-architecture.md

<interfaces>
Current circular (D01-002):
  backends.ts:4 → value import: getAdapters, registerAdapter from registry.ts
  registry.ts:9 → import type MemoryAdapter from adapter.ts
  adapter.ts:10 → import type MemoryTierHealth from backends.ts  (creates cycle)

Current exports from registry.ts:
  export function registerAdapter(adapter: MemoryAdapter): void
  export function getAdapters(tier: MemoryTier): MemoryAdapter[]
  export function clearRegistry(): void

Current exports from adapter.ts:
  export type MemoryCapability = ...
  export interface MemorySearchResult { ... }
  export interface MemoryAdapter { ... }

Current exports from backends.ts:
  export interface MemoryTierHealth { status; latencyMs; docCount; queuedWrites; vectorStore }
  export class VectorMemoryAdapter implements MemoryAdapter
  export class GraphMemoryAdapter implements MemoryAdapter

Seal circular (D01-004):
  seal/types.ts:2 → import type ProposalType from "./proposal-registry"
  seal/proposal-registry.ts:1 → import type ProposalDraft from "./types"
  (both edges are import type — compile-time only, but structural smell)

seal/types.ts also contains (D01-005, ARCH-01):
  export type SealAuditEvent  ← knip: unused export — remove in this plan's Task 2

context-sources.ts D01-009:
  Line 119: execFileSync("command", ["-v", tool], { shell: "/bin/sh", stdio: "ignore" })
  Fix: use execFileSync("sh", ["-c", 'command -v "$1" >/dev/null 2>&1', "sh", tool])
  tool source: source.requiredTools[] ← JSON config (operator-controlled, not request-reachable)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Break runtime memory circular — extract registry-contract.ts (D01-002)</name>
  <files>
    apps/memroos/src/lib/memory/registry-contract.ts,
    apps/memroos/src/lib/memory/backends.ts,
    apps/memroos/src/lib/memory/registry.ts,
    apps/memroos/src/lib/memory/adapter.ts
  </files>
  <read_first>
    Run gitnexus_impact before editing:
      gitnexus_impact({target: "registerAdapter", direction: "upstream"})
      gitnexus_impact({target: "getAdapters", direction: "upstream"})
      gitnexus_impact({target: "MemoryTierHealth", direction: "upstream"})
    MCP fallback (path-only filter):
      grep -rn "registerAdapter\|getAdapters\|MemoryTierHealth" apps/memroos/src/ --include="*.ts" --include="*.tsx" | grep -v "/__tests__/\|\.test\.\|\.spec\.\|\.d\.ts$"

    Read the full content of these files before editing (single pass each):
      apps/memroos/src/lib/memory/adapter.ts
      apps/memroos/src/lib/memory/registry.ts
      apps/memroos/src/lib/memory/backends.ts
  </read_first>
  <action>
    The runtime circular is:
      backends.ts:4 → value imports getAdapters/registerAdapter from registry.ts
      registry.ts:9 → import type MemoryAdapter from adapter.ts
      adapter.ts:10 → import type MemoryTierHealth from backends.ts  ← closes cycle

    Fix: move MemoryTierHealth out of backends.ts into a new leaf module (registry-contract.ts) so adapter.ts no longer imports from backends.ts.

    **Step 1 — Create apps/memroos/src/lib/memory/registry-contract.ts:**
    This is a pure-types leaf module with no imports from siblings. Read backends.ts to get the exact MemoryTierHealth interface shape, then copy it here verbatim:
      // registry-contract.ts — leaf type module; no imports from memory siblings.
      // Prevents circular: adapter.ts → backends.ts → registry.ts → adapter.ts
      export interface MemoryTierHealth {
        // (copy exact fields from backends.ts — do not guess the shape)
      }

    **Step 2 — Update adapter.ts:**
    Change: import type { MemoryTierHealth } from "./backends"
    To:     import type { MemoryTierHealth } from "./registry-contract"
    (This breaks the adapter.ts → backends.ts edge.)

    **Step 3 — Update registry.ts:**
    If registry.ts imports MemoryTierHealth from backends.ts or adapter.ts, update to registry-contract.ts.

    **Step 4 — Update backends.ts:**
    Remove the MemoryTierHealth interface definition.
    Add: import type { MemoryTierHealth } from "./registry-contract"
    All existing class implementations and exports that use MemoryTierHealth remain in backends.ts.

    **Step 5 — Verify new cycle-free dependency graph:**
      registry-contract.ts  (no imports from siblings)
      ↑
      adapter.ts (imports type from registry-contract.ts only)
      ↑
      registry.ts (imports type from adapter.ts)
      ↑
      backends.ts (imports value from registry.ts; imports type from registry-contract.ts)
  </action>
  <verify>
    <automated>
      cd /Users/lcalderon/github/memroos && npx madge --circular --extensions ts,tsx apps/memroos/src/lib/memory/ 2>/dev/null | grep -c "Circular" || echo "0"
      # Should output 0
      cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit 2>&1 | grep -v "/__tests__/\|\.test\.\|\.spec\." | grep "error TS" || echo "clean"
    </automated>
  </verify>
  <done>
    madge reports 0 circular imports in apps/memroos/src/lib/memory/.
    registry-contract.ts exists with MemoryTierHealth export.
    adapter.ts, backends.ts (re-import), registry.ts all reference registry-contract.ts for MemoryTierHealth.
    TypeScript compiles clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix seal type-only circular (D01-004), remove SealAuditEvent (D01-005), harden execFileSync (D01-009)</name>
  <files>
    apps/memroos/src/lib/seal/proposal-types.ts,
    apps/memroos/src/lib/seal/types.ts,
    apps/memroos/src/lib/seal/proposal-registry.ts,
    apps/memroos/src/lib/context-sources.ts
  </files>
  <read_first>
    Run gitnexus_impact before editing:
      gitnexus_impact({target: "ProposalType", direction: "upstream"})
      gitnexus_impact({target: "ProposalDraft", direction: "upstream"})
      gitnexus_impact({target: "SealAuditEvent", direction: "upstream"})
      gitnexus_impact({target: "defaultHasTool", direction: "upstream"})
    MCP fallback (path-only filter):
      grep -rn "ProposalType\|ProposalDraft\|SealAuditEvent" apps/memroos/src/ --include="*.ts" --include="*.tsx" | grep -v "/__tests__/\|\.test\.\|\.spec\.\|\.d\.ts$" | head -30

    Read the full content of:
      apps/memroos/src/lib/seal/types.ts
      apps/memroos/src/lib/seal/proposal-registry.ts
      apps/memroos/src/lib/context-sources.ts (lines 110-130 for the execFileSync context)
  </read_first>
  <action>
    **Seal circular fix (D01-004):**

    The cycle: types.ts imports ProposalType from proposal-registry.ts, and proposal-registry.ts imports ProposalDraft from types.ts.

    Step 1 — Create apps/memroos/src/lib/seal/proposal-types.ts (leaf module, no imports from siblings):
    ProposalType = keyof typeof PROPOSAL_TYPES. Rather than duplicating the PROPOSAL_TYPES object, read proposal-registry.ts to extract the actual key union and define it as a string literal type in proposal-types.ts:
      // proposal-types.ts — leaf type module; no imports from seal siblings.
      // Breaks circular: types.ts <-> proposal-registry.ts
      export type ProposalType =
        | "skill_revision"
        | "instruction_change"
        | "config_change"
        | "memory_policy"
        | "agent_config"
        | ... ; // copy exact keys from PROPOSAL_TYPES in proposal-registry.ts

    Step 2 — Update types.ts:
    Change: import type { ProposalType } from "./proposal-registry"
    To:     import type { ProposalType } from "./proposal-types"
    (No longer imports from proposal-registry.ts — cycle broken.)

    Step 3 — Update proposal-registry.ts:
    - Remove: import type { ProposalDraft } from "./types"
    - Add: import type { ProposalType } from "./proposal-types"
    - Inline the ProposalDraft type locally in proposal-registry.ts, or re-export it from proposal-types.ts if both files need it.
    - Update `PROPOSAL_TYPES` const to use `ProposalType` from the leaf module if needed.

    Step 4 — Run grep to update all consumers of ProposalType:
      grep -rn "from.*proposal-registry.*ProposalType\|ProposalType.*from.*types" apps/memroos/src/lib/seal/ --include="*.ts"
    Update each import to use "./proposal-types".

    **SealAuditEvent dead export removal (D01-005, ARCH-01 — folded here to avoid file conflict):**

    Step 5 — In seal/types.ts, after the circular fix:
    Run gitnexus_impact({target: "SealAuditEvent", direction: "upstream"}) or grep fallback.
    If 0 production callers confirmed: remove the `export type SealAuditEvent` declaration entirely.
    If callers exist: drop `export` keyword only (make module-private), or keep and add TODO.

    **context-sources.ts shell fix (D01-009):**

    Step 6 — In defaultHasTool(tool: string): boolean (line 117-121):
    Replace:
      execFileSync("command", ["-v", tool], { shell: "/bin/sh", stdio: "ignore" });
    With:
      execFileSync("sh", ["-c", 'command -v "$1" >/dev/null 2>&1', "sh", tool], { stdio: "ignore" });
    This passes tool as positional arg ($1), not interpolated into the shell command string.

    Add comment above the function:
      // D01-009 accepted-risk rationale: `tool` originates from source.requiredTools[] in operator-controlled
      // JSON config, not from any HTTP request. Fixed to positional args (not shell-interpolated) as defense-in-depth.
  </action>
  <verify>
    <automated>
      cd /Users/lcalderon/github/memroos && npx madge --circular --extensions ts,tsx apps/memroos/src/lib/seal/ 2>/dev/null | grep -c "Circular" || echo "0"
      # Should output 0
      grep -n 'shell.*"/bin/sh"' /Users/lcalderon/github/memroos/apps/memroos/src/lib/context-sources.ts | grep -v "/__tests__/\|\.test\."
      # Should output nothing (shell interpolation removed)
      grep -n "SealAuditEvent" /Users/lcalderon/github/memroos/apps/memroos/src/lib/seal/types.ts | grep "export"
      # Should output nothing (removed or de-exported)
      cd /Users/lcalderon/github/memroos/apps/memroos && npx tsc --noEmit 2>&1 | grep -v "/__tests__/\|\.test\.\|\.spec\." | grep "error TS" || echo "clean"
    </automated>
  </verify>
  <done>
    madge reports 0 circular imports in apps/memroos/src/lib/seal/.
    seal/proposal-types.ts exists as a leaf type module with ProposalType.
    seal/types.ts no longer exports SealAuditEvent (or it has 0 production callers documented).
    context-sources.ts defaultHasTool uses positional argv with D01-009 rationale comment.
    TypeScript compiles clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory registry → adapter callers | Changing import paths could introduce init-order bugs if lazy initialization is assumed |
| context-sources.ts → shell exec | Shell form was config-controlled but fragile; positional argv removes shell interpolation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02-01 | Tampering | memory circular dep fix | mitigate | Run madge --circular to verify 0 cycles; run tsc --noEmit to confirm no init-order regressions |
| T-112-02-02 | Elevation of Privilege | context-sources execFileSync shell injection | mitigate | Replace shell:true with positional argv; D01-009 rationale comment added; tool still config-only |
| T-112-02-03 | Tampering | SealAuditEvent removal from seal/types.ts | mitigate | Run gitnexus_impact + grep before removal; keep if callers found |
| T-112-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
1. `cd /Users/lcalderon/github/memroos && npx madge --circular --extensions ts,tsx apps/memroos/src/lib/memory/ apps/memroos/src/lib/seal/ 2>/dev/null` — no output (no cycles)
2. `grep -n 'shell.*"/bin/sh"' apps/memroos/src/lib/context-sources.ts` — no matches
3. `grep -n 'D01-009' apps/memroos/src/lib/context-sources.ts` — shows rationale comment
4. `cd apps/memroos && npx tsc --noEmit 2>&1 | grep -v "/__tests__/\|\.test\.\|\.spec\." | grep "error TS"` — no output
5. `ls apps/memroos/src/lib/memory/registry-contract.ts apps/memroos/src/lib/seal/proposal-types.ts` — both exist
6. `grep -n "export.*SealAuditEvent" apps/memroos/src/lib/seal/types.ts` — no output (removed)
</verification>

<success_criteria>
- D01-002: Runtime circular import in memory subsystem resolved; 0 madge cycles in memory/
- D01-004: Type-only circular in seal resolved; 0 madge cycles in seal/
- D01-005 (partial): SealAuditEvent dead export removed from seal/types.ts (folded here to prevent Wave 1 file conflict)
- D01-009: defaultHasTool uses positional argv; D01-009 rationale documented in source
- No TypeScript errors introduced
</success_criteria>

<output>
Create `.planning/phases/112-architecture-cleanup/112-02-SUMMARY.md` when done.
</output>
