---
phase: 04-flow-diagram-upgrade
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
  - FLOW-05
  - FLOW-06
  - FLOW-07
must_haves:
  truths:
    - "All 7 FLOW requirements are visually verified on the running Flow page"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint — human confirms all 7 FLOW requirements are met on the live Flow page.

Purpose: Plans 01 and 02 are fully automated, but the core value of this phase is visual: non-overlapping layout, readable labels, correct edge colors, and proper panel behavior. A human must verify these before the phase is marked complete.

Output: Human approval or issue list for iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-flow-diagram-upgrade/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of all FLOW requirements</name>
  <files>none</files>
  <action>
Build and start the server, then present the verification checklist to the user.

Run: `cd /Users/lcalderon/github/agent-kitchen && npm run build && npm start -- --port 3002`

Then present the how-to-verify checklist below and wait for human approval.

**FLOW-01 — Labels readable:**
1. Open http://localhost:3002/flow in a browser at 1280px+ width
2. Confirm all node labels are fully visible — especially "Knowledge Curator", "User / Telegram", "Gateways"
3. Labels should wrap to 2 lines at 90px width, NOT be cut off with ellipsis

**FLOW-04 — No overlap:**
4. Confirm 4 distinct rows of nodes with clear vertical separation
5. No nodes overlap horizontally within any row
6. Canvas height should be taller than before (720px)

**FLOW-05 — Knowledge Curator node:**
7. Confirm node with emoji appears in Row 4 (bottom row) between LLM Wiki and Obsidian
8. Subtitle shows "nightly · curator"
9. Click the node — panel should show Stats with "Schedule: nightly 2am" and "Steps: 4"
10. Confirm 4 outgoing edges: to GitNexus (green), LLM Wiki (green), mem0 (blue), QMD (green)

**FLOW-07 — Obsidian node:**
11. Confirm node with notebook emoji appears rightmost in Row 4
12. Subtitle shows "knowledge vault"
13. Confirm 3 incoming edges: from QMD (green), LLM Wiki (green), Knowledge Curator (green)
14. Node border should be green (status: active)

**FLOW-06 — New data-flow edges:**
15. Confirm edge from mem0 to QMD (blue/sky color, memory flow)
16. Confirm edge from LLM Wiki to QMD (green, knowledge flow)

**FLOW-02 — Heartbeat panel:**
17. Click any agent node (e.g., Alba) — panel opens
18. Panel subtitle should read "Node Activity" (since no HEARTBEAT_STATE.md files exist yet)
19. No "Last State" section should appear (graceful degradation)
20. No loading spinner stuck permanently

**FLOW-03 — Activity feed cleanup:**
21. Scroll to the "Live Activity" section below the canvas
22. Confirm event messages do NOT contain === or --- or raw ISO timestamps
23. Messages should be clean, readable text

**Quick API checks (optional):**
```
curl -s http://localhost:3002/api/heartbeat?agent=alba | python3 -m json.tool
curl -s "http://localhost:3002/api/heartbeat?agent=../etc/passwd"
```
  </action>
  <verify>
    <automated>cd /Users/lcalderon/github/agent-kitchen && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms all 7 FLOW requirements pass visual inspection — types "approved" or describes issues</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan — no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No code changes in this plan — threat model covered by Plans 01 and 02 |
</threat_model>

<verification>
Human visual inspection of all 7 FLOW requirements on the live /flow page.
</verification>

<success_criteria>
- Human confirms all 7 FLOW requirements pass visual inspection
- No layout overlap, no truncated labels, correct edge colors
- Heartbeat graceful degradation confirmed
- Activity feed is clean of log noise
</success_criteria>

<output>
After completion, create `.planning/phases/04-flow-diagram-upgrade/04-03-SUMMARY.md`
</output>
