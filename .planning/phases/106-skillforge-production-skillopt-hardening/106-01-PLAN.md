---
phase: 106
plan: "01"
status: complete
completed: "2026-06-04"
requirements:
  complete: [SKILLOPT-HARDEN-02, SKILLOPT-HARDEN-03, SKILLOPT-HARDEN-04, SKILLOPT-HARDEN-05]
  partial: [SKILLOPT-HARDEN-01]
---

# Phase 106 Plan 01 - SkillForge Production SkillOpt Hardening Pass

## Goal

Harden SkillForge's production SkillOpt path enough for governed proposal generation, schema-level traceability, typed edit operations, audit receipts, and fail-closed held-out evidence handling, while keeping the true sandbox-backed behavioral scorer as explicit remaining work.

## Scope

- Consolidate proposal generation so worker, approval, and export flows share one production path.
- Store edit hash, split ids, baseline W, validation W, held-out W, and evaluator receipts as first-class proposal fields.
- Represent proposed skill edits as typed bounded operations before rendering unified diffs.
- Enforce textual-learning-rate limits, forbidden-section protections, stable hashes, and rejected-edit preservation.
- Replace fixed or heuristic held-out pass-rate assumptions with deterministic held-out evidence that fails closed when evidence is missing.
- Keep the non-simulated behavioral sandbox scorer out of scope for this pass because the Phase 94 behavioral A/B helper still contains simulated/random scoring.

## Verification Plan

- Run focused SkillForge Vitest coverage for edit generation, eval gate, proposal, operator approval, and worker flows.
- Run repository typecheck.
- Run `git diff --check`.
- Record remaining debt for `SKILLOPT-HARDEN-01` in `106-02-PLAN.md`.

## Result

Plan 01 is complete as a partial phase hardening pass. Phase 106 remains in progress until Plan 02 replaces simulated behavioral scoring with a real sandbox scorer.
