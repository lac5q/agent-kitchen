---
phase: 201-invite-multi-harness-agent-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memroos/src/lib/public-base-url.ts
  - apps/memroos/src/lib/__tests__/public-base-url.test.ts
  - apps/memroos/src/lib/agent-onboarding.ts
  - apps/memroos/src/lib/agent-registry.ts
  - apps/memroos/src/types/index.ts
  - apps/memroos/src/lib/ui-constants.ts
  - apps/memroos/src/lib/invite-email-draft.ts
  - apps/memroos/src/lib/__tests__/invite-email-draft.test.ts
  - apps/memroos/src/app/api/onboarding/register/route.ts
  - apps/memroos/src/app/api/onboarding/bootstrap/route.ts
  - apps/memroos/src/app/api/onboarding/__tests__/route.test.ts
  - apps/memroos/src/app/api/auth/register/route.ts
  - apps/memroos/src/app/api/auth/__tests__/session-cookies.test.ts
  - apps/memroos/src/app/invite/[token]/page.tsx
  - apps/memroos/src/app/team/page.tsx
autonomous: true
requirements:
  - INVBOOT-01
  - INVBOOT-02
  - INVBOOT-03
  - INVBOOT-04
  - INVBOOT-05
  - INVBOOT-06
must_haves:
  truths:
    - "After invitee account create, the invite page shows a Connect your agents step instead of jumping straight to login"
    - "Invitee can multi-select harnesses and receive one numbered copy-paste command per harness"
    - "Minted commands use a public MemRoOS base URL (never localhost when a public host or non-localhost env is known)"
    - "Agents registered via bootstrap commands persist registered_agents.owner_id = the invitee users.id"
    - "Team invite success panel shows a copyable 3-step email draft that includes the invite URL"
    - "Automated tests cover bootstrap mint, owner_id persistence, public URL resolution, and easy-copy presence"
  artifacts:
    - path: "apps/memroos/src/lib/public-base-url.ts"
      provides: "resolvePublicMemroosUrl(request) per D-11"
      exports: ["resolvePublicMemroosUrl"]
    - path: "apps/memroos/src/app/api/onboarding/bootstrap/route.ts"
      provides: "Authenticated multi-platform command mint"
      exports: ["POST"]
    - path: "apps/memroos/src/app/invite/[token]/page.tsx"
      provides: "Option A post-register Connect → commands → done step machine"
    - path: "apps/memroos/src/lib/invite-email-draft.ts"
      provides: "Team copyable 3-step email draft builder"
      exports: ["buildInviteEmailDraft"]
    - path: "apps/memroos/src/lib/agent-registry.ts"
      provides: "Optional owner_id persistence on registerAgent"
      contains: "owner_id"
  key_links:
    - from: "apps/memroos/src/app/invite/[token]/page.tsx"
      to: "/api/onboarding/bootstrap"
      via: "Authorization Bearer accessToken after register"
      pattern: "onboarding/bootstrap"
    - from: "apps/memroos/src/app/api/onboarding/bootstrap/route.ts"
      to: "createAgentOnboardingToken"
      via: "ownerUserId: session.userId, ttlSeconds: 3600"
      pattern: "ownerUserId"
    - from: "apps/memroos/src/app/api/onboarding/register/route.ts"
      to: "registerAgent"
      via: "ownerId from verified.payload.ownerUserId only (never request body)"
      pattern: "ownerId:.*ownerUserId"
    - from: "apps/memroos/src/app/team/page.tsx"
      to: "buildInviteEmailDraft"
      via: "inviteUrl after generate success"
      pattern: "buildInviteEmailDraft|email draft|Three easy steps"
---

<objective>
Ship the locked Option A invitee journey: account create → Connect agents (multi-harness) → one public-URL command per harness → done, with agents owned by the new user, plus a Team copyable email draft (no SendGrid).

Purpose: Close the gap between human team invites and agent onboarding so Eric-style onboarding is one continuous, jargon-light flow.
Output: Working bootstrap API, ownership-wired register path, invite Connect UX, Team email draft, and fast+slow tests for INVBOOT-01..06.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/201-invite-multi-harness-agent-bootstrap/201-CONTEXT.md
@.planning/phases/201-invite-multi-harness-agent-bootstrap/201-RESEARCH.md

# Prior systems this phase stitches (do not reinvent)
@apps/memroos/src/lib/agent-onboarding.ts
@apps/memroos/src/app/api/onboarding/invite/route.ts
@apps/memroos/src/app/api/onboarding/register/route.ts
@apps/memroos/src/lib/agent-registry.ts
@apps/memroos/src/app/api/auth/register/route.ts
@apps/memroos/src/app/api/auth/login/route.ts
@apps/memroos/src/app/invite/[token]/page.tsx
@apps/memroos/src/app/team/page.tsx
@apps/memroos/src/lib/ui-constants.ts
@apps/memroos/src/app/api/onboarding/__tests__/route.test.ts
@apps/memroos/src/app/api/auth/__tests__/session-cookies.test.ts

<interfaces>
<!-- Contracts the executor must extend. Use these directly — avoid scavenger-hunt exploration. -->

From apps/memroos/src/lib/agent-onboarding.ts (extend with optional ownerUserId):
```typescript
export interface AgentOnboardingTokenPayload {
  version: 1;
  exp: number;
  memroosUrl: string;
  mcpUrl: string;
  allowedAgentIds?: string[];
  defaultPlatform?: AgentPlatform;
  defaultProtocol?: AgentProtocol;
  capabilities?: RegisteredAgentCapability[];
  nonce: string;
  // ADD: ownerUserId?: string;
}
export interface CreateAgentOnboardingTokenInput {
  memroosUrl: string;
  mcpUrl?: string;
  ttlSeconds?: number;
  allowedAgentIds?: string[];
  defaultPlatform?: AgentPlatform;
  defaultProtocol?: AgentProtocol;
  capabilities?: RegisteredAgentCapability[];
  // ADD: ownerUserId?: string;
}
export function createAgentOnboardingToken(input: CreateAgentOnboardingTokenInput): {
  token: string;
  payload: AgentOnboardingTokenPayload;
};
export function verifyAgentOnboardingToken(token: string): VerifiedOnboardingToken | RejectedOnboardingToken;
export function shellQuote(value: string): string;
```

From apps/memroos/src/types/index.ts (extend):
```typescript
export interface RegisterAgentInput {
  id: string;
  name: string;
  role: string;
  platform: AgentPlatform;
  protocol: AgentProtocol;
  // ...existing optional fields...
  issueApiKey?: boolean;
  // ADD: ownerId?: string;
}
export interface RegisteredAgent extends Agent {
  // ...existing fields...
  // ADD optional: ownerId?: string | null;
}
```

From apps/memroos/src/app/api/onboarding/invite/route.ts (reuse platform whitelist + command shape; do NOT copy operator-only auth):
```typescript
const PLATFORMS = new Set(["cursor", "claude", "cline", "codex", "qwen", "pi", "gemini", "opencode", "zcode", "hermes", "openclaw", "chatgpt", "grok", "droid"]);
// Operator invite uses requireRole(..., "operator") — bootstrap must NOT.
```

From apps/memroos/src/app/api/auth/login/route.ts (mirror cookie pattern on register):
```typescript
// After signAccessToken: insert refresh token row, then:
// Set-Cookie memroos_refresh (HttpOnly) + access_token (HttpOnly) with isHttpsRequest Secure flag
// Keep returning JSON body accessToken for same-page Bearer bootstrap (D-07)
```

From apps/memroos/src/lib/ui-constants.ts:
```typescript
export const PLATFORM_LABELS: Record<string, string> = {
  claude: "Claude", // CHANGE to "Claude Code" per D-05
  // ... Cursor, Codex, Hermes, OpenClaw, Pi, Gemini, Qwen, Droid, Cline, ChatGPT, Grok, OpenCode, ZCode already present
};
```

Discretion locked by research (honor unless blocked):
- Route: POST /api/onboarding/bootstrap
- Welcome: step state on /invite/[token] (not new /welcome)
- Agent ids: `{userId.slice(0,8)}-{platform}` + allowedAgentIds
- Skip for now: yes
- Bootstrap TTL: 60m; operator invite default stays 15m
- Register Set-Cookie: yes (login parity)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Public URL + ownership token/registry plumbing</name>
  <goal>INVBOOT-03 + INVBOOT-04 foundation — resolve public base URLs and persist owner_id from signed onboarding tokens only.</goal>
  <files>
    apps/memroos/src/lib/public-base-url.ts,
    apps/memroos/src/lib/__tests__/public-base-url.test.ts,
    apps/memroos/src/lib/agent-onboarding.ts,
    apps/memroos/src/types/index.ts,
    apps/memroos/src/lib/agent-registry.ts,
    apps/memroos/src/app/api/onboarding/register/route.ts,
    apps/memroos/src/lib/ui-constants.ts
  </files>
  <behavior>
    - resolvePublicMemroosUrl prefers MEMROOS_PUBLIC_BASE_URL / MEMROOS_APP_URL / MEMROOS_BASE_URL when set and NOT localhost/127.0.0.1/::1
    - When env is localhost or unset, prefer x-forwarded-host + x-forwarded-proto over request URL host
    - createAgentOnboardingToken includes optional ownerUserId in HMAC payload when provided; verify returns it
    - registerAgent with ownerId writes registered_agents.owner_id; without ownerId leaves NULL; ON CONFLICT uses COALESCE(excluded.owner_id, registered_agents.owner_id)
    - /api/onboarding/register passes ownerId from verified.payload.ownerUserId only — never from request JSON body
    - PLATFORM_LABELS.claude === "Claude Code"
  </behavior>
  <steps>
    1. Before editing symbols, run GitNexus impact upstream on registerAgent, createAgentOnboardingToken, verifyAgentOnboardingToken; warn if HIGH/CRITICAL (ownerId must stay optional — Pitfall 7).
    2. Wave 0 TDD: create apps/memroos/src/lib/__tests__/public-base-url.test.ts covering (a) non-localhost env wins, (b) localhost env skipped in favor of forwarded host, (c) forwarded host used when env unset, (d) trailing slash stripped. Confirm RED then GREEN.
    3. Implement resolvePublicMemroosUrl in apps/memroos/src/lib/public-base-url.ts exactly per D-11 / RESEARCH Pattern 3 (isLocalhostUrl helper + candidate order).
    4. Add optional ownerUserId?: string to CreateAgentOnboardingTokenInput and AgentOnboardingTokenPayload; include in payload when present (omit or undefined when absent so legacy tokens unchanged).
    5. Add optional ownerId?: string to RegisterAgentInput and optional ownerId on RegisteredAgent; map RegisteredAgentRow.owner_id through rowToRegisteredAgent.
    6. Update registerAgent INSERT column list to include owner_id; ON CONFLICT set owner_id = COALESCE(excluded.owner_id, registered_agents.owner_id). Do not wipe existing owners on re-register without a new claim.
    7. In onboarding register route, after verify, pass ownerId: verified.payload.ownerUserId into registerAgent. Explicitly ignore any client body ownerId/owner_id fields.
    8. Update PLATFORM_LABELS.claude to "Claude Code" (D-05).
  </steps>
  <action>
    Implement D-11 public URL helper and D-08/D-09 ownership claim plumbing. Keep ownerId optional so A2A/manual registerAgent callers stay unbroken. Never accept owner from register JSON body — signed token claim only (D-07/D-08). Do not change /api/onboarding/script. Do not require ownerId on legacy operator invites (undefined → NULL owner_id).
  </action>
  <verify>
    <automated>cd /Users/lcalderon/github/memroos-product && npm test -- --run apps/memroos/src/lib/__tests__/public-base-url.test.ts</automated>
  </verify>
  <done>
    resolvePublicMemroosUrl tested for D-11 cases; tokens can carry ownerUserId; registerAgent persists optional owner_id with COALESCE on conflict; onboarding register trusts token claim only; Claude label is "Claude Code".
  </done>
  <threat_notes>
    T-201-01 Spoofing: forge owner_id via register body — mitigate by ignoring body; only HMAC payload.ownerUserId.
    T-201-02 Tampering: open redirect / localhost command via client memroosUrl — bootstrap (Task 2) must ignore client memroosUrl and use resolvePublicMemroosUrl only; this task ships the resolver.
    Blast radius: registerAgent is shared — keep ownerId optional; run detect_changes before commit.
  </threat_notes>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap API + register Set-Cookie + route tests</name>
  <goal>INVBOOT-02 + INVBOOT-03 + INVBOOT-04 + INVBOOT-06 API surface — authenticated multi-harness mint with 60m TTL, public URLs, reviewer-allowed auth, and register cookie parity for D-03.</goal>
  <files>
    apps/memroos/src/app/api/onboarding/bootstrap/route.ts,
    apps/memroos/src/app/api/onboarding/__tests__/route.test.ts,
    apps/memroos/src/app/api/auth/register/route.ts,
    apps/memroos/src/app/api/auth/__tests__/session-cookies.test.ts
  </files>
  <behavior>
    - POST /api/onboarding/bootstrap without auth → 401
    - Authenticated reviewer (not only operator) can mint
    - Body { platforms: string[] } returns one command object per valid platform (label, agentId, command, expiresAt); rejects unknown platforms; caps array size (e.g. ≤ PLATFORMS.size)
    - Commands embed resolvePublicMemroosUrl result (not localhost when x-forwarded-host is public); ignore client memroosUrl
    - Each token uses ttlSeconds=3600, ownerUserId=session.userId, allowedAgentIds=[`${userId.slice(0,8)}-${platform}`], --id and --platform and --mcp-target auto in command
    - Registering via owned token sets DB owner_id = that user id
    - Successful /api/auth/register sets HttpOnly access_token + memroos_refresh cookies like login AND still returns JSON accessToken/userId
  </behavior>
  <steps>
    1. Create POST apps/memroos/src/app/api/onboarding/bootstrap/route.ts: authenticateUser only (no requireRole operator); 401 if missing; parse platforms whitelist from same PLATFORMS set as invite route; map via RESEARCH Pattern 1; ttlSeconds = 60 * 60; force memroosUrl = resolvePublicMemroosUrl(request).
    2. Response shape: { commands: Array&lt;{ platform, label, agentId, command, expiresAt }&gt; } using PLATFORM_LABELS for label.
    3. Extend apps/memroos/src/app/api/onboarding/__tests__/route.test.ts (tags: ['slow']): bootstrap 401; reviewer success multi-platform; public URL via forwarded host / non-localhost env; owner_id after script/register path or direct register with minted token; expired token still 403; unknown platform 400.
    4. Update /api/auth/register to mirror login cookie issuance (refresh row + Set-Cookie access_token + memroos_refresh with isHttpsRequest Secure flag) while keeping 201 JSON { accessToken, userId }.
    5. Add register Set-Cookie cases to session-cookies.test.ts (or adjacent auth test) asserting both cookies present on successful invitee register.
  </steps>
  <action>
    Add self-service bootstrap mint per D-02/D-07/D-10 and register cookie parity per RESEARCH recommendation for D-03. Do not put long-lived agent keys in invite URLs. Do not require operator role. Do not accept client-supplied memroosUrl or ownerUserId. Keep operator /api/onboarding/invite default TTL at 15m unchanged.
  </action>
  <verify>
    <automated>cd /Users/lcalderon/github/memroos-product && npm test -- --run apps/memroos/src/app/api/auth/__tests__/session-cookies.test.ts && npm run test:slow -- --run apps/memroos/src/app/api/onboarding/__tests__/route.test.ts</automated>
  </verify>
  <done>
    Bootstrap mints one public-URL command per selected platform bound to session.userId; register persists owner_id; register sets session cookies like login; slow onboarding suite covers mint + ownership + auth gates.
  </done>
  <threat_notes>
    T-201-03 Elevation: mint as another user — ownerUserId MUST come from session.userId only.
    T-201-04 Info disclosure: accessToken in JSON + cookies — cookies are HttpOnly (CR-01); Bearer used only for same-page bootstrap; short onboarding TTL (60m) with refresh re-mint.
    T-201-05 Replay: expired onboarding token → existing verify exp check → 403.
    T-201-SC: no new npm packages — N/A.
  </threat_notes>
</task>

<task type="auto">
  <name>Task 3: Invite Connect UX + Team email draft + copy tests</name>
  <goal>INVBOOT-01 + INVBOOT-02 + INVBOOT-05 + INVBOOT-06 UX — Option A post-register Connect step, jargon-light commands UI, and Team copyable 3-step email draft.</goal>
  <files>
    apps/memroos/src/app/invite/[token]/page.tsx,
    apps/memroos/src/app/team/page.tsx,
    apps/memroos/src/lib/invite-email-draft.ts,
    apps/memroos/src/lib/__tests__/invite-email-draft.test.ts
  </files>
  <steps>
    1. Refactor invite page into step state: "register" | "connect" | "commands" | "done". On successful register: store accessToken + userId in state; set step to "connect"; do NOT router.push("/login?message=account-created") yet (D-01).
    2. Pitfall 1: once step !== "register", skip invite re-validation on remount/effect so consumed invite does not wipe Connect UI. Bootstrap must not require unused team invite.
    3. Connect step: multi-select harnesses using PLATFORM_LABELS (D-05 human names). Primary copy: numbered easy steps, no HMAC/TTL/MCP jargon in prose (D-04). Optional secondary Advanced note may mention restarting the app. Include "Skip for now" → login/console.
    4. On Continue: POST /api/onboarding/bootstrap with Authorization: Bearer ${accessToken} and selected platforms; show one copyable command block per harness; copy "These commands expire in about an hour — use Refresh if they stop working" (D-10); Refresh re-calls bootstrap while token/session valid.
    5. Done: prefer router.push("/") or console if cookies present; else "/login?message=account-created" (D-03).
    6. Add buildInviteEmailDraft(inviteUrl) in apps/memroos/src/lib/invite-email-draft.ts with RESEARCH Team draft text (3 steps: open link, create account, pick AI tools + run one-line commands). Wire Team success panel to show textarea/pre + copy button (D-06/D-12). No SendGrid.
    7. Fast unit test invite-email-draft.test.ts asserts draft contains inviteUrl, "1)", "2)", "3)", and easy phrasing (no "HMAC"/"SendGrid").
    8. Grep gate (hygiene): ensure invite page no longer blindly redirects on success alone — success path must set connect step. Avoid ==0 on comment-only files; assert behavioral absence via test or targeted grep of router.push login only after done/skip.
  </steps>
  <action>
    Implement locked Option A UX (D-01..D-06, D-12) on existing invite and team pages. Keep commands fully copyable (including --mcp-target auto in the command string) while surrounding UI prose stays jargon-free (D-04 / RESEARCH A3). Do not add admin harness preselection (option B deferred). Do not call SendGrid.
  </action>
  <verify>
    <automated>cd /Users/lcalderon/github/memroos-product && npm test -- --run apps/memroos/src/lib/__tests__/invite-email-draft.test.ts apps/memroos/src/lib/__tests__/public-base-url.test.ts && npm run test:slow -- --run apps/memroos/src/app/api/onboarding/__tests__/route.test.ts</automated>
  </verify>
  <done>
    Invitee register advances to Connect → multi-harness commands → done/skip; Team shows copyable 3-step email draft with invite URL; easy-copy and bootstrap/owner tests remain green.
  </done>
  <threat_notes>
    T-201-06 Elevation: Connect after invite consume — bootstrap auth is JWT/session, not unused invite token; do not re-open consumed invite.
    T-201-07 XSS: accessToken briefly in React state for Bearer bootstrap — prefer cookies for Done navigation; do not render token into DOM/attributes.
  </threat_notes>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Invitee browser → `/api/auth/register` | Untrusted account create; gated by team invite token |
| Invitee browser → `/api/onboarding/bootstrap` | Authenticated session/JWT; platforms array untrusted input |
| Agent host → `/api/onboarding/register` | Onboarding HMAC token is sole trust for ownership claim |
| Admin browser → Team UI | Authz already admin; email draft is local clipboard only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-201-01 | Elevation / Spoofing | onboarding/register | mitigate | ownerUserId only inside HMAC token; ignore body ownerId |
| T-201-02 | Tampering | bootstrap mint URL | mitigate | resolvePublicMemroosUrl only; ignore client memroosUrl; skip localhost env when public host known |
| T-201-03 | Elevation | bootstrap route | mitigate | ownerUserId = session.userId only; authenticateUser required |
| T-201-04 | Info disclosure | register accessToken | mitigate | HttpOnly cookies on register; Bearer only for same-page bootstrap; 60m onboarding TTL |
| T-201-05 | Tampering / Replay | onboarding token | mitigate | existing exp + signature verify → 403 |
| T-201-06 | Elevation | invite Connect after consume | mitigate | step state + session/JWT for bootstrap; do not require unused invite |
| T-201-07 | Info disclosure / XSS | invite page token state | mitigate | never render accessToken in DOM; cookies for Done path |
| T-201-SC | Tampering | npm installs | accept | No new packages this phase |
</threat_model>

<verification>
Phase gate (all must pass before SUMMARY):
1. `npm test -- --run apps/memroos/src/lib/__tests__/public-base-url.test.ts apps/memroos/src/lib/__tests__/invite-email-draft.test.ts`
2. `npm test -- --run apps/memroos/src/app/api/auth/__tests__/session-cookies.test.ts`
3. `npm run test:slow -- --run apps/memroos/src/app/api/onboarding/__tests__/route.test.ts`
4. Source audit: INVBOOT-01..06 each covered by a task above
5. Manual smoke optional: create invite → register → Connect → mint → confirm command host is public; DB owner_id matches user
</verification>

<success_criteria>
- INVBOOT-01: Register success on `/invite/[token]` enters Connect step (no blind login redirect)
- INVBOOT-02: Multi-select returns one command per harness from POST /api/onboarding/bootstrap
- INVBOOT-03: Commands never embed localhost when public env or x-forwarded-host is known
- INVBOOT-04: Bootstrap-registered agents have owner_id = invitee user id
- INVBOOT-05: Team success UI shows copyable 3-step email draft including inviteUrl
- INVBOOT-06: Fast helper tests + slow onboarding route tests cover mint, owner_id, and easy-copy presence
</success_criteria>

## Source Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Invitee Connect + owned agents + Team email draft | 01 | COVERED | Tasks 1–3 |
| REQ | INVBOOT-01 | Register → Connect step | 01 | COVERED | Task 3 |
| REQ | INVBOOT-02 | Multi-select → one command each | 01 | COVERED | Tasks 2–3 |
| REQ | INVBOOT-03 | Public base URL | 01 | COVERED | Tasks 1–2 |
| REQ | INVBOOT-04 | owner_id persistence | 01 | COVERED | Tasks 1–2 |
| REQ | INVBOOT-05 | Team email draft | 01 | COVERED | Task 3 |
| REQ | INVBOOT-06 | Tests | 01 | COVERED | Tasks 1–3 |
| CONTEXT | D-01 | Post-register harness select | 01 | COVERED | Task 3 |
| CONTEXT | D-02 | Multi-harness commands | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-03 | Done / auto-session | 01 | COVERED | Tasks 2–3 (cookies + Done) |
| CONTEXT | D-04 | Easy non-jargon copy | 01 | COVERED | Task 3 |
| CONTEXT | D-05 | Human harness labels | 01 | COVERED | Tasks 1+3 |
| CONTEXT | D-06 | 3-step email draft | 01 | COVERED | Task 3 |
| CONTEXT | D-07 | Authenticated bootstrap, no keys in invite URL | 01 | COVERED | Task 2 |
| CONTEXT | D-08 | owner_id = user | 01 | COVERED | Tasks 1–2 |
| CONTEXT | D-09 | Reuse onboarding token pipeline | 01 | COVERED | Tasks 1–2 |
| CONTEXT | D-10 | Prefer ≤60m TTL + expire/refresh copy | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-11 | resolvePublicMemroosUrl | 01 | COVERED | Task 1 |
| CONTEXT | D-12 | Copyable draft only | 01 | COVERED | Task 3 |
| RESEARCH | — | POST /api/onboarding/bootstrap | 01 | COVERED | Task 2 |
| RESEARCH | — | Register Set-Cookie parity | 01 | COVERED | Task 2 |
| RESEARCH | — | Agent id `{userShort}-{platform}` | 01 | COVERED | Task 2 |
| RESEARCH | — | Skip for now | 01 | COVERED | Task 3 |
| RESEARCH | out of scope | SendGrid / option B / ops PUBLIC_BASE_URL | — | EXCLUDED | Deferred |

<output>
Create `.planning/phases/201-invite-multi-harness-agent-bootstrap/201-01-SUMMARY.md` when done
</output>
