---
phase: 118
plan: 1
type: planning
title: Proactive Recollection Triggering
requirements:
  - RECOLLECT-01
  - RECOLLECT-02
  - RECOLLECT-03
  - RECOLLECT-04
  - RECOLLECT-05
  - RECOLLECT-06
  - RECOLLECT-07
status: completed
---

# 118-01 Plan: Proactive Recollection Triggering

## Progress

- 2026-06-27: Slice 1 pure recollection policy foundation implemented. Added `apps/memroos/src/lib/recollection-policy.ts` with deterministic `search_required` / `search_skipped` decisions, bounded tier-aware query planning, transparent candidate score components, thresholded context-pack assembly, policy-denied exclusion, and belief-stage reliance labels for bronze/silver/gold candidates. Focused tests cover recency/project/handoff triggers, mechanical skip receipts, query limit/scope preservation, old-critical-over-recent-noise ranking, stale-source demotion, policy denial, and silver/bronze caveats. Runtime wiring, memory trace receipts, recall eval fixtures, and NOC read model remain open.
- 2026-06-27: Slice 2 agent-runtime memory-client wiring implemented. `buildContextInjection()` now records a typed recollection decision, maps retrieved runtime memories into recollection candidates, assembles a policy-gated context pack, excludes `authorization: "denied"` memories from injected text, preserves silver/bronze caveats in context-pack metadata, and returns the legacy `{ text, memories }` fields alongside `{ recollection, contextPack }`. Runtime memory records can now carry tier, belief stage, source health, authorization, policy risk, provenance, and usefulness metadata. Memory trace receipts, recall eval fixtures, and NOC read model remain open.
- 2026-06-27: Slice 3 recollection receipt persistence implemented. `recordMemoryTrace()` now accepts `causalPath.recollection` receipts, persists them in `agent_memory_traces.causal_path_json`, emits privacy-safe recollection metadata on the existing Phase 117 `retrieval_trace` efficiency event, and adds operator timeline lines for skipped-search receipts. Efficiency payloads expose decision, timing, reason codes, skip reason, injected/ignored candidate IDs, scores, reliance labels, and belief-stage counts without copying raw memory content. Recall eval fixtures and NOC read model remain open.
- 2026-06-27: Slice 4 proactive recall eval fixtures implemented. `MemoryRecallEvalCase` now supports optional `avoidMemoryIds` and `avoidFacts` so negative/noisy-recall fixtures can fail when a decoy appears in top-k results. `evals/memory-recall/cases.json` now includes three Phase 118 full-mode scenario cases for active-handoff before-plan recall, source-rule before-tool-use recall, and noisy-recency suppression where a recent bronze deploy note must not outrank an older gold deployment-readiness rule. NOC read model remains open.
- 2026-06-27: Slice 5 operator/NOC recollection read model implemented. `/api/operations/noc` now aggregates recollection decisions from `retrieval_trace` efficiency payloads, including required vs skipped counts, skip reasons, injected/ignored candidate counts, policy-denied and below-threshold counts, belief-stage counts, reliance counts, and the five latest recollection decisions. `EfficiencySignals` now renders a recollection decisions card while remaining backward-compatible with older NOC payloads missing the new block. Phase 118 implementation is complete.

## Objective

Make MemRoOS decide when an agent needs memory before work starts, before tools run, or before a final answer, without relying on Luis to say "search memory." The result should be an auditable recollection decision that either searches and injects context or records why search was skipped.

## Scope

- Add a deterministic recollection trigger policy for task text, entities, recency language, source refs, active project, handoff state, rediscovery risk, and required timing.
- Add a bounded query planner that emits tier-aware recall queries with explicit project/source scope.
- Add a candidate scorer that blends semantic/lexical relevance, recency, salience/importance, source freshness, prior usefulness, and policy-risk penalties.
- Add context-pack assembly that injects only threshold-cleared memories and records ignored/skipped candidates.
- Add a belief-stage label to each retrieved candidate and injected memory: bronze raw source snapshot, silver candidate claim, or gold admitted operational truth.
- Write recollection receipts into memory traces and NOC efficiency telemetry.
- Extend recall evals with proactive timing cases and negative fixtures for noisy recency.

## Non-Goals

- No new memory backend.
- No replacement of mem0, Qdrant, Neo4j, SQLite, qmd, or GitNexus.
- No automatic cross-project recall without explicit allowlist and policy proof.
- No storing raw sensitive payloads in recollection receipts.
- No LLM-only hidden trigger decision; any LLM assist must produce typed evidence consumed by deterministic policy.
- No treating silver candidate claims or bronze raw snapshots as gold operational truth without explicit promotion policy, provenance, freshness, conflict, and dedupe checks.

## Design Contract

### Trigger Decision

The recollection policy returns:

```json
{
  "decision": "search_required",
  "timing": "before_plan",
  "reasons": ["task_has_recent_ref", "project_has_recent_handoff"],
  "queries": [
    {
      "text": "Cordant Sagi meeting follow-through",
      "tiers": ["episodic", "vector", "qmd"],
      "scope": { "project": "cordant" },
      "limit": 8
    }
  ],
  "skipReason": null
}
```

Skip decisions are also receipts:

```json
{
  "decision": "search_skipped",
  "timing": "before_tool_use",
  "reasons": ["low_memory_need", "no_stable_entities"],
  "queries": [],
  "skipReason": "Task is a local formatting command with no recall dependency."
}
```

### Scoring

Use a transparent score object per candidate:

- relevance: lexical/semantic match to task and generated query
- recency: time since capture or last access, windowed by task type
- importance: salience tier, pin state, access/reinforcement history
- freshness: source health, source mtime/index time, stale-source penalty
- usefulness: prior successful use in similar tasks or eval traces
- policy: authorization result, redaction state, sensitivity penalty

Recency is a boost, not an override. A recent low-salience memory should lose to an older critical rule when the task depends on that rule.

### Belief Stage

Every retrieved candidate and injected memory carries a belief stage:

- `bronze_raw_source`: raw source snapshot such as a transcript, document, trace, message, or vault artifact. It can support evidence review, but agents should not treat it as summarized truth.
- `silver_candidate_claim`: extracted or summarized claim such as an `agent_memory_candidates` row, source-span-backed extraction, or proposed lesson. Agents must caveat it unless a workflow explicitly asks for draft/candidate material.
- `gold_operational_truth`: admitted memory that passed provenance, policy, freshness, conflict, dedupe, and promotion checks. Agents may rely on it directly, subject to TTL or revocation.

Receipts must include the belief stage, source/provenance pointer, authorization result, promotion reason when gold, and caveat reason when silver or bronze is injected.

## Implementation Steps

1. Add `apps/memroos/src/lib/recollection-policy.ts` with trigger, query planning, scoring, thresholding, and receipt types.
2. Add focused tests for required-search, skipped-search, noisy-recency suppression, old-critical promotion, stale-source demotion, policy-denied candidates, and silver/bronze candidates not being treated as gold.
3. Wire recollection policy into the narrowest runtime seam first: dispatch/context-pack assembly or the agent-runtime memory client.
4. Record receipts through `recordMemoryTrace()` and Phase 117 efficiency events once that event table exists.
5. Add eval cases to `evals/memory-recall/cases.json` for proactive timing and negative recall.
6. Add a small operator-facing read model so NOC can show recent recollection decisions, skipped-search reasons, and belief-stage counts for retrieved/injected memories.

## Verification

- `npm --prefix apps/memroos run test -- src/lib/__tests__/recollection-policy.test.ts --run`
- focused route/runtime tests for the chosen integration seam
- `npm --prefix apps/memroos run eval:memory -- --canary`
- `npm run typecheck`
- `npm run build`
- `gitnexus_detect_changes()` before commit if runtime code is edited
