# Phase 147 Plan 01 — Secrets Broker + Kernel HA Path

**Phase:** 147  
**Milestone:** v8.5  
**Requirements:** FLEET-22..26  
**Status:** potential plan  

## Goal

Document and implement the minimum secrets + HA path so the fleet plane is not permanently "one Mac SQLite". Prefer MIT/Apache OSS: litestream, Vault/Sealed Secrets patterns, existing envelope encryption.

## Tasks

1. Secrets broker design: where adapter API keys live, rotation, no plaintext in audit
2. Kernel durability: litestream → S3/compatible **or** Postgres migration path for canonical registry
3. LangGraph checkpoint durability alignment with Phase 144
4. Operator runbook: restore drill (restore from backup, verify registry + one graph resume)
5. Stretch notes only: SPIFFE/SPIRE, Envoy rate limit for 50-machine — document, do not implement

## Success criteria

1. Secrets path documented and used for at least one adapter key class
2. Backup/restore drill for kernel state is written and executed once
3. Stretch multi-machine identity deferred with explicit "not v8.5"

## Verification

- Restore drill log attached to phase summary
- No secrets in git or receipts
