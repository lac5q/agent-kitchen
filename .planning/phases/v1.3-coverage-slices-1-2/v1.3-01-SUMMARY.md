# M1.3 Coverage — Slices 1 + 2

**Completed:** 2026-07-18
**Branches:**
- `gsd/v1.3-coverage-slice-1` (merged as part of `3ac1b713`)
- `gsd/v1.3-coverage-slice-2` (merged as part of `f2581e77`)

**Commits:**
- `ce768b00` test(coverage): v1.3 slice 1 — utility parsers + topology + registry helpers
- `452da05f` test(coverage): v1.3 slice 2 — cron-health.ts from 0% to 95% lines

## Scope

Continued the M1.3 coverage push past the M1.2 src/lib baseline (89.4/74.5) by adding focused unit tests for previously zero-coverage utility files and the cron-health module.

## Slice 1 — utility parsers + topology + registry helpers

| File | Before | After (v8) |
|---|---|---|
| `agent-registry-seed.ts` | 0% / 0% / 0% / 0% | 89.83% stmts / 89.39% branch / 100% funcs / 100% lines |
| `runtime-topology.ts` | uncovered | 100% / 98.14% / 100% / 100% |
| `storage-health.ts` | thin | 100% / 85.71% / 100% / 100% |
| `ui-constants.ts` | uncovered | 100% / 100% / 100% / 100% |
| `agent-runtime/middleware.ts` | uncovered | 93.06% / 80.35% / 95.83% / 92.3% |
| `agent-runtime/observability.ts` | uncovered | 97.22% / 84.61% / 100% / 100% |

80 new tests added across these files.

## Slice 2 — cron-health.ts

| File | Before | After (v8) |
|---|---|---|
| `cron-health.ts` | 0% / 0% / 0% / 0% | 95% / 85.71% / 100% / 98% lines |

33 new tests covering: ensureDefaultCronJobs (seeding, idempotency, status preservation), upsertCronHealthJob (defaults, status normalization, conflict upsert), updateCronJobStatus (status transitions, normalization, missing-job handling), isCronJobRunnable (active/paused/stopped/missing behavior), recordCronHealthRun (success/failure paths, metadata merging, status preservation), listCronHealthJobs (grouping by source_family), computeHealth (paused/stopped/warning/unknown/healthy/stale/failure).

## Verification

- Lint: 0 errors (84 pre-existing warnings).
- Typecheck: clean.
- `test:fast`: 3322 passed + 23 skipped (vs baseline 3289; net +33 new tests from this work).
- 88 knowledge-mcp pytest passed.
- Pre-existing flakes unrelated: `audit-perf.test.ts` hook timeout, `pulse-strip.test.tsx` UI copy assertion — both were failing on `f3974a5d` baseline before any of this work.

## Constraints honored

- No new runtime npm dependencies.
- No source-file changes (only new test files) — preserves the slice-1/2 contract of "tests only".
- MEMSEC-08 regression corpus byte-identical.

## Source-level findings surfaced (NOT fixed in scope)

- `upsertCronHealthJob` with `id: ""` stores with a UUID but reads back with empty string, so the inserted row is unreachable. Edge case; harmless unless caller explicitly passes empty id.
- `updateCronJobStatus` does not normalize invalid status; SQLite CHECK constraint rejects it. Pre-normalization happens only on the read path (`mapCronHealthJob`).

## Follow-ups

- M1.3 src/app + src/components coverage push remains parked at 66.4/55.9 (228 files below target). The 6-milestone plan in `docs/test-coverage-baseline.md` continues to apply for future slices.
