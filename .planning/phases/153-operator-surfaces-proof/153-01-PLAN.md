# Phase 153 — Operator Surfaces + Proof

**Milestone:** v8.11 Unified Meeting Memory  
**Created:** 2026-07-14  
**Updated:** 2026-07-14  
**Status:** planned  
**Requirements:** URECALL-04, URECALL-05, URECALL-06

## Goal

Console + docs surface meeting QMD lanes; regressions prove Monaco Circleback and Fathom Impromptu are findable via `memory_recall` without `-c`.

## Tasks

### T1 — multi-search QMD lane (URECALL-04)

- Extend `/api/memory/multi-search` with a `qmd` tier that searches enabled meeting collections.
- Shared TS helper for collection discovery + qmd CLI search.

### T2 — Docs + collections.config (URECALL-05)

- Update `docs/integrations/meet-recordings.md` for `memory_recall` + health + public providers.
- List private meeting collections in `collections.config.json` (documented; paths remain local).

### T3 — Regressions (URECALL-06)

- Python tests: resolver federation + Monaco/Fathom fixture search without `-c`.
- Prefer fixture-based unit tests; optional live skip if qmd/collections absent.

## Success Criteria

1. multi-search returns a `qmd` tier.
2. Docs describe unified recall; collections.config lists meeting collections.
3. Tests prove recall path does not require agent-supplied `-c`.

## Out of Scope

- Expanding ContextSourceType.
- Global embed backfill.
