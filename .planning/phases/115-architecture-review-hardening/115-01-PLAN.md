---
phase: 115
plan: 1
type: implementation
title: A3 SQLite Migration Runner and Architecture Review Roadmap
requirements:
  - ARCHREV-03
  - ARCHREV-09
status: complete
---

# 115-01 Plan: A3 SQLite Migration Runner and Architecture Review Roadmap

## Objective

Close the highest-value contained code slice from `.code-review/ARCHITECTURE-REVIEW.md`: finding A3 on SQLite schema versioning and first-start admin seeding. Capture the remaining architecture review findings as explicit GSD requirements so they can be executed as follow-on slices instead of staying as review prose.

## Scope

- Add a versioned SQLite migration runner stamped through `PRAGMA user_version`.
- Preserve the existing additive schema as baseline migration version `1`.
- Fail closed when a database has a future schema version newer than this code supports.
- Make default admin seeding complete before `getDb()` returns.
- Add regression tests proving version stamping, legacy upgrade, and synchronous admin seed behavior.
- Roadmap all architecture review findings as `ARCHREV-*` requirements.

## Verification

- `npm --prefix apps/memroos run test -- src/lib/__tests__/db.test.ts --run`
- `npm run typecheck`
- `npm run build`
- `npm run lint`
- Full test suite before deploy.

