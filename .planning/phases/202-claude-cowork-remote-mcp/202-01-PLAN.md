---
phase: 202-claude-cowork-remote-mcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/cordant-hermes-01/systemd/memroos-mcp-http.service
  - deploy/cordant-hermes-01/cloudflared/memroos-cordant.yml
  - scripts/install-memroos-mcp-systemd.sh
  - docs/integrations/mcp.md
  - docs/production-deployment.md
  - apps/memroos/src/lib/invite-email-draft.ts
  - apps/memroos/src/app/invite/[token]/page.tsx
  - apps/memroos/src/lib/ui-constants.ts
autonomous: true
requirements:
  - COWORK-01
  - COWORK-02
  - COWORK-03
  - COWORK-04
  - COWORK-05
must_haves:
  truths:
    - "Public HTTPS Streamable HTTP MCP responds on https://memroos-cordant.epiloguecapital.com/mcp (not connection refused)"
    - "Unauthenticated /mcp is rejected (401/403); valid Bearer from MEMROOS_MCP_BEARER_TOKEN is accepted"
    - "Invite Connect + Team draft include Claude Cowork connector steps (not curl|bash as primary for Cowork)"
    - "Deep-link or numbered connector instructions reference the Cordant /mcp URL"
    - "External smoke checklist exists and is run against live Cordant hostname"
  artifacts:
    - path: "deploy/cordant-hermes-01/systemd/memroos-mcp-http.service"
      provides: "Supervised Streamable HTTP MCP on :8765"
    - path: "deploy/cordant-hermes-01/cloudflared/memroos-cordant.yml"
      provides: "Path /mcp* → 127.0.0.1:8765 before UI catch-all"
    - path: "scripts/install-memroos-mcp-systemd.sh"
      provides: "Linux installer generating bearer env + enabling unit"
---

# Phase 202 Plan 01 — Cowork Remote MCP on Cordant

## Locked decisions (execute as written)

- **Auth D-09a:** `Authorization: Bearer` via `MEMROOS_MCP_BEARER_TOKEN` (ChatGPT HTTP pattern). Cloudflare Access deferred.
- **Ingress D-03:** same hostname path `/mcp*` → `http://127.0.0.1:8765` (higher priority than UI `:3000`).
- **Runtime:** host systemd running `scripts/memroos-mcp.sh --http --host 127.0.0.1 --port 8765` (NOT docker `knowledge-mcp` on 3291 — that is a different FastAPI health service).
- **Platform UX:** add `cowork` label "Claude Cowork" with connector instructions; keep Claude Code curl|bash.

## Tasks

### Task 1 — Linux MCP systemd + cloudflared path (ops + repo)
1. Add `deploy/cordant-hermes-01/systemd/memroos-mcp-http.service` and matching cloudflared YAML snippet.
2. Add `scripts/install-memroos-mcp-systemd.sh` (generate `~/.memroos/memroos-mcp-http.env` with bearer, enable unit).
3. On hermes: install unit, update `/etc/cloudflared/memroos-cordant.yml`, restart cloudflared + mcp.
4. Smoke: local `curl -i http://127.0.0.1:8765/mcp` reaches FastMCP; public `https://memroos-cordant.epiloguecapital.com/mcp` without bearer → 401/403; with bearer → MCP handshake response (not 404 from Next).

### Task 2 — Invite/Team Cowork UX
1. `PLATFORM_LABELS.cowork = "Claude Cowork"`.
2. Invite Connect: when cowork selected, show connector URL + deep link + Request header bearer note (operator pastes token once at Team level; do not put long-lived bearer in invite email body by default — link to Team settings / rotate doc).
3. Extend `buildInviteEmailDraft` with optional Cowork step when Cordant URL.

### Task 3 — Docs + external smoke checklist
1. Update `docs/integrations/mcp.md` + `docs/production-deployment.md` Cordant `/mcp` section.
2. Checklist under phase dir: Tailscale-less laptop add custom connector → tools list.
3. Do **not** mark COWORK-05 complete until checklist executed with evidence.

## Verification (must pass before SUMMARY)

```bash
# hermes
systemctl is-active memroos-mcp-http
curl -sS -o /dev/null -w "%{http_code}" http://127.0.0.1:8765/mcp
curl -sS -o /dev/null -w "%{http_code}" https://memroos-cordant.epiloguecapital.com/mcp
# expect non-connection-refused; unauth reject
```

## Honesty rule

Never mark Phase 202 or COWORK-* complete in ROADMAP/STATE without SUMMARY + live smoke evidence above.
