# Requirements Archive: v1.2 Live Data + Knowledge Sync

**Milestone:** v1.2 Live Data + Knowledge Sync
**Shipped:** 2026-04-12
**Requirements:** 16 total — all satisfied

## CONFIG — Library Fixes

- [x] **CONFIG-01**: User can see the correct meet-recordings file count in Library (fix basePath pointing to stale path — diverged from where ingestion actually writes)
  - **Outcome:** Fixed basePath `/Users/lcalderon/knowledge/gdrive/meet-recordings` → `/Users/lcalderon/github/knowledge/gdrive/meet-recordings`. Count now shows 100 documents (was 105 from stale GDrive sync path).

- [x] **CONFIG-02**: mem0-exports collection is visible in Library view alongside other knowledge collections
  - **Outcome:** Added mem0-exports entry with basePath `/Users/lcalderon/github/knowledge/mem0-exports`, category=agents. Shows 22 docs in Library.

## FLOW — Live Observability

- [x] **FLOW-08**: Obsidian node derives status from live vault filesystem signals (today's journal exists → active; >24h → idle; inaccessible → error) — hardcoded status removed
  - **Outcome:** `obsidianStatus()` added to health API. fsStat vault root + today's journal (`toLocaleDateString("en-CA")`). ENOENT → error. 3 states covered.

- [x] **FLOW-09**: knowledge-curator node derives status from `/tmp/knowledge-curator.log` (ran <26h + complete → active; ran with warnings → idle; missed cron → error) — hardcoded status removed
  - **Outcome:** `curatorStatus()` added to health API. Reads log, parses last run block, checks "Knowledge Curator complete." and "Warning:" count, 26h window. ENOENT → error.

- [x] **FLOW-10**: Flow diagram auto-fits the viewport on initial page load without the user needing to manually zoom or pan
  - **Outcome:** `fitViewOptions: { padding: 0.2, duration: 200 }` set in ReactFlow. Auto-fits on mount.

- [x] **FLOW-11**: Flow diagram is visually clean — nodes well-spaced, edges don't cross excessively, readable at default zoom, fits screen without feeling cramped or oversized
  - **Outcome:** All 50+ edges switched to explicit type (smoothstep for cross-row, straight for same-row). `animated:true` only on amber/request-flow edges (~80% motion noise reduction). Container height: `min(900px, calc(100vh - 220px))`.

## KNOW — Knowledge Sync

- [x] **KNOW-06**: Obsidian daily journal notes are ingested into mem0 nightly as memories (Obsidian → mem0 direction, running as curator Step 6)
  - **Outcome:** `obsidian-to-mem0.py` fixed and activated. 4 journal entries synced to mem0 as `agent_id=claude` memories confirmed via API.

- [x] **KNOW-07**: Ingestion is idempotent — mtime watermark + content-hash dedup + origin tag prevent duplicate memories across runs
  - **Outcome:** All 3 loop guards verified active. Second run: 0 synced, 4 duplicate — idempotency confirmed. Memories carry `metadata.source="obsidian-sync"`.

## SKILL — Skill Management Dashboard

- [x] **SKILL-01**: `skill-sync.py` appends events to `skill-contributions.jsonl` after each sync/prune run (JSONL bridge — ~10 lines added to existing script)
  - **Outcome:** `--export-jsonl` flag activated in cron. `skill-contributions.jsonl` seeded with 235 events from first cron run.

- [x] **SKILL-02**: `/api/skills` route returns live skill stats (total count, contributed by Hermes, contributed by Gwen, stale count, last pruned date)
  - **Outcome:** New route reads live skill directory + JSONL. Returns `totalSkills=248` (was hardcoded 405). 13/13 tests passing.

- [x] **SKILL-03**: Cookbooks node in Flow shows live skill stats panel — Skills · From Hermes · From Gwen · Last Pruned · Stale
  - **Outcome:** 5-row stats panel rendered when Cookbooks selected. Live data via `useSkills()` hook.

- [x] **SKILL-04**: Dashed cyan edges appear in Flow connecting alba to cookbooks and cookbooks to gateways (skill contribution flow)
  - **Outcome:** `skillSyncEdges` added with `strokeDasharray:"5,5"`, `EDGE_COLORS.sync = "#06b6d4"`. Guarded by alba availability.

- [x] **SKILL-05**: Activity feed shows skill contribution events (last 2h from JSONL) when cookbooks node is selected
  - **Outcome:** Activity route section 4 reads JSONL last 2h, emits events with `node:"cookbooks"`.

## AGENT — Gwen Self-Improving Loop

- [x] **AGENT-01**: Gwen's `self-improving-agent` skill installed with mem0-only memory (Cognee-OpenClaw explicitly NOT installed — conflicts with Qdrant/mem0 architecture)
  - **Outcome:** SKILL.md installed in `~/.openclaw/skills/`. Cognee-OpenClaw absent (confirmed).

- [x] **AGENT-02**: Skills Gwen creates during sessions are staged to `.hermes-staging/` for Hermes pickup (not to `~/skills/`)
  - **Outcome:** `STAGING_DIR` pickup block added to `skill-sync.py`. Skills from `.hermes-staging/` copied to master with 365-day grace period.

- [x] **AGENT-03**: Gwen reflection cron runs at 3am (avoids 4am collision with Hermes sync cron)
  - **Outcome:** `gwen-reflection.json` registered: `expr="0 3 * * *"`, `tz="America/Los_Angeles"`. No collision with Hermes 4am cron.

## Out of Scope

| Feature | Reason |
|---------|--------|
| Auth/login | Local dashboard, single user |
| Write operations | Read-only dashboard — no mutations from UI |
| Real-time file-watcher sync (Obsidian → mem0) | iCloud/filesystem events unreliable; nightly batch sufficient |
| Merging ~/knowledge/ and ~/github/knowledge/ paths | Data duplication risk; canonical path decision is sufficient |
| Cognee-OpenClaw for Gwen | Creates parallel memory layer conflicting with Qdrant/mem0 |
| qmd embed anywhere | Forbidden per architecture — all semantic search via Qdrant Cloud |
| execSync/exec in any new code | Security constraint — use execFileSync or pure fs/promises |
| Alerting/notifications | Dashboard is read-only observability only |
| Mobile-first | Desktop-first; responsive is nice-to-have |

## Deferred to v1.3

| Requirement | Reason |
|-------------|--------|
| FLOW-12: Collapsible node groups | Not needed for v1.2 observability goals |
| FLOW-13: Per-node inline activity drill-down | Deferred — low usage signal |
| SKILL-06/07/08: Failure rate, coverage gaps, usage heatmap | Deferred — requires more JSONL signal volume |
| KNOW-08/09: projects/ subdirectory ingestion, per-project agent_id routing | Deferred — canonical path decision needed first |

---
*Requirements archived: 2026-04-12*
*Coverage: 16/16 requirements satisfied*
